# 시안 갭 리스트 — 기획 ↔ 시안 ↔ 코드 (2026-08-31 작성 · **9/2 갱신**)

> 대상: 디자이너 전달용. 관리자(A-01~~A-06)는 팀원 B 담당이라 제외했다.
> 기준 문서: `../docs/spec.md`(US1~~16, FR-001~063) · `../docs/tasks.md`(web 태스크, 8/31 갱신) · 시안 `design/design.pen` 보드 `nZTTY`(웹 v6)·`MGeTr`(L-01) · 코드 `src/config/routes.ts` 34개 라우트.
> 계약(REST/WS)은 문서가 없어 `../Passmate-KMP/shared` DTO를 기준으로 삼았다 (§C 참고).
> 판정 방법: 기획이 요구하는 화면·상태를 시안 프레임과 (시안을 1:1로 구현한) 코드에서 찾아 없으면 갭으로 기록.
>
> **⚠️ 9/1 갱신** — 본문(§1~§6)은 **8/31 시점 기록이다.** 그 뒤 두 가지가 들어왔다.
> ① **API 명세서 v2 + 백엔드 실물**(9/1 새벽) — 계약 질문 상당수가 풀렸다. 라우트 44개(45→44), 코드 기준선도 바뀌었다.
> ② **시안 v6 전달본**(9/1 16:39) — 우리가 요청한 화면 여럿이 도착했고, 대신 **새 질문 6건**이 생겼다.
> ③ **백엔드 확정 계약**(9/2) — 스웨거가 아니라 **백엔드 코드**를 원천으로 51개 엔드포인트를 1:1 대조했다. `/api/v1` prefix 없음·오프셋 페이지·null 필드 생략·UTC naive 시각·WS 봉투가 전부 달랐고, 웹을 거기에 맞췄다.
> 어디가 풀리고 어디가 남았는지는 **§0-2(9/2)** → **§0-1(9/1)** 갱신 이력, 그리고 **§4-2 G 항목**·**§4-3 H 질문**을 먼저 볼 것. 본문의 낡은 서술은 지우지 않고 해소 표시만 달았다.

## 0. 한눈에

| 구분                                   | 건수      | 내용                                                                                                                                                          |
| -------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A. 화면 자체가 없음**                | 8         | 학생 결과·리포트(웹), 학생 웹 대기실, 호스트 최종 결과, 첨삭 입력, 공개 방 목록, 로그인 콜백, 호스트 공개 프로필, 신고 다이얼로그                             |
| **B. 화면은 있으나 상태·변형이 없음**  | 14개 화면 | 오류·빈 상태·한도·실패·재연결·환불·보류·화면 잠금 등                                                                                                          |
| **C. 기획–시안–계약 충돌 (결정 필요)** | 10        | 로그인 방식, 에디터 API 부재, 첨삭 API 부재, 결제 수단 관리, 알림, 캐릭터 ID, 정산 수치 등                                                                    |
| **D. 백엔드에 요청할 계약 추가**       | 14        | 웹이 필요하지만 KMP 계약에 없는 호출                                                                                                                          |
| **N. 확정 계약을 보고 알게 된 공백**   | 7         | 목록에 PIN 없음, PIN 조회에 호스트·문항 수 없음, 참가자별 제출 여부 없음, 진행률 없음, 결과에 호스트 이름 없음, 게스트 이관 문구, 앱 공통 패턴 2장 — **§0-2** |
| **H. 백엔드 회신 대기 질문**           | 9         | B-1~B-9 — **§4-3**                                                                                                                                            |

우선순위는 spec의 스토리 우선순위(P1 MVP → P4)를 그대로 썼다. **P1은 시연 최소 제품(US1~3)에 필요한 것**이다.

## 0-3. 갱신 이력 (2026-09-04) — 시안 프레임 인벤토리 + 선생님 프로필 대조

> 방법: Pencil로 `design/design.pen`을 열어 보드 `01 · 웹 — 선생님 · 공통`의 프레임을 전수로 훑고,
> **레이어 이름이 아니라 텍스트 `content`만 근거로** 삼았다(레이어 이름이 낡은 프레임이 많다).

### ✅ §2 "A. 화면 자체가 없음"은 이제 사실이 아니다 — **A 8건 전부 코드가 있다**

프레임 인벤토리와 코드를 함께 확인한 결과, **없는 것은 "화면"이 아니라 "웹 시안"이었다.**
§2의 A 목록은 8/31 기준이라 그대로 두고, 아래를 현재 상태로 읽는다.

| 요청                               | 웹 시안                                                      | 코드                                    | 지금 상태                                                                                           |
| ---------------------------------- | ------------------------------------------------------------ | --------------------------------------- | --------------------------------------------------------------------------------------------------- |
| **A-1** 학생 결과·리포트(웹)       | ✅ `wr0Pp` 최종 결과 · `aUb5m` 내 리포트 · `KkzVg` 문항 상세 | ✅                                      | **시안대로 구현 완료.** 본문 폭 1280·버튼 234×48·KPI 4종·"틀린 n문항만 다시 풀기" 문구까지 일치한다 |
| **A-3** 호스트 최종 결과(프로젝터) | ✅ `dCOgo` W-12 · `AnrUC` 레일 접힘                          | ✅ `host/live/final-page.tsx`           | 구현됨 (알려진 버그는 `DESIGN_DIFF_W` A-2)                                                          |
| **A-7** 호스트 공개 프로필         | ✅ `SSSTw`                                                   | ⚠️ `host/profile/host-profile-page.tsx` | **코드가 앱 M-10 이식본이다** — 아래 표 참고                                                        |
| **A-2** 학생 웹 대기실             | ❌ **없다**                                                  | ⚠️ `participant/play/waiting-page.tsx`  | **코드가 앱 M-02 이식본이다**(파일 주석에 그렇게 적혀 있다)                                         |

문서 전체에서 대기실 프레임은 `IEZTk`(W-04 프로젝터·호스트용)와 `Ybj9i`·`TY2oz`(M-02 앱·학생용)뿐이다.

> **디자이너에게 남은 요청은 둘로 좁혀졌다.**
> ① **A-2 학생 웹 대기실** — 유일하게 웹 시안이 없는 화면.
> ② **W-11 부족분 0 상태** — 아래.
>
> A-7은 시안이 왔으니 요청이 아니라 **우리 쪽 재작업 대상**이다. 다만 아래 표의 ❌ 세 줄
> (평균 참여율 · 한 줄 소개 · 레벨 엠블럼)은 답을 받아야 그릴 수 있다.

### 🔴 W-11(결제) 부족분 0 상태 — 여전히 대기 중

`§3 B` W-11 행에 적어 둔 그대로다. 코인이 충분한데도 "₩10,000 충전"이라고 말하는 화면
(QA `F-12`)을 못 고치고 있다. **부족분이 0일 때의 결제 화면**을 그려 주시면 바로 붙인다.

### 🟡 A-7 선생님 프로필 — 시안은 왔는데 코드는 아직 앱 시안이다

코드(`features/host/profile/host-profile-page.tsx`)는 **앱 M-10을 옮겨 만든 것**이라
웹 시안 `SSSTw`와 뼈대부터 다르다. 문구까지 한 줄씩 대조한 결과다.

| #   | 시안 `SSSTw`                                                            | 코드                                           | 판정                                                                                  |
| --- | ----------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| 1   | 캐릭터 아바타 104px                                                     | ~~이니셜 타일~~ → 캐릭터 아바타                | ✅ **고쳤다**(2026-09-04). `HostProfileResponse.defaultAvatarId`가 생겨 D-19가 풀렸다 |
| 2   | 무료 칩 민트 · 유료 칩 앰버                                             | 유·무료 **둘 다 주황**이었다                   | ✅ **고쳤다** — 칩을 직접 그리다 색 분기를 빠뜨렸다. 공용 `StatusChip`으로 되돌렸다   |
| 3   | 본문 폭 **1280**(1440 캔버스)                                           | `max-w-xl`(**576px**)                          | ❌ **레이아웃이 통째로 다르다.** 앱 시안 기준이라 좁다                                |
| 4   | 지표 **4칸** — 평균 평가 · 방 운영 · 누적 학생 · **평균 참여율 92%**    | 3칸                                            | ❌ **평균 참여율은 계약에 없다**(`HostProfileResponse`에 없음) → 빼도 될지 확인 필요  |
| 5   | 한 줄 소개 "Spring · JPA · CS 면접 대비 방을 운영해요 · 2026년 3월부터" | 감춤                                           | ❌ 계약에 없다(기존 기록 유지)                                                        |
| 6   | 레벨 칩 "Lv.3 검증된 운영자"만                                          | 칩 + **육각형 레벨 엠블럼**                    | ⚠️ 코드에만 있다 — 시안대로 뺄지 확인                                                 |
| 7   | "획득 뱃지" · "지금 열려 있는 방"                                       | "획득한 뱃지" · "운영 중인 방" + "전체 보기 ›" | ⚠️ 문구 차이. "전체 보기"는 시안에 없다                                               |
| 8   | 하단 "프로필 신고 · 차단"                                               | 없음                                           | ❌ 신고는 계약이 있고(`POST /reports`), **차단은 화면이 없다**(QA §5)                 |
| 9   | "‹ 뒤로"                                                                | 없음                                           | ⚠️                                                                                    |

### 🟡 칩 색 규칙이 시안 안에서 어긋난다 — **디자이너 확인**

같은 "유료/무료" 칩인데 두 프레임의 색이 다르다. 무료는 일치하고 **유료만 갈린다.**

|                       | 무료                         | 유료                                    |
| --------------------- | ---------------------------- | --------------------------------------- |
| `direz` 공개 방 목록  | `#eaf8f1` / `#0e8a63` (민트) | **`#f3f4f6` / `#6b7280` (회색)**        |
| `SSSTw` 선생님 프로필 | `#eaf8f1` / `#0e8a63` (민트) | **`#fff3db` / `#6e4c06` (앰버)**        |
| 코드 `StatusChip`     | `bg-muted` (회색)            | `bg-orange-soft` / `text-orange` (주황) |

**물어볼 것**

1. 유료 칩은 회색인가 앰버인가? (두 프레임이 다르다)
2. 무료 칩은 시안 두 곳 모두 **민트**인데 코드는 회색이다 — 민트로 맞추면 되나?

> 코드의 `free`·`paid` 톤은 결과 화면에서 **정답/오답 칩으로도 쓰이고 있어**
> (`participant/result/question-detail-page.tsx:91`) 색을 바꾸면 거기까지 번진다.
> 그래서 이번엔 공용 토큰을 건드리지 않고 프로필 화면만 고쳤다. 답이 오면 한 번에 정리한다.

### ✅ G-1 AI 무료 생성 정책 — **백엔드 소스로 답이 났다(시안 문구가 틀렸다)**

§4-2 G-1의 세 질문 중 1번이 확정됐다.

- `application.yml:65` → `ai-free-limit: 5   # (호스트, 누적 — 명세 "최초 5회 무료")`
- `AiQuestionService.successCount`는 **성공 호출을 날짜 필터 없이 누적으로 센다**

즉 **계정당 1회성 5회이고 매일 리셋되지 않는다.** 따라서 `04` 보드 B 카드의

> "**오늘** 무료 생성을 다 썼어요 / **내일** 다시 5회가 채워져요"

는 구현과 어긋난다. **"무료 생성 5회를 모두 썼어요"처럼 리셋을 약속하지 않는 문구로 바꿔 주세요.**
코드는 이미 "최초 5회는 무료예요"로 적고 있다(`host/editor/generate-panel.tsx:171`).

> 남은 두 질문(코인제 대체 여부 · 잔여 횟수 표시)은 그대로다. 잔여 횟수는 백엔드에 계산 함수가
> 이미 있는데 응답에 안 실려 있다 — `QA_BACKLOG.md` B-7.

## 0-2. 갱신 이력 (2026-09-02) — 백엔드 확정 계약 51개 대조

> 근거는 전부 백엔드 코드다(`Passmate-Backend@develop 5f433d2`). 대조표: `specs/001-passmate-mvp/contracts/rest-api.md`.

### ✅ 해소

| 항목                                   | 결과                                                                                                                              | 근거                             |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| **G-7** 커서 페이지에 전체 개수가 없다 | **커서가 아니라 오프셋 페이지였다.** `PageResponse<T>`에 `totalElements`·`totalPages`가 있다 — 홈·목록의 개수를 정확히 셀 수 있다 | `common/dto/PageResponse.kt`     |
| **G-4 (d)** 공개 방 "오늘" 필터        | `GET /rooms/public`에 **`today` 파라미터가 있다** — 칩을 되살렸다                                                                 | `room/dto/PublicRoomRequests.kt` |
| **D-6** 호스트용 방 상세(연결된 세트)  | `GET /rooms/{roomId}`가 `questionSetId`·`screenLocked`·`currentQuestionNo`까지 준다                                               | `RoomController`                 |
| **D-17** 문항별 보기 분포              | `QUESTION_ENDED` 페이로드와 `GET …/questions/{id}/result`의 `distribution`(키=보기 원문)                                          | `SessionEvents.kt`               |
| 강퇴                                   | `DELETE /rooms/{roomId}/participants/{participantId}` 있음 — 대기실에 붙였다                                                      | `ParticipantController`          |

### 🔴 새로 생긴 공백 (v2 계약을 보고 알게 된 것)

| #       | 무엇                                                                                                                              | 화면에 미치는 영향                                                                                        | 지금 처리                                                                                                                                                    |
| ------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **N-1** | **공개 방 목록·참여한 방 목록에 PIN이 없다**                                                                                      | 카드에서 방으로 바로 못 들어간다. "다시 들어가기"가 성립하지 않는다                                       | 버튼을 PIN 입력 화면(`/join`)으로 보낸다. 서버가 PIN을 주거나, 시안에서 그 버튼을 빼야 한다 — **디자이너 + 백엔드**                                          |
| **N-2** | **`GET /rooms/pin/{pin}`에 호스트·문항 수·일정이 없다**                                                                           | 유료 방 결제 화면의 "김민지 선생님 · Lv.3 · 별점 4.5 · 8문항"을 그릴 근거가 없다                          | 값이 없으면 그 블록을 통째로 감춘다                                                                                                                          |
| **N-3** | **참가자별 제출 여부가 없다**                                                                                                     | W-05 우측 레일의 "제출함/풀이 중" 개인 표시가 불가능하다. 집계(n/m·정답률·보기 분포)만 온다               | 개인 표시를 끄고 숫자로만 보여준다 — **디자이너 확인**                                                                                                       |
| **N-4** | **참여한 방에 진행률(3/8)이 없다**                                                                                                | "3/8 문항 진행 중" 문구를 만들 수 없다                                                                    | 문구에서 뺐다                                                                                                                                                |
| **N-5** | **세션 결과에 호스트 이름이 없다**(G-8과 같은 뿌리)                                                                               | 별점 시트의 "김선생 선생님" 줄                                                                            | 줄을 감춘다                                                                                                                                                  |
| **N-6** | **게스트 기록 이관 문구가 계약보다 앞서 있다** — 랜딩 FAQ 첫 답변이 "그동안 푼 방과 점수가 그대로 계정에 들어옵니다"라고 약속한다 | `POST /guest-records/claim`은 **백엔드 미구현**(실서버 404)이다. `guestToken`은 발급되는데 받는 곳이 없다 | 웹은 `guestToken`을 로컬에 7일 보관하고 `@draft`로 호출만 준비해 뒀다. 문구를 "나중에 옮길 수 있어요"로 낮출지 — **기획 + 디자이너**, 구현 일정은 **백엔드** |
| **N-7** | **앱 보드 "공통 패턴" 2장이 웹에도 적용되는지 모른다**                                                                            | 로딩·빈 상태·오류 같은 공통 화면을 웹이 각자 만들고 있다                                                  | 앱과 같은 패턴을 웹에 그대로 쓰면 되는지, 웹용 변형이 따로 있는지 — **디자이너**                                                                             |

### ⏳ 백엔드 회신 대기 (문안은 `specs/001-passmate-mvp/research.md` §B)

B-1 대기실 입·퇴장 이벤트 미발행(웹은 3초 폴링으로 대신) · B-2 운영 WS 허용 도메인 불일치(`passmate.app` vs `passmate.kr`) · B-3 시각 직렬화(오프셋 없는 `LocalDateTime`) · B-4 끝난 방 PIN도 404(410 구분 요청) · B-5 기본 아바타 `"default"` · B-6 게스트 토큰 1시간·refresh 없음 · B-7 `aiAnalysisEnabled` 폐기 여부 · B-8 AI 생성 잔여 무료 횟수 응답 없음(G-1) · B-9 웹 배포 방식(nginx 정적 vs Node).

---

## 0-1. 갱신 이력 (2026-09-01)

### ✅ 해소 — 더 이상 묻지 않아도 되는 것

| 항목                                 | 결과                                                                                                        | 근거                                                                                    |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| **C-1** 로그인 방식                  | **Google 단일로 확정.** 이메일 폼·비밀번호 변경 화면 제거 완료                                              | 명세 v2 우선순위 6 "(보류) — Google 로그인으로 대체" · 커밋 `c9504f1`·`a038f2d` (45→44) |
| **C-6** 캐릭터(아바타)               | **문자열 키로 확정** (숫자 ID 아님)                                                                         | ERD `avatar_id varchar(30)` · 커밋 `3deead4`                                            |
| **C-10** 관리자 판별                 | `role` 대신 **`isAdmin: boolean`**. 가드는 `adminOnly`                                                      | ERD `user` 주석 "역할 컬럼 없음" + `is_admin` · 커밋 `187ce7d`                          |
| **D-1** 웹 로그인 콜백               | 리다이렉트가 아니라 **토큰 교환**으로 바뀜 — `POST /auth/login/{provider}` + `{idToken\|authorizationCode}` | 백엔드 구현체 · 커밋 `187ce7d`. **D-14(쿼리스트링 노출) 우려도 함께 해소됐다**          |
| **D-2** 에디터 계약                  | 경로 확정. AI 생성이 **2단계**로 — `POST /question-sets/{setId}/questions/generate`                         | 명세 v2 · 커밋 `21d9db1`                                                                |
| **D-4** 세트 복제                    | `POST /question-sets/{setId}/duplicate` (clone 아님)                                                        | 명세 v2 · 커밋 `21d9db1`                                                                |
| **D-5** 강의자료 출제                | `POST /question-sets/{setId}/questions/generate-from-file`                                                  | 명세 v2 · 커밋 `21d9db1`                                                                |
| **D-7** 첨삭 저장                    | `PUT /rooms/{roomId}/answers/{answerId}/review` · 답안 목록은 `GET /rooms/{roomId}/answers`                 | 명세 v2 · 커밋 `2851596`                                                                |
| **D-8** 리포트 내보내기              | `GET /rooms/{roomId}/reports/export`                                                                        | 명세 v2 · 커밋 `2851596`                                                                |
| **D-9** `role`·`userId`              | `role`은 `isAdmin`으로 대체 (위 C-10)                                                                       | 커밋 `187ce7d`                                                                          |
| **D-11** 비밀번호 변경·이메일 로그인 | **보류 확정 — 만들지 않는다**                                                                               | 명세 v2 우선순위 6, `PUT /users/me/password` 보류                                       |
| PTT 거취                             | **존치**                                                                                                    | `POST·GET /rooms/{roomId}/session/hints` 우선순위 2                                     |
| W-01 홈 2카드 섹션                   | **복원이 맞다**                                                                                             | `GET /users/me/rooms/joined` 설명에 "홈 '최근 참여한 방'에도 사용" 명시                 |
| 문항 구성(W-08 우 패널)              | **계약 있음** — `GET /question-sets/{setId}`가 문항 목록 반환                                               | 명세 v2                                                                                 |

### 🎨 시안이 도착한 것 (v6, 9/1 16:39)

우리가 §2에서 요청했던 화면들이 왔다. **상세 대조는 `DESIGN_DIFF_V6.md`에 있다.**

| 요청                              | v6 시안                                                                |
| --------------------------------- | ---------------------------------------------------------------------- |
| **A-5** 공개 방 목록 `/rooms`     | `P-Web 공개 방 목록` — 검색·필터 4칩·카드 6장·더 보기 (문구 판독 완료) |
| **A-6** 로그인 콜백 처리 중·실패  | `C-01a 로그인 처리 중` · `C-01b 로그인 실패` (문구 판독 완료)          |
| **A-8** 신고 다이얼로그           | `P-Web — 신고 다이얼로그` (**프레임만 확인, 문구 미판독**)             |
| **A-1** 학생 결과·리포트          | `P-Web 리포트 — 문항 상세` · `P-Web 별점 시트` (**문구 미판독**)       |
| **§3 B표** 공통 로딩·에러·빈 상태 | **`04 상태·오류` 보드 15장 + `07 로딩·스켈레톤` 보드 13장 신설**       |
| **§3 B표** W-05 호스트 연결 끊김  | `W-05e 프로젝터 연결 끊김`                                             |
| **§3 B표** W-11 결제 실패·재시도  | `W-11e 결제 실패`                                                      |

### ⏳ 여전히 회신 대기 (백엔드)

`HANDOVER.md`의 대기 목록 그대로다. **이게 가장 큰 병목이다.**

1. **미구현 22개 엔드포인트의 요청·응답 필드** — 명세 CSV에 경로·메서드만 있고 필드가 없다. 노션 하위 페이지에도 없음을 확인했다. 대상: 문항 CRUD · 방 상세/수정/종료/QR · 닉네임 중복확인 · 랭킹 조회 · 문항 결과 · 누적 학습 리포트 · 학생별 결과 상세 · 내 답안 AI 피드백 등
   → **스웨거를 올려주기로 함**(9/1 사용자 확인). 별도 스키마 문서는 안 온다
2. **FR-064~075 기능명세** — 백엔드가 FR-075(서술형 AI 분석 코인화)를 구현했는데 그 번호대 문서가 없다. `../docs/spec.md`는 FR-001~060까지
3. **Google 로그인 설정** — GIS 클라이언트 ID, `idToken` 방식인지 `authorizationCode` 방식인지
4. **화면 잠금 거취** — 명세 우선순위 2에 `PUT /rooms/{roomId}/session/lock`이 살아 있는데 팀 결정은 "W-05에서 제거"였다. 모순 상태

## 1. 지금 시안에 있는 화면 (참고)

`nZTTY` 보드 33프레임 + 랜딩 1. 코드 라우트와 1:1로 대응한다.

| 구역      | 시안 프레임                                                                                                                                                                                     | 코드 라우트                                                                                                                     |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 공통      | C-01 v2 로그인 `b6JNW` · C-03 게스트 입장 `XYhiR` · L-01 랜딩 `MGeTr`                                                                                                                           | `/login` `/join` `/`                                                                                                            |
| 홈        | W-01 v6 홈 `sPzaR` (+새 방 모달 `uG32o`, 빈 상태 `xI3fy`)                                                                                                                                       | `/home`                                                                                                                         |
| 개설·운영 | W-02 v2 방 설정 `D0Y0Yi` · W-03 에디터 `IwAFJ` · W-04 대기실 `gCEm3` · W-05 진행 `aCIne` · W-06 문항 결과 `uiWx4` · W-07 방 리포트 `xu88g` · W-08 문제 세트 `Ytlzh` · W-09 내가 만든 방 `voPdY` | `/host/rooms/new` `/host/editor` `/host/rooms/[code]/lobby·live·result` `/host/sessions/[id]/review` `/host/sets` `/host/rooms` |
| 참여      | W-11 결제 3장 `ADdRk` `r1wqq` `OYjYo` · P-Web 풀이 `nwR8C`                                                                                                                                      | `/pay/[code]` `/play/[code]`                                                                                                    |
| 회원      | C-02 v3 마이페이지 `P9QblI` + 서브 12장(C-02-1~12) · W-13 참여한 방 `mX5Jn` · W-10 정산 `EU3G0`                                                                                                 | `/me/*` `/me/joined` `/me/settlement`                                                                                           |

코드에서 **자리표시자(구현 예정)로만 있는 라우트**: `/rooms`(공개 방 목록), `/result/[sessionId]`(학생 결과) — 둘 다 시안이 없어서다 (→ A-5, A-1).

## 2. A. 화면 자체가 없음 — 신규 프레임 요청

### A-1. 학생 결과·리포트 (웹) — `/result/[sessionId]` · **P2** (US5·US4·US7·US12·US8)

> 🎨 **v6에서 부분 도착** — `P-Web 리포트 — 문항 상세` · `P-Web 별점 시트 (웹)` 두 프레임이 생겼다. **아직 문구를 판독하지 않았으니** 참여자 흐름에 착수하기 전에 읽을 것. 아래 요구 목록을 다 덮는지는 미확인이다.

가장 큰 구멍이다. 세션이 끝나면 학생이 보는 화면인데 웹 시안이 없다. 앱(KMP)에는 `SessionResultResponse`·`LearningReportResponse` 기준으로 화면이 있으니 **앱 시안을 데스크톱 폭으로 옮기는 작업**에 가깝다.

들어가야 할 것 (FR 근거):

- 내 점수·최종 순위, 전체 참가자 수 (FR-026)
- 문제별 카드: 정오, 내 답, 정답·해설, 획득 점수(속도 보너스 표기) (FR-030)
- 서술형 문항: **AI 피드백 3상태** — 분석 중(대기) / 완료(핵심 포함 여부·부족한 부분·개선 방향) / 분석 불가 안내 (FR-027·029)
- **선생님 첨삭**을 AI 피드백과 구분해 표시, 보정된 최종 점수 (FR-035)
- 취약 주제(개념) 요약 (FR-030)
- **별점 1~5 + 태그 5종 + 선택 코멘트** 입력 — 계약 `POST /rooms/{roomId}/ratings {stars, tags[], comment?}`, 태그는 `CLEAR_EXPLANATION`(설명이 명확해요)·`GOOD_DIFFICULTY`(난이도 적절)·`GOOD_PACING`(진행 속도 좋아요)·`HELPFUL_HINTS`(힌트가 도움됐어요)·`GOOD_QUALITY`(문제 퀄리티) 고정. 제출 후 잠금, 이미 평가함(409), **평가 기간 24h 경과**(410) 상태, "평가 없이 나가기" (FR-042~043)
- **게스트 전용 CTA** "가입하고 기록 저장하기" + 보관 기한 7일 안내 (FR-036)
- 리포트 내보내기 버튼 (FR-063, `GET /participations/{id}/report/export`)
- 회원이면 "마이페이지 › 참여한 방에 저장됐어요" 안내 (FR-032)
- 호스트·문제 **신고** 진입(→ A-8), 호스트 이름 → 공개 프로필(→ A-7)

### A-2. 학생 웹 대기실 — P-Web 대기 상태 · **P1** (US2)

`/join`(C-03)에서 입장한 뒤 세션 시작 전까지 학생이 보는 화면. P-Web 프레임(`nwR8C`)에는 풀이 상태만 있고 메모("앱과 동일한 컴포넌트·상태 — 반응형으로 폭만 조정")만 남아 있다. 앱 시안이 있으면 그것으로 갈음해도 되지만, **웹 폭에서의 배치 1장**은 필요하다.

- 방 제목·호스트(등급·별점, FR-044·048)·PIN, 참가자 목록 실시간 갱신 (FR-006)
- 회원이면 "풀이 기록이 계정에 연동돼요" 배지 (US2-2)
- 유료 방이면 결제 완료 표시
- "선생님이 시작하면 자동으로 넘어가요" 안내, 연결 상태 표시

### A-3. 호스트 최종 결과 (프로젝터) · **P1** (US3, FR-026)

W-06(`uiWx4`)은 **문항별** 결과다. 마지막 문항 뒤 "최종 결과와 리포트가 열려요" 문구만 있고 최종 화면이 없다. 코드 `result-page.tsx`도 같은 문구로 끝난다.

- 최종 랭킹(포디움 1~3위 + 전체 목록), 전체 정답률, 세션 소요 시간
- 서술형이 있으면 "AI 분석 n건 진행 중 — 리포트에서 확정돼요" 상태
- 버튼: 리포트 보기(W-07) · 방 닫기 · (선택) 같은 세트로 새 방

### A-4. 첨삭 입력 — W-07 확장 또는 W-07-1 · **P3** (US7, FR-034~035, T071)

W-07 방 리포트(`xu88g`)는 서술형 AI 분석을 **읽는** 화면이다. 코드 `review-page.tsx`에도 코멘트·점수 보정·개선사항 **입력**이 없다.

- 학생별 답변 목록 → 답변 상세 패널: AI 피드백(읽기) + 첨삭 폼(코멘트·개선사항·보정 점수) + 저장/저장됨 상태
- 첨삭 완료 표시(학생 n명 중 m명), 첨삭 후 최종 점수 우선 규칙 안내
- 문항별 음성 힌트 사용 이력 표기 (FR-041)

### A-5. 공개 방 목록 — `/rooms` · **P4** (US14 FR-054, US16)

> ✅ **v6에서 도착** — `P-Web 공개 방 목록` (`DESIGN_DIFF_V6.md` §4-6에 문구 전부). 검색 · 필터 4칩(전체·무료·유료·오늘) · 카드 6장 · "더 보기 ›".
> 시안이 아래 요구와 다른 점: **Lv.4+ 상단 섹션·브랜디드 배지는 없고**, 대신 "인기" 배지와 **\[알림 받기]**(시작 예정 방)가 생겼다. 알림 받기는 계약이 없다 → **G-4**.

routes에 등록돼 있고 코드는 자리표시자. 앱에는 `PublicRoomDto` 기준 `RoomListScreen`이 있다.

- 카드: 방 제목·주제·호스트(등급·별점)·참가비/무료·정원·시작 예정 — W-01 인기 방 카드 재사용 가능
- **Lv.4+ 호스트 방 상단 섹션**, **브랜디드 퀴즈 배지** (FR-054·059)
- 필터(무료/유료/주제), 빈 상태, 로그인 없이 볼 수 있음 → 유료 방 클릭 시 로그인 유도

### A-6. 로그인 콜백 처리 중 · 실패 — `/auth/callback` · **P1** (T019)

> ✅ **v6에서 도착** — `C-01a 로그인 처리 중` · `C-01b 로그인 실패`, 그리고 401은 별도로 `E-401 세션 만료`(04 보드)까지 왔다. 문구 전부 `DESIGN_DIFF_V6.md` §4-2·4-3·§2-2.
> 확정 문구: "로그인 중이에요 / 구글 계정을 확인하고 있어요. 잠시만요. / 보통 3초 안에 끝나요" · "로그인하지 못했어요 / 구글 계정 연결이 취소됐어요. 다시 시도하거나 PIN으로 바로 들어갈 수 있어요."
> **시안은 실패 CTA가 "홈으로"가 아니라 \[다시 시도] + \[PIN으로 게스트 입장]이다.** Google 로그인 배선할 때 같이 넣을 것.
> 단, `E-401`이 "작성 중이던 내용은 임시 저장돼 있어요"를 약속한다 → **G-2**.

Google 로그인 후 되돌아오는 1~2초 화면과 실패 상태. 작지만 없으면 빈 화면이 뜬다.

- 로딩("로그인 처리 중…") / 실패("로그인에 실패했어요" + 다시 시도·홈으로) / 세션 만료로 재로그인 안내(401)

### A-7. 호스트 공개 프로필 · **P3~P4** (FR-044·048, 계약 `GET /users/{userId}/profile`)

앱에는 있고 웹에는 없다. 공개 방 목록(A-5)·결제 화면(W-11)·대기실의 호스트 이름을 누르면 열리는 화면.

- 닉네임·소개·등급 엠블럼·평균 별점·평가 수·방 운영 횟수·누적 학생 수
- 획득 뱃지(8종: 첫 방·방 10회·학생 100명·별점 4.5·평가 50건·30일 연속·첫 유료 방·AI 세트 50개 — `lMoA1` v5 뱃지 재사용)
- 이 호스트의 공개 방 목록(A-5 카드 재사용), 신고(A-8) 진입
- 모달/사이드 패널 1장이면 충분

### A-8. 신고 다이얼로그 · **P3** (계약 `POST /reports`, 관리자 A-04가 소비)

> 🎨 **v6에서 도착** — `P-Web — 신고 다이얼로그` 프레임이 생겼다. **아직 문구를 판독하지 않았다.** 사유 6종이 서버 고정값과 맞는지 대조해야 한다. 코드 쪽 호출은 이미 있다 — `src/lib/api/me.ts:53` `POST /reports`.

관리자 A-04 신고 관리는 있는데 **신고를 넣는 쪽 UI가 웹에 없다**. 사유 6종은 서버 고정값: `NICKNAME`(부적절 닉네임)·`QUESTION_ERROR`(문제 오류)·`PAID_ROOM`(유료 방 문제)·`OPERATION`(운영)·`SPAM`(도배)·`DIFFICULTY`(난이도).

- 대상(호스트/문제/참가자) 표시 → 사유 라디오 6종 → 상세(선택) → 접수 완료
- 진입점: 결과 화면(A-1) 호스트·문항, 호스트 대기실/진행(W-04·05) 참가자 메뉴, 호스트 프로필(A-7)
- 게스트도 익명 신고 가능

## 3. B. 화면은 있으나 상태·변형이 없음

기획이 요구하는 **오류·빈·한도·실패 상태**가 시안(과 코드)에 없다. 화면별로 묶었다. ★는 P1.

> ✅ **이 표의 상당수가 v6로 해소됐다.** 오류·빈 상태·로딩이 `04 상태·오류 — 웹`(15장)과 `07 로딩·스켈레톤`(13장) 두 보드로 통째로 왔다. 특히 표 맨 아래 "공통 — 로딩·에러·빈 상태 컴포넌트, 시안 없음" 행은 완전히 채워졌다(스켈레톤 색·모서리·반짝임 값까지 확정).
> 아직 안 온 것: **W-05 화면 잠금·문항 마감**(거취 자체가 미결), **W-03 자료 업로드**, **W-09 등급 이력**, **W-13 게스트 기록 연동**, **W-10 이월·차감**.
> 화면별 대응은 `DESIGN_DIFF_V6.md` §2·§3 참고.

| 화면                           | 없는 상태·변형                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 근거                                |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| **C-03 게스트 입장** `XYhiR` ★ | 잘못된 PIN(404) · 종료된 방(410) · 닉네임 중복(409 `NICKNAME_TAKEN`) 인라인 오류 3종 / **유료 방 PIN → 로그인·가입 유도**(401 `LOGIN_REQUIRED`) / 진행 중 방 늦은 입장 안내("현재 문항부터 참여, 지난 문항은 미응답")                                                                                                                                                                                                                                                                           | US2-4·5, FR-050, FR-024             |
| **W-01 홈 PIN 입장** `sPzaR` ★ | 위와 같은 오류 3종 (회원 버전)                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | US2-4·5                             |
| **W-03 문제 에디터** `IwAFJ` ★ | **잔여 무료 생성 횟수 상시 표시**(FR-061) / **한도 소진**(403 `FREE_QUOTA_EXCEEDED`: "추후 코인 결제 예정" + 직접 작성·세트 복제 경로 안내) / **생성 실패**(502 `AI_GENERATION_FAILED`: 오류 안내 + 재요청 버튼 — 코드는 "실패하면 자동 재시도 1번" 문구만) / 생성 중 진행(≈30초) 상태 / **강의자료 업로드**(텍스트·PDF 첨부, 추출 중, 용량·형식 오류) / 문항별 **배점·제한시간 편집 컨트롤**(코드는 표시만) / 문항 재생성 중 상태                                                              | FR-009~~016, FR-061~~062, T031·T089 |
| **W-02 방 설정** `D0Y0Yi`      | **공개 방 목록 노출 토글**(`isListed`, FR-054) / **주제·시작 예정 시각·정원 입력**(계약 `topic`·`scheduledAt`·`maxParticipants` — W-11 결제 화면은 이 값을 보여주는데 입력하는 곳이 없다) / 참가비 상·하한 초과 오류 / 서버 거부(403 `HOST_LEVEL_REQUIRED`) 토스트 / 세트 미선택·미확정(DRAFT) 경고                                                                                                                                                                                             | FR-049·054, T091                    |
| **W-04 대기실** `gCEm3` ★      | 세트 미확정이라 시작 불가(409) 안내 / **서술형 세트인데 무료 분석 한도 소진 → "이번 세션은 AI 분석 없이 진행"** 배지(`aiAnalysisEnabled=false`, T055) / 유료 방: 결제 완료 인원 / 참가자 0명 빈 상태 / QR 실제 렌더(코드는 회색 박스)                                                                                                                                                                                                                                                           | FR-004, FR-061~062                  |
| **W-05 진행** `aCIne` ★        | **호스트 연결 끊김·재연결 중 오버레이**(세션 유지, 제어권 복구) / PTT 3상태: 녹음 중·송출 중·실패(마이크 권한 거부) / **진행 중 이탈 확인 다이얼로그** / **학생 화면 잠금 토글**(계약 `PUT …/session/lock` → `SCREEN_LOCKED`) / **"문항 마감" 버튼**(계약 `POST …/current/end` — 시간 전 수동 마감) / 서술형 문항 진행 변형(정답 없음, 잠정 배점 안내) / 늦은 입장자 표시 / 마지막 문항일 때 "세션 종료" 버튼 / 앱 리모컨 연결 표시(`PROJECTOR_CONNECTED` — 이 웹 화면이 프로젝터, 앱이 리모컨) | Edge case, FR-039, 규칙 §2-1        |
| **P-Web 풀이** `nwR8C`         | **화면 잠금 오버레이**(`SCREEN_LOCKED`) / 제출 완료 대기·마감(410) 상태 / 재접속 중 배너 / 늦은 입장 안내 / 힌트 다시 듣기 목록                                                                                                                                                                                                                                                                                                                                                                 | FR-019·022·024·040                  |
| **W-06 문항 결과** `uiWx4`     | **서술형 문항 결과 변형**(선택 분포 대신 제출 n/총 n · "AI 분석 중 n건") / OX 문항 변형 / 마지막 문항 → "최종 결과 보기"(A-3 연결)                                                                                                                                                                                                                                                                                                                                                              | FR-025~026                          |
| **W-07 방 리포트** `xu88g`     | 첨삭 입력(→A-4) / AI 분석 실패 항목 표시 / 첨삭 확정 후 점수 보정 반영 표시 / 내보내기 진행·완료 / 리포트 생성 중(세션 직후) 상태                                                                                                                                                                                                                                                                                                                                                               | FR-029·031·035·063                  |
| **W-08 문제 세트** `Ytlzh`     | 빈 상태(첫 세트 만들기) / **DRAFT·확정 배지** / 복제 완료 → 편집 진입 흐름 / 삭제 확인                                                                                                                                                                                                                                                                                                                                                                                                          | FR-014, US9                         |
| **W-09 내가 만든 방** `voPdY`  | 빈 상태(첫 방 만들기) / **유료 방 취소 → 전액 환불 확인 다이얼로그**(FR-052) / 진행 중 세션 중단(ABORTED, 환불·수익 미적립 경고) / 등급 판정 이력(승급·하락 기록, FR-048)                                                                                                                                                                                                                                                                                                                       | FR-052, FR-047~048                  |
| **W-10 정산** `EU3G0`          | 정산 상태 **보류(HELD)·이월(최소 정산액 미만)** 배지 / 환불로 인한 **차감(REVERSAL)** 행 / 계좌 미등록 경고 배너(관리자 A-05엔 있음) / 빈 상태(유료 방 없음, Lv.3 미만)                                                                                                                                                                                                                                                                                                                         | FR-052·055~058                      |
| **W-11 유료 방 결제** `ADdRk`… | 결제 실패·취소 사유 + 재시도(코드에 텍스트만) / 이미 결제한 방 재입장 / 정원 초과 / 세션 이미 시작됨(늦은 입장 결제) / 게스트 접근 → 로그인 유도 / **잔액이 참가비 이상이라 충전이 필요 없는 경우**(부족분 0 — 충전 금액 프리셋·결제 수단·"₩N 충전" CTA가 모두 불필요하고 "N C 차감하고 입장"만 남는다. **9/4 발견**)                                                                                                                                                                           | FR-050~051                          |
| **W-13 참여한 방** `mX5Jn`     | 빈 상태 / **게스트 기록 연동 결과** — 연동 완료 배너 · 파기됨 안내(410 `RECORD_PURGED`) / 진행 중 세션 카드의 "다시 들어가기" 실패(종료됨)                                                                                                                                                                                                                                                                                                                                                      | FR-036~037                          |
| **C-02 마이페이지** `P9QblI`   | 프로필 카드에 **평균 별점·평가 수**(FR-044) / 코인 내역(C-02-9)에 **환불(`REFUND`) 유형**(FR-053) / 탈퇴 거부 상태(409: 정산 미지급·진행 중 방 있음) / 세션 만료·로그아웃 완료 토스트                                                                                                                                                                                                                                                                                                           | FR-044·053                          |
| **공통**                       | 로딩·에러(재시도)·빈 상태 컴포넌트 — 코드엔 `ScreenLoading`·`ScreenError`가 있으나 시안 없음 / 토스트 스타일 / 다이얼로그(확인·경고) 기본형                                                                                                                                                                                                                                                                                                                                                     | 규칙 §10                            |

## 4. C. 기획–시안–계약 충돌 — 결정이 필요한 것

시안이 기획(spec/tasks)이나 계약(KMP `shared` DTO, 2026-08-28 백엔드 명세 기준)과 어긋나는 곳. 디자이너 혼자 결정할 수 없어 **기획·백엔드와 함께** 정해야 한다.

> ✅ **해소: C-1**(Google 단일 확정, 코드 반영 완료) · **C-6**(아바타 문자열 키) · **C-10**(`isAdmin`). 근거는 §0-1.
> ⏳ **미해소: C-2**(에디터 — 경로는 확정됐으나 문항 CRUD 필드는 스웨거 대기) · **C-3**(서술형 첨삭 — 경로만 확정) · **C-4**·**C-5**·**C-7**·**C-8**·**C-9**.
> 🆕 **v6가 새 충돌 6건을 추가했다 → §4-2 G 항목.** 이 표의 계약 기준은 8/28 KMP DTO라 낡았다는 점도 감안할 것.

| #    | 항목                                  | 기획(spec)                      | 계약(KMP)                                                                                                            | 시안·코드                                                                                     | 권장                                                                                                        |
| ---- | ------------------------------------- | ------------------------------- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| C-1  | **로그인 방식**                       | 소셜(Google·Kakao) 전용 FR-001  | Google OAuth만, `client=mobile` 딥링크. 이메일·비밀번호·회원가입·Kakao 없음                                          | C-01 v2에 이메일+비밀번호 로그인·회원가입·비밀번호 찾기, C-02-2 비밀번호 변경                 | Google 단일로 정리(이메일·비밀번호·C-02-2 제거) + 백엔드에 웹 콜백(`client=web`) 요청. Kakao는 계약 추가 후 |
| C-2  | **문제 에디터·문제 세트** (W-03·W-08) | US1 P1 — AI 생성·검토·확정·복제 | `GET /question-sets` 목록뿐. 생성·문항 CRUD·확정·AI 생성·복제·자료 업로드 **전무**                                   | 화면은 완성                                                                                   | 백엔드에 T027·T028·T076·T087 우선 구현 요청(웹 P1인데 앱에 없어 빠진 듯). 웹은 tasks.md 경로로 초안 연결    |
| C-3  | **W-07 서술형 분석·첨삭**             | US7 첨삭·점수 보정              | 방 전체 집계(`/rooms/{id}/results`)만. 학생별 서술 답안·AI 피드백 목록·**첨삭 저장 API 없음**                        | W-07에 AI 분석 패널 있음, 첨삭 입력 없음(A-4)                                                 | 백엔드에 답안 목록 조회 + `POST /answers/{id}/review`(T070) 요청                                            |
| C-4  | **결제 수단 관리** (C-02-8)           | FR-053 결제 내역 조회만         | `PUT /users/me/payment-method {method}` 기본 수단 1개(카카오·네이버·토스·카드·계좌이체). 카드 등록·삭제(빌링키) 없음 | 카드 목록·기본 변경·삭제·추가                                                                 | 화면을 "기본 결제 수단 선택" 1단으로 축소                                                                   |
| C-5  | **알림 설정** (C-02-10)               | —                               | 3종만: 세션 시작·별점 요청·정산 완료                                                                                 | 4종(마케팅 포함)                                                                              | 마케팅 토글 제거 또는 계약 추가                                                                             |
| C-6  | **캐릭터(아바타)**                    | 없음(닉네임만)                  | `avatarId: number`(범위 불명)                                                                                        | 12종 키(fox·cat…)                                                                             | 12종 ↔ 1~12 ID 표 확정, 백엔드 범위 검증 합의                                                               |
| C-7  | **정산 상태·수치** (W-10·C-02 v3)     | 대기·완료·보류·**이월** FR-057  | `SCHEDULED`·`PAID`·`HELD`, 이월 없음                                                                                 | scheduled·paid 2종. 이번 달 정산 예정 ₩64,000(C-02 v3) vs ₩384,000(W-10), W-10 표 합계 불일치 | 이월 표현(다음 회차 이월 안내) 추가, 시안 숫자 통일                                                         |
| C-8  | **유료 방 취소·강퇴**                 | FR-052 취소 시 전액 환불        | `ROOM_CANCELLED`·`PARTICIPANT_LEFT(KICKED)` 이벤트만 있고 **호출 API 없음**                                          | 둘 다 UI 없음                                                                                 | 백엔드에 방 취소·강퇴 API 요청 후 W-09·W-04에 추가                                                          |
| C-9  | **학생 웹(P-Web) 범위**               | 학생은 앱, 웹은 보조            | 학생 REST·STOMP 전부 있음(웹 콜백만 없음)                                                                            | `/join`·`/play` 있음, `/result` 없음                                                          | MVP에 넣을지 결정 → 넣으면 A-1·A-2 필수                                                                     |
| C-10 | **관리자 판별**                       | `/admin/*` ADMIN 롤             | `GET /users/me`에 `role` 없음                                                                                        | 가드가 `profile.role` 사용                                                                    | 백엔드에 `role` 필드 요청 (팀원 B 공유)                                                                     |

## 4-1. D. 백엔드에 요청할 계약 추가 (웹이 필요, KMP에 없음)

`../docs/tasks.md`에 이미 있는 백엔드 태스크는 번호를 적었다. 웹은 이 경로로 초안(`@draft`) 연결해 두고 계약이 오면 맞춘다.

> ✅ **해소: D-1 · D-2 · D-4 · D-5 · D-7 · D-8 · D-9 · D-11 · D-14** — 명세 v2와 백엔드 구현으로 경로가 확정됐고 커밋 8건으로 코드에 반영됐다. 확정 경로는 §0-1 표와 `HANDOVER.md`의 "경로 9건" 블록에 있다.
> ❗ **D-3 `GET /me/ai-usage`는 "해소"가 아니라 "삭제"다.** 명세 v2에서 엔드포인트 자체가 사라졌는데 "무료 5회"는 남아 있어, 지금 에디터의 사용량 표시를 **감춰 둔 상태**다. v6 시안이 이 자리에 "오늘/내일 5회" 문구를 넣으면서 모순이 커졌다 → **G-1**.
> ⏳ **미해소: D-6 · D-10 · D-12 · D-13 · D-15 · D-16 · D-17 · D-18 · D-19** — 스웨거 대기.
> 🆕 v6가 요구하는 새 계약은 **§4-2 G-3 · G-4**에 있다.

| #    | 호출                                                                                                                                                | 용도                                                                                                                        | tasks     |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------- |
| D-1  | `GET /auth/oauth/google?client=web` → `/auth/callback?accessToken&refreshToken`                                                                     | 웹 로그인                                                                                                                   | T012·T019 |
| D-2  | `POST /question-sets/generate` · `POST /question-sets` · `GET/PATCH /question-sets/{id}` · 문항 추가/수정/삭제 · `POST /question-sets/{id}/confirm` | W-03 에디터                                                                                                                 | T027      |
| D-3  | `GET /me/ai-usage`                                                                                                                                  | 잔여 무료 횟수(FR-061)                                                                                                      | T028      |
| D-4  | `POST /question-sets/{id}/clone`                                                                                                                    | W-08 복제                                                                                                                   | T076      |
| D-5  | `POST /materials` (multipart)                                                                                                                       | 강의자료 출제                                                                                                               | T087      |
| D-6  | `GET /rooms/{roomId}` (호스트용 상세, 세트·설정 포함) · 방 취소 · 강퇴                                                                              | W-09·W-04                                                                                                                   | —         |
| D-7  | 서술형 답안 목록(학생별·AI 피드백 포함) · `POST /answers/{id}/review`                                                                               | W-07·A-4                                                                                                                    | T070      |
| D-8  | `GET /sessions/{id}/stats/export` · `GET /participations/{id}/report/export`                                                                        | 내보내기                                                                                                                    | T060      |
| D-9  | `GET /users/me`에 `userId`·`role`                                                                                                                   | 호스트 판별·관리자 가드                                                                                                     | —         |
| D-10 | 세션 종료 시 최종 결과(전체 랭킹·정답률) — `SESSION_ENDED.finalRanking`으로 충분한지 확인                                                           | A-3                                                                                                                         | —         |
| D-11 | 비밀번호 변경·이메일 로그인 (C-1 결정에 따라)                                                                                                       | C-01·C-02-2                                                                                                                 | —         |
| D-12 | 알림 마케팅 토글 (C-5 결정에 따라)                                                                                                                  | C-02-10                                                                                                                     | —         |
| D-13 | **참가비 결제 멱등성** — `POST /rooms/{roomId}/entry-payments`를 같은 방에 두 번 불러도 한 번만 차감되게(또는 이미 결제 시 409 `ALREADY_PAID`)      | 결제 후 입장 실패 → 재시도 시 이중 차감 방지. 지금은 프론트가 sessionStorage로 막고 있어 다른 기기·브라우저에서는 못 막는다 | —         |
| D-14 | **로그인 토큰 전달 방식** — `/auth/callback?accessToken=…&refreshToken=…`(쿼리스트링) 대신 프래그먼트(`#accessToken=…`)나 1회용 교환 코드           | 쿼리스트링은 프록시 로그·브라우저 기록·`Referer`에 남는다. 백엔드가 아직 안 만들었으니 지금 정하면 공짜                     | T012·T019 |
| D-15 | 문항별 **자동 넘김** 여부 — `QuestionDraft`에 `timeLimitSec`은 있지만 자동 넘김 필드가 없다                                                         | W-02b 문항별 시간 설정                                                                                                      | —         |
| D-16 | 세션 **진행 시간** — `RoomReportSummary`에 없다. 없으면 세션 시작·종료 타임스탬프라도                                                               | W-12 최종 순위 요약 레일                                                                                                    | —         |
| D-17 | **문항별 오답자 목록** — 누가 틀렸는지, 그 학생이 고른 보기가 무엇인지                                                                              | W-06 "이 문항을 틀린 학생"                                                                                                  | —         |
| D-18 | **진행 중 문항별 누적 정답률** — 지금은 방금 끝난 문항 하나만 알 수 있어 미니 차트가 나머지를 "미진행"으로 그린다                                   | W-06·W-12 레일 미니 차트                                                                                                    | —         |
| D-19 | 호스트 공개 프로필의 **아바타** — `HostProfileResponse`에 `avatarId`가 없다. 지금은 이름 이니셜 타일로 대신한다                                     | M-10 선생님 프로필                                                                                                          | —         |

## 4-2. G. v6 시안이 새로 만든 질문 (2026-09-01)

시안 v6 전달본(9/1 16:39)이 **답을 준 만큼 새 질문도 만들었다.** 아래 6건은 그리기 전에 정해야 하거나, 계약이 없어 지금은 못 만드는 것이다. 상세 문구 인용은 `DESIGN_DIFF_V6.md`에 있다.

---

### G-1. AI 무료 생성 정책이 3자 모순 ★ — **기획 + 백엔드**

**(a) 시안** — `04` 보드 B 카드:

> "**오늘** 무료 생성을 다 썼어요"
> "**내일** 다시 5회가 채워져요. 지금은 직접 문항을 추가하거나 저장해 둔 세트를 재사용할 수 있어요."

**(b) 명세·백엔드**

- API 명세서 v2에서 **`GET /me/ai-usage`가 삭제됐다** (잔여 횟수를 줄 API가 없다)
- 백엔드는 **코인 지갑**을 구현했고 에러코드는 `INSUFFICIENT_COINS`다 (구 `FREE_QUOTA_EXCEEDED`는 이 코드로 통합됐다)
- 기존 기획(FR-061)은 **"최초 5회 무료"** — 계정당 1회성

**(c) 모순** — 셋이 전부 다르다.

| 출처   | 성격          | 리셋             |
| ------ | ------------- | ---------------- |
| 시안   | 일일 무료 5회 | **매일**         |
| 기획   | 최초 5회 무료 | **없음** (1회성) |
| 백엔드 | 코인 차감     | 충전             |

**(d) 물어볼 것**

1. 무료 5회는 **일일 리셋인가, 계정당 1회성인가**? (기획)
2. 코인제로 완전히 대체됐다면 시안의 "오늘/내일 5회" 문구는 폐기하고 **코인 부족 문구**로 바꿔야 한다 (디자이너)
3. 어느 쪽이든 **잔여 횟수(또는 잔여 코인)를 주는 엔드포인트가 필요하다.** 지금은 API가 없어 에디터의 사용량 표시를 감춰 둔 상태다 (백엔드)

> 이게 정해지기 전에는 `04` 보드 B 카드 2번을 구현할 수 없다.

---

### G-2. E-401이 약속하는 "임시 저장"의 정체 — **기획 + 백엔드**

**(a) 시안** — `E-401 세션 만료` 화면 안 초록 배너:

> "**작성 중이던 내용은 임시 저장돼 있어요**"
> "문제 세트 · 방 설정 · 코멘트"

그리고 본문에 "다시 로그인하면 **보던 화면으로 그대로 돌아옵니다**".

**(b) 코드·계약** — 임시 저장이 **어디에도 없다.** `src/lib/api/client.ts`가 401에서 refresh를 1회 시도할 뿐이고, 실패 후 폼 상태를 보존하는 장치가 없다. 계약에도 초안 저장 엔드포인트가 없다.

**(c) 모순** — 사용자에게 **하지 않는 일을 했다고 말하는 화면**이다. 그대로 구현하면 배너만 뜨고 내용은 날아간다.

**(d) 물어볼 것**

1. **로컬 저장인가 서버 저장인가?** 로컬(`localStorage`)이면 계약 없이 우리가 만들 수 있다. 서버면 초안 저장 API가 필요하다 (기획 → 백엔드)
2. 범위가 정확히 **"문제 세트 · 방 설정 · 코멘트" 셋인가?** 서술형 답안(학생)은 왜 빠졌나
3. "보던 화면으로 그대로 돌아옵니다" — 재로그인 후 **원래 경로 복귀**를 뜻하는가? (이건 `returnTo` 쿼리로 우리가 구현 가능)

> 권장: **로컬 저장으로 좁히면 지금 만들 수 있다.** 서버 저장이면 스웨거 대기 목록에 추가해야 한다.

---

### G-3. 서술형 실시간·집계 계약 ★ — **백엔드**

v6가 `W-05 — 서술형 케이스` · `W-06 — 서술형 케이스` 두 장을 새로 그렸는데, 둘 다 **지금 계약으로는 채울 수 없는 데이터**를 요구한다.

**(a) 시안**

`W-05` 하단 패널 — "학생들이 작성하고 있어요 / **제출 전까지 답안 내용은 선생님 화면에 보이지 않아요**"

| 학생 | 표시                |
| ---- | ------------------- |
| 준영 | **작성 중 · 312자** |
| 채원 | 제출 완료           |
| 승현 | **작성 중 · 180자** |
| 유진 | 제출 완료           |
| 민지 | **작성 중 · 64자**  |
| 하늘 | 아직 시작 안 함     |

`W-06` — **모범답안** 전문 + "**채점 기준** — 1차 캐시 / 변경 감지 / 쓰기 지연 중 2개 이상 언급" + **AI 첨삭 요약 3분류**:

> "핵심 개념을 모두 담음 **3명**" · "일부만 언급 **2명**" · "핵심 누락 · 재학습 권장 **1명**"
> "핵심 누락 학생 — 하늘 — **이름을 누르면 답안 전문과 AI 첨삭을 볼 수 있어요**"

또 좌측 랭킹 레일에 **순위 변동 화살표**(▲2 · — · ▼1)가 붙었다.

**(b) 계약**

- STOMP 이벤트에 **타이핑 상태·글자 수가 없다.** 지금은 제출 여부만 안다
- 서술형 AI 첨삭은 아직 `@draft`다. **분류 체계(3단계)도, 모범답안·채점 기준 필드도 계약에 없다**
- 순위 변동은 **직전 문항 대비 순위**가 필요한데 없다 (기존 D-18과 같은 계열)

**(d) 물어볼 것**

1. 서술형 진행 중 **학생별 3상태(미시작 / 작성 중 + 글자 수 / 제출 완료)** 실시간 이벤트를 줄 수 있나? 글자 수를 실시간으로 보내면 트래픽이 커지는데 **주기(예: 5초)나 구간(50자 단위)으로 줄여도 되는지** 함께 판단 필요
2. 문항에 **모범답안·채점 기준** 필드가 있나? 있으면 이름이 무엇인가
3. AI 첨삭 결과의 **분류값이 3단계로 고정인가?** enum 이름과 값 목록 필요
4. 문항별 AI 첨삭 **집계**(분류별 인원수)를 서버가 주나, 아니면 학생별 결과를 받아 프론트가 세나?
5. 순위 변동(▲▼)을 위해 **직전 문항 순위**를 줄 수 있나?

> FR-064~~075 문서가 없어서 이 영역의 기획 의도 자체를 못 읽고 있다(§0-1 회신 대기 2번). 그 문서가 오면 1~~4가 한꺼번에 풀릴 수 있다.

---

### G-4. 계약에 없는 동작 4종 — **백엔드**

v6 시안에 버튼·문구는 있는데 부를 API가 없다. 넷 다 **그리기는 끝났고 계약만 없는** 상태다.

| #   | 시안                                       | 필요한 것                                 | 지금 상태                                                        |
| --- | ------------------------------------------ | ----------------------------------------- | ---------------------------------------------------------------- |
| a   | `W-06e` **\[시간 30초 더 주기]**           | 진행 중 문항의 **제한시간 연장** API      | 없음. `POST …/current/end`(수동 마감)는 있으나 연장은 없다       |
| b   | `W-10` 정산 계좌 "은행 조회 결과 "이한겷"" | **은행 예금주 실명 조회**                 | 없음. 계좌 등록만 있고 검증 결과를 돌려주는 필드가 없다          |
| c   | `E-500` 칩 **"예상 완료 · 오늘 14:30"**    | **점검 정보**(점검 여부 · 예상 완료 시각) | 없음. 헬스체크성 엔드포인트도 없다                               |
| d   | `P-Web 공개 방 목록` **\[알림 받기]**      | **시작 예정 방 알림 구독**                | 없음. `scheduledAt`은 `dto/rooms.ts`에 있어 시각 표시까지는 가능 |

> ✅ 같은 화면의 **"오늘" 칩은 해소됐다**(2026-09-02) — `GET /rooms/public`에 `today` 파라미터가 있어 되살렸다.

**물어볼 것** — 넷 다 "**만들 계획이 있는가, 아니면 시안에서 뺄 것인가**"를 먼저 정해야 한다. 특히 (c)는 서버가 죽었을 때 보여줄 화면이라 **점검 정보를 정적 파일이나 CDN에 두는 방법**도 있다(서버가 죽으면 API도 못 부르므로 오히려 그쪽이 맞을 수 있다).

---

### G-5. C-02-2 비밀번호 변경 화면이 시안에 잔존 — **디자이너**

**(a) 시안** — `C-02-2 비밀번호 변경` 프레임이 `01` 보드에 그대로 있다.

**(b) 명세** — 명세 v2 우선순위 6에서 `PUT /users/me/password`가 **보류**로 확정됐고, 로그인은 Google 단일이다.

**(c) 코드** — 이미 **화면과 라우트를 제거했다** (커밋 `a038f2d`, 라우트 45→44).

**(d) 물어볼 것** — 시안에서도 빼도 되는지 확인. **낡은 프레임으로 보이지만 우리가 임의로 판단할 일이 아니다.** 마이페이지(`C-02 v3`)의 "비밀번호" 행도 함께 지워야 한다.

---

### G-6. 미전달 문서 2건 — **디자이너**

`00 · 읽는 법` 보드의 "구현 전에 먼저 읽어 주세요" 카드 하단에 이렇게 적혀 있다.

> "문서 · **프론트_전달_v5/README.md** + **디자인_회신_20260901.md**"

**두 파일 모두 워크스페이스 어디에도 없다.** `find /Users/pocari/workspace -maxdepth 5`로 확인했고 `../docs/`에도 없다.

**물어볼 것**

1. 두 문서를 `../docs/`에 넣어 주실 수 있나?
2. 특히 `디자인_회신_20260901.md`는 **우리가 보낸 `패스메이트_웹_수정요청_20260901.pdf`에 대한 회신**으로 보인다. 어느 요청에 어떻게 답했는지 대조표가 그 안에 있으면 §0-1의 "시안이 도착한 것" 표를 추정이 아니라 확인으로 바꿀 수 있다
3. `README.md`가 말하는 **`assets/brand` SVG**와 **`tokens.css`**도 실물이 전달됐는지 확인 필요(색인 카드가 "정본"이라고 지목한 파일들이다)

---

### ~~G-7. 커서 페이지에 전체 개수가 없다~~ — ✅ **해소 (2026-09-02)**

> **커서가 아니라 오프셋 페이지였다.** 서버 `PageResponse<T>`가 `totalElements`·`totalPages`를 준다 —
> 홈·목록의 개수를 정확히 셀 수 있다. 아래는 기록으로 남긴 옛 내용이다.

**(a) 코드에서 드러난 것** — `/rooms` 공개 방 목록을 만들며 목에 커서 페이지(6개씩)를 넣었더니, **홈(W-01) 인기 방 섹션의 "운영 중인 방 12개"가 "6개"로 바뀌었다.** 그 숫자는 지금 응답 한 페이지의 `items.length`를 그대로 쓰고 있어서다.

**(b) 계약** — `CursorPage<T> = { items, nextCursor, hasNext }`. **전체 개수를 주는 필드가 없다.** 지금까지 목이 12개를 한 번에 돌려줘서 우연히 맞아 보였을 뿐, 실서버가 페이지를 끊으면 **홈의 숫자는 첫 페이지 크기가 된다.**

**(c) 물어볼 것**

1. `GET /rooms/public` 응답에 `total`(또는 `totalCount`)을 넣어 줄 수 있나? 목록형 응답 전반(`hosted`·`joined`·코인 내역 등)에 공통으로 필요하다
2. 못 준다면 홈 문구에서 개수를 빼야 한다 — **디자이너 확인 필요**(시안 W-01에 "운영 중인 방 N개"가 있다)

> 지금 코드는 시안 문구를 그대로 두고 한 페이지 기준으로 센다. 숫자가 틀릴 수 있다는 걸 알고 두는 상태다.

---

### G-8. 별점·신고의 라벨↔enum 대응이 확실하지 않다 — **백엔드 + 디자이너**

P-Web 별점 시트·신고 다이얼로그를 만들며 드러났다. **잘못 이어도 타입 검사에 안 걸리고, 관리자 화면에 엉뚱하게 분류된 데이터가 쌓인다.**

**(a) 별점 태그** — 시안 라벨 5개 ↔ `RatingTag` 5개. 앞의 셋은 뜻이 겹치지만 뒤의 둘은 추정이다.

| 시안 라벨            | 이은 enum           | 확실한가                               |
| -------------------- | ------------------- | -------------------------------------- |
| 설명이 쉬웠어요      | `CLEAR_EXPLANATION` | ○                                      |
| 문제가 실전 같았어요 | `GOOD_DIFFICULTY`   | ○                                      |
| 진행이 매끄러웠어요  | `GOOD_PACING`       | ○                                      |
| 피드백이 자세해요    | `HELPFUL_HINTS`     | **✕** — enum은 "힌트", 시안은 "피드백" |
| 분위기가 좋았어요    | `GOOD_QUALITY`      | **✕** — enum은 "품질", 시안은 "분위기" |

**(b) 신고 사유** — 시안 5개 ↔ `ReportReason` 6개(`NICKNAME`·`QUESTION_ERROR`·`PAID_ROOM`·`OPERATION`·`SPAM`·`DIFFICULTY`). 개수부터 안 맞는다.

| 시안 라벨                       | 이은 enum         | 확실한가                            |
| ------------------------------- | ----------------- | ----------------------------------- |
| 수업 내용이 설명과 달라요       | `SPAM`(허위 안내) | **✕**                               |
| 문제가 부적절해요               | `QUESTION_ERROR`  | ○                                   |
| 선생님이 불쾌하게 했어요        | `OPERATION`       | △                                   |
| 참가비를 받고 진행하지 않았어요 | `PAID_ROOM`       | ○                                   |
| 그 밖의 문제                    | —                 | **✕** — "기타"에 해당하는 값이 없다 |

시안에 대응이 없는 `NICKNAME`·`DIFFICULTY`도 남는다(앱 전용일 수 있다).

**(c) 별점 시트에 호스트 이름이 없다** — 시안은 "김선생 선생님"을 카드 맨 위에 크게 띄우는데 `GET /rooms/{roomId}/results/me` 응답에 호스트 이름이 없다. 지금은 그 줄을 감춰 뒀다.

**물어볼 것**

1. 위 표에서 ✕·△ 표시한 대응을 확정해 주세요. 라벨을 바꿀지 enum을 바꿀지도 함께
2. 신고에 "기타"가 필요한가요? 없으면 시안의 "그 밖의 문제"를 빼야 합니다
3. `SessionResultResponse`에 호스트 이름(또는 hostId)을 넣어 줄 수 있나요?
4. 별점 시트가 뜨는 시점 — 시안에 \[건너뛰기]가 있어 **부르지 않아도 스스로 뜨는 화면**으로 봤고, 세션 결과 화면에서 `canRate`가 참일 때 자동으로 띄웠습니다. 의도가 맞는지 확인 부탁드립니다

## 4-3. H. 백엔드 회신 대기 질문 (2026-09-02, 확정 계약 대조 후)

백엔드 코드(`Passmate-Backend@develop 5f433d2`)를 그대로 읽고 만든 질문이다 — **문안 그대로** 보내면 된다.
원본은 `specs/001-passmate-mvp/research.md` §B.

| #   | 질문                                                                                                                                                                                                                                                           | 왜 필요한가    |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| B-1 | `PARTICIPANT_JOINED`·`PARTICIPANT_LEFT`가 enum에만 있고 `ParticipantService`에서 발행하지 않습니다. 대기실 실시간 갱신(FR-006)을 위해 입장·퇴장·강퇴 시 `/topic/rooms/{id}`로 `ParticipantResponse`를 실어 보내 주실 수 있나요? 그 전까지 웹은 3초 폴링합니다. | 대기실         |
| B-2 | `WebSocketConfig.setAllowedOriginPatterns`가 `https://*.passmate.app`인데 운영 도메인은 `passmate.kr`입니다. `WEB_BASE_URL` 기반으로 바꾸거나 `*.passmate.kr`을 추가해 주세요.                                                                                 | 운영 WS        |
| B-3 | 시각 필드가 `LocalDateTime`(오프셋 없음)으로 나갑니다. 서버가 UTC라 웹은 `Z`를 붙여 해석할 텐데, 앱과 통일하려면 `OffsetDateTime`/ISO `Z` 직렬화가 안전합니다. 바꿀 계획이 있나요?                                                                             | 시각           |
| B-4 | 종료·취소된 방의 PIN도 `ROOM_NOT_FOUND`(404)입니다. 시안 "종료된 방이에요" 문구를 위해 `ROOM_ENDED`(410) 구분이 가능한가요?                                                                                                                                    | 입장 오류 문구 |
| B-5 | 참가자 기본 아바타가 `"default"`로 들어갑니다. 웹·앱 아바타 키 12종(`cat`…`dino`) 중 하나로 맞출 수 있나요?                                                                                                                                                    | 아바타         |
| B-6 | 게스트 `accessToken` 수명이 액세스 토큰과 같은 1시간이고 refresh가 없습니다. 1시간 넘는 세션에서 게스트가 끊깁니다 — 방 종료 시각까지로 늘리거나 재발급 경로가 필요합니다.                                                                                     | 게스트         |
| B-7 | `POST /rooms/{id}/session/start`가 204입니다. 명세 FR-062의 `aiAnalysisEnabled`(세션별 서술형 분석 on/off)는 폐기된 건가요, 아니면 `POST …/answers/me/analysis`(회원 직접 요청·월 5회)로 대체된 건가요?                                                        | 결과 화면 분기 |
| B-8 | AI 문항 생성 **남은 무료 횟수**를 주는 응답이 없습니다(`GET /me/ai-usage` 삭제). 429 전에 화면에 "n회 남음"을 보이려면 `MyProfileResponse` 또는 세트 상세에 필드 추가가 필요합니다(DESIGN_GAPS G-1).                                                           | 에디터         |
| B-9 | nginx가 웹을 정적 SPA로 서빙하도록 돼 있는데 웹은 Next.js SSR입니다. Node 컨테이너 프록시로 갈지, 웹은 별도 호스팅으로 갈지 결정이 필요합니다(D-2).                                                                                                            | 배포           |

> B-8은 위 **G-1**(AI 무료 생성 정책 3자 모순)과 같은 뿌리다 — 정책이 정해져도 **횟수를 주는 필드**가 없으면 화면에 못 그린다.
> B-1은 지금 웹이 **3초 폴링**으로 버티고 있다. 이벤트가 오면 폴링을 지우는 쪽이 서버·클라이언트 모두에 낫다.

---

## 5. 디자이너 요청 순서 (제안)

> ✅ **v6로 상당수 도착.** 아래 목록 중 **A-6 로그인 콜백 · A-5 공개 방 목록 · A-8 신고 · W-11 실패 상태**와 **B의 오류·빈 상태·로딩 전반**이 왔다(§0-1).
> 🔜 **남은 요청은 3건으로 좁혀졌다** — ① `A-3 호스트 최종 결과`(프로젝터, W-12는 왔으나 A-3과 같은 화면인지 확인 필요) ② `A-2 학생 웹 대기실` ③ `A-7 호스트 공개 프로필`.
> ⏸ **G-1·G-5는 그리기 전에 결정이 필요하다**(AI 무료 정책 문구, 비밀번호 변경 프레임 폐기 여부).

1. **P1 — 시연 최소 제품에 필요** (US1~3): A-3 호스트 최종 결과 · A-2 학생 웹 대기실 · A-6 로그인 콜백 · B의 ★ 항목(C-03/W-01 오류 3종, W-03 한도·실패·잔여 횟수, W-04 시작 불가·분석 없음, W-05 재연결·이탈 확인·화면 잠금·문항 마감)
2. **P2** (US4~6): A-1 학생 결과·리포트(별점 태그 5종 포함) · W-06 서술형 변형 · P-Web 잠금·마감 상태 · W-13 빈 상태
3. **P3** (US7~13): A-4 첨삭 입력 · A-8 신고 · A-7 호스트 프로필 · W-03 자료 업로드 · W-08 DRAFT 배지 · W-09 등급 이력 · C-02 별점
4. **P4** (US14~16): A-5 공개 방 목록 · W-02 노출 토글·일정·정원 · W-09 취소·환불 · W-10 보류·이월·차감 · W-11 실패 상태
5. **결정 먼저** (§C): C-1 로그인 방식과 C-9 학생 웹 범위는 그리기 전에 정해야 낭비가 없다.

## 6. 부록 — 오류 코드 → 화면 문구 매핑 (디자이너 참고)

시안에 오류 상태를 그릴 때 쓰는 서버 코드. HTTP 숫자가 아니라 코드로 문구를 나눈다.

> ⚠️ **9/1 전면 교체.** 8/31에 적었던 코드 8개 중 **서버에 실재한 건 2개뿐이었다.** 아래는 백엔드 `common/exception/ErrorCode.kt`(develop @ `f719493`)를 그대로 옮긴 정본이며, 코드에서는 `src/lib/types/error-codes.ts`가 원천이다(서버가 바뀌면 `src/lib/types/error-codes.test.ts`가 먼저 깨진다).
>
> **바뀐 이름 (옛 → 지금)** — 낡은 문서·시안에서 옛 이름을 보면 이 표로 옮길 것
>
> | 옛 이름 (8/31)                             | 지금                                                               |
> | ------------------------------------------ | ------------------------------------------------------------------ |
> | `NICKNAME_TAKEN`                           | **`NICKNAME_DUPLICATED`** (409)                                    |
> | `LOGIN_REQUIRED` 401                       | **`GUEST_NOT_ALLOWED`** (403)                                      |
> | `PAYMENT_REQUIRED` · `FREE_QUOTA_EXCEEDED` | **`INSUFFICIENT_COINS`** (402)                                     |
> | `GONE` 410                                 | **없다** — `ROOM_NOT_JOINABLE`(409)·`ROOM_NOT_FOUND`(404)로 나뉜다 |
> | `ALREADY_RATED` · `RECORD_PURGED`          | 서버 enum에 **아직 없다** (`@draft`, 우선순위 3 미구현)            |
> | `SCREEN_LOCKED`                            | 화면 잠금 **거취 자체가 미결** (§0-1 회신 대기 4번)                |

| 코드                                       | HTTP    | 상황                                    | 화면                              |
| ------------------------------------------ | ------- | --------------------------------------- | --------------------------------- |
| `ROOM_NOT_FOUND`                           | 404     | PIN이 틀림 — "그런 방이 없어요"         | C-03 · W-01                       |
| `ROOM_NOT_JOINABLE`                        | 409     | 종료됐거나 입장할 수 없는 방            | C-03 · W-01                       |
| `ROOM_FULL`                                | 409     | 정원 초과                               | C-03 · W-11                       |
| `NICKNAME_DUPLICATED`                      | 409     | 같은 방에 같은 닉네임                   | C-03 · W-01                       |
| `ALREADY_JOINED`                           | 409     | 이미 참여한 방                          | C-03                              |
| `GUEST_NOT_ALLOWED`                        | 403     | 게스트가 유료 방·회원 전용 기능 접근    | C-03 · W-11                       |
| `INSUFFICIENT_COINS`                       | 402     | **코인 부족** — 유료 방 입장 · AI 생성  | W-11 · **W-03(B 카드)**           |
| `HOST_LEVEL_REQUIRED`                      | 403     | Lv.3 미만 유료 방 개설                  | W-02                              |
| `AI_GENERATION_FAILED`                     | 502     | AI 생성 최종 실패                       | **W-03 "문제를 만들지 못했어요"** |
| `PIN_GENERATION_FAILED`                    | 500     | PIN 발급 실패                           | **W-02e "방을 만들지 못했어요"**  |
| `QUESTION_SET_EMPTY`                       | 409     | 문항이 없는 세트로 시작                 | W-04                              |
| `QUESTION_SET_ALREADY_CONFIRMED`           | 409     | 확정된 세트를 다시 수정                 | W-03 · W-08                       |
| `NOT_ROOM_HOST` · `NOT_QUESTION_SET_OWNER` | 403     | 남의 방·세트 조작                       | W-05 · W-08                       |
| `TOKEN_EXPIRED` · `TOKEN_INVALID`          | 401     | 세션 만료 (refresh 1회 후에도 실패)     | **E-401 세션 만료**               |
| `SOCIAL_TOKEN_INVALID`                     | 401     | 구글 토큰 검증 실패                     | **C-01b 로그인 실패**             |
| `ACCOUNT_SUSPENDED`                        | 403     | 정지된 계정                             | C-01b                             |
| `NOT_FOUND`                                | 404     | 그 밖의 없는 리소스                     | **E-404**                         |
| `INTERNAL_ERROR` · `EXTERNAL_API_ERROR`    | 500·502 | 서버·외부 API 장애                      | **E-500 점검 중**                 |
| `CONFLICT`                                 | 409     | 정산 미지급·진행 중 방이 있어 탈퇴 불가 | C-02-12                           |
| `ALREADY_RATED` `@draft`                   | 409     | 같은 세션 재평가                        | P-Web 별점 시트                   |
| `RECORD_PURGED` `@draft`                   | 410     | 7일 지난 게스트 기록 연동               | W-13                              |

**v6 시안 문구와의 대응** — `04` 보드 D 카드가 "오류코드를 쓰지 말라"고 했으니 **코드는 분기용으로만 쓰고 화면에는 절대 노출하지 않는다.** 위 표의 "화면" 열이 곧 그 분기표다.
