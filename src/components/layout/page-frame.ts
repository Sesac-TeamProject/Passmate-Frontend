/**
 * 상단 헤더와 그 아래 본문이 같은 세로선에서 시작하게 하는 폭 규격.
 *
 * 시안은 로고·로그인이 페이지 가장자리에서 한 뼘 안쪽에 서고, 본문 카드는 그보다 조금 더
 * 안쪽에 선다. 헤더와 본문이 각자 숫자를 들면 한쪽만 바뀌어 줄이 어긋나므로 여기 한 곳에 둔다.
 * (리포트·결과 화면처럼 자기 시안 폭이 따로 있는 화면은 그 폭을 쓰고, 이 규격은 헤더와
 * 가장자리를 공유하는 화면만 쓴다.)
 */
export const PAGE_FRAME = "mx-auto w-full max-w-[1440px] px-10";
