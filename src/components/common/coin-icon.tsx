import { cn } from "@/lib/utils";

/**
 * 코인 아이콘 (디자인 시스템 `icon/coin` 402:5276 — "코인 잔액 · 충전 · 24px 그리드 · 선 2px").
 *
 * 내보낸 SVG 를 `public/` 에 두지 않고 컴포넌트로 두는 이유는 **색 때문**이다. 이 아이콘 무리는
 * "색은 ink / idle / mint 중 하나"로 쓰라고 적혀 있는데(icon/home · icon/user 등 같은 규칙),
 * 파일로 두면 내보낼 때 박힌 한 가지 색으로 굳는다 — 실제로 마이페이지는 mint, 코인 내역은 ink 다.
 * 그래서 획을 `currentColor` 로 두고 색은 쓰는 자리의 `text-*` 가 정하게 한다.
 *
 * 좌표·획 두께는 피그마 내보내기 그대로다(22 박스 · 1.8333 = 24그리드 2px 를 22 로 줄인 값).
 * 크기는 `size-*` 로 준다 — 박스째 비율로 줄고 늘어 시안의 인스턴스(18 · 22 · 24)와 같아진다.
 */
export function CoinIcon({ className }: { className?: string }) {
  return (
    <svg
      aria-hidden
      viewBox="0 0 22 22"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.83333}
      strokeLinecap="round"
      strokeLinejoin="round"
      className={cn("size-[22px] shrink-0", className)}
    >
      <path d="M11 19.25C15.5563 19.25 19.25 15.5563 19.25 11C19.25 6.44365 15.5563 2.75 11 2.75C6.44365 2.75 2.75 6.44365 2.75 11C2.75 15.5563 6.44365 19.25 11 19.25Z" />
      <path d="M13.75 8.70833C13.2752 8.11532 12.6279 7.68441 11.8977 7.47517C11.1674 7.26592 10.3901 7.28867 9.67334 7.54027C8.95655 7.79186 8.33559 8.25988 7.89629 8.87965C7.457 9.49942 7.22104 10.2403 7.22104 11C7.22104 11.7597 7.457 12.5006 7.89629 13.1203C8.33559 13.7401 8.95655 14.2081 9.67334 14.4597C10.3901 14.7113 11.1674 14.7341 11.8977 14.5248C12.6279 14.3156 13.2752 13.8847 13.75 13.2917" />
    </svg>
  );
}
