# features/host

방을 **개설·운영**하는 화면(W-01~W-08: 대시보드·문제 세트·에디터·방 설정·프로젝터·리포트·정산)의 도메인 컴포넌트를 둔다. 계정에 역할은 없다 — 같은 회원이 여기서 방을 열고 `features/participant`에서 참여한다.

- 예: `src/features/host/<화면명>/<컴포넌트>.tsx`
- 목업: `mock.ts` (`TODO(API)`가 연동 지점)
