# 폰 폭(390px) 실측 대장

> 측정: 2026-09-19 · 43경로 전수 · 목 모드
> 다음에 "폰 대응 어디까지 됐나"를 물을 때 **추측하지 말고 이 표를 본다.**
> 이 표가 생긴 이유 — 핸드오버에 코드를 확인하지 않고 적은 후보 목록이 있었고,
> 그 목록의 두 묶음(`/result` 계열 · `/me` 하위)은 **이미 끝난 상태**였다.

## 재는 방법

레포 사본을 만들어 3100에 목 모드로 띄우고, `public/harness.html` 에 390×844 iframe 을 두고
`contentDocument` 로 요소 좌표를 직접 잰다. 절차는 `HANDOVER.md` 의 "화면 확인하는 법" 과 같다.

판정은 두 숫자다.

- `documentElement.scrollWidth` — 390 을 넘으면 페이지가 옆으로 밀린다
- 뷰포트(391px)를 벗어난 요소 개수

**가로 스크롤 레일 안의 요소는 세지 않는다.** 조상에 `overflow-x: auto|scroll` 이 있으면
의도된 레일이다(랜딩·홈 캐러셀). 이걸 빼지 않으면 멀쩡한 화면이 "12곳 넘침" 으로 잡힌다.

```js
// 레일 안인지 판정 — 이 한 줄이 오탐을 없앤다
for (var p = el.parentElement; p && p !== de; p = p.parentElement) {
  var ox = getComputedStyle(p).overflowX;
  if (ox === "auto" || ox === "scroll") return; // 세지 않는다
}
```

## 결과 — 34 / 43 깨끗 · 6 깨짐(전부 admin) · 3 미측정

### ✅ 깨끗 (scrollWidth 390 · 넘친 요소 0)

| 구역            | 경로                                                                                                                                                                                                             |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| public (5)      | `/` · `/terms` · `/privacy` · `/hosts/[userId]` · `/rooms`                                                                                                                                                       |
| member (13)     | `/home` · `/me` · `/me/joined` · `/settlement` · `/account` · `/settlement-account` · `/character` · `/coins` · `/coins/charge` · `/coins/charge/complete` · `/payment-methods` · `/notifications` · `/withdraw` |
| participant (5) | `/join` · `/play/[code]` · `/result/[id]` · `/result/[id]/report` · `/result/[id]/report/[no]`                                                                                                                   |
| host (11)       | `/host/rooms` · `/reputation` · `/sets` · `/rooms/new` · `/editor` · `/sessions/[id]/review` · `/rooms/[code]/lobby` · `live` · `result` · `final` · `timing`                                                    |

### ❌ 남은 것 — admin 6경로

| 경로               | scrollWidth | 넘친 요소 |
| ------------------ | ----------- | --------- |
| `/admin/dashboard` | 706         | 76        |
| `/admin/payments`  | 577         | 13        |
| `/admin/branded`   | 567         | 12        |
| `/admin/reports`   | 528         | 9         |
| `/admin/users`     | 427         | 1         |
| `/admin/rooms`     | 427         | 1         |

**보류 결정(2026-09-19)** — 피그마 `02 · 웹 — 관리자` 섹션에 1440 만 있고 폰 시안이 없다.
숫자로도 1440 전용 마크업 그대로임이 확인됐다. 시안이 생기면 이 표부터 다시 잰다.

### ⬜ 목 모드로는 못 잰 3경로

| 경로             | 왜                                   | 코드의 폰 분기        |
| ---------------- | ------------------------------------ | --------------------- |
| `/login`         | 목 자동 로그인이 `/home` 으로 보낸다 | `login-page.tsx` 13곳 |
| `/auth/callback` | 같음                                 | —                     |
| `/pay/[roomId]`  | 목에 맞는 방이 없어 "없는 방이에요"  | `pay-page.tsx` 6곳    |

실서버 모드(8080)로 띄우면 잴 수 있다. 목 모드의 한계라 화면 결함이라는 뜻은 아니다.

`/host/rooms/[code]/timing` 도 같은 이유로 1차에서는 못 쟀다. 실서버 + 개발용 로그인으로 쟀다 —
`/login` 의 개발용 로그인 칸에 방 주인의 key 를 넣는다(방 81 `야구 퀴즈` 는 `host2`).
`key` 는 계정 이메일의 `@` 앞부분이다(`host2@dev.passmate.local`). 방에 문제 세트가 연결돼 있어야
화면이 뜬다 — 없으면 "연결된 문제 세트를 찾지 못했어요" 로 떨어진다.

## 고친 내역 (2026-09-19)

| 경로                                  | 전  | 후  | 원인                                                                                 |
| ------------------------------------- | --- | --- | ------------------------------------------------------------------------------------ |
| `/result/[id]/report` 신고 다이얼로그 | 494 | 333 | `w-[520px]` 가 shadcn 기본 `max-w-[calc(100%-2rem)]` 를 덮음                         |
| `/host/rooms/[code]/lobby`            | 550 | 390 | 방 코드 밑줄 `w-[430px]` · 3단계·지표 줄 가로 배치                                   |
| `/host/rooms/[code]/result`           | 574 | 390 | 헤더 문항 레일(8칸) · 본문 2단                                                       |
| `/host/rooms/[code]/final`            | 558 | 390 | 문항 레일 · 시상대 `w-[168px]`×3 · 순위 2열 · 버튼 176px×2                           |
| `/host/rooms/[code]/timing`           | 602 | 390 | 프리셋 5개(84px)+`일괄 적용`(164px)이 고정폭이라 글자 블록이 한 글자 폭까지 짜부라짐 |

공통 원인은 프로젝터 셸(`projector-shell.tsx`)의 좌우 여백 80 + 레일 300px 고정 = 폰 최소 460px.

## 재면서 걸린 함정 (다음에 또 걸린다)

- **상태 리다이렉트로 같은 화면을 여러 번 잰다.** 목의 방이 `WAITING` 이라
  `/live` · `/result` · `/final` 이 전부 `/lobby` 로 갔다. 네 화면이 똑같이 550px 로 나와
  "같은 셸이라 당연하다" 고 읽었는데 **대기실을 네 번 잰 것**이었다.
  → **착지 URL(`location.pathname`)을 매번 확인한다.** 숫자만 보면 안 된다.
- 진행 화면은 목의 상태 머신을 UI 로 밀어야 나온다: 대기실 `시험 시작` → `/live`,
  `정답 공개` → `/result`, `다음 문항` 을 끝까지 → `세션 종료` → `/final`.
  파일을 고치면 dev 서버가 다시 뜨면서 목 상태가 `WAITING` 으로 돌아간다.
- **데이터가 오기 전에 재면 로딩 화면을 잰다.** 본문 텍스트를 같이 받아 확인한다.
  목이라도 2.6초로는 모자란 화면이 있다(6~7초로 준다).
- **브라우저 JS 는 45초 제한.** 문항 8개를 넘기며 재려 하면 넘는다 — 진행과 측정을 나눈다.
- `/me/withdraw` 의 `탈퇴하기` 는 **확인 다이얼로그가 없다.** 체크박스만 누르면 바로 실행된다.
  자동화로 화면을 훑을 때 무심코 누르면 목 탈퇴가 돌고 랜딩으로 튕긴다.

## PC 불변을 확인하는 방법

눈이 아니라 좌표다. iframe 폭을 1440 으로 바꿔(`f.style.width`) 수정본 좌표를 재고,
**사본의 그 파일만** `git show HEAD:<경로>` 로 되돌려 다시 재서 비교한다.

2026-09-19 대조 결과 — 신고 다이얼로그 · 대기실 · 문항 결과 · 최종 순위 · 방 리포트 개요 탭
전부 차이 0.

## 2026-09-19 · PR #62 머지 뒤 재측정

팀원분의 W-07 리포트 리디자인(PR #62)이 `develop` 에 들어와 이 브랜치에 머지한 뒤 다시 쟀다.
**리포트 화면은 `StudentReviewPanel` 을 PC · 폰이 함께 쓰는데 그 파일에 폰 분기가 없다**
(`max-md:` 한 줄도 없이 `stacked` prop 으로 갈린다). 그래서 파일이 안 겹쳐도 폰이 바뀔 수 있어
전수로 다시 확인했다.

| 화면 · 상태                                   | scrollWidth | 넘친 요소 |
| --------------------------------------------- | ----------- | --------- |
| `/host/sessions/1/review` 개요 탭             | 390         | 0         |
| 〃 문항별 탭 (기본 · 문항 펼침)               | 390         | 0         |
| 〃 학생별 탭 (기본 · 학생 펼침 · 이름순 정렬) | 390         | 0         |
| `/host/rooms/482913/lobby`                    | 390         | 0         |
| `/host/rooms/482913/final`                    | 390         | 0         |
| `/host/rooms/482913/timing`                   | 390         | 0         |

폰이 안전한 까닭은 렌더 경로에서 확인했다 — `stacked` 일 때 새 비교 표는 `hidden`,
우측 상세 헤더는 `!stacked` 조건, `w-[396px] shrink-0` 고정폭은 `flex-1` 로 갈린다.
DOM 에 `<table>` 2개가 있으나 `offsetParent === null` 로 **보이지 않는 것**까지 실측으로 봤다.
폰에서 실제로 달라진 값은 컨테이너 간격뿐이다(`gap-5` → `gap-3`, `pt-4` → `pt-0`).

### `/host/rooms/[code]/result` 는 목으로 못 잰다 — 구조적 한계

`result/page.tsx` 는 `session-store` 의 `reveal` 이 있어야 렌더된다. 그 값은 STOMP
`QUESTION_ENDED` 가 채우는데 **목 모드는 실시간을 no-op 으로 대체한다.** 그래서 주소로 들어가면
`reveal === null` 이라 `ScreenLoading` 을 거쳐 대기실로 돌아간다.
이번에도 `/result` 를 열었다가 대기실 문구가 잡혀 리다이렉트를 확인했다(지난번과 같은 함정).

**이 화면을 재려면 로컬 백엔드로 실제 세션을 시작해 문항을 마감시켜야 한다.**
PR #62 는 `result-page.tsx` 를 건드리지 않았으므로 이번 머지분에 대해서는 재측정 대상이 아니다.

### 착지 확인은 문구로 한다 — `location` 은 읽히지 않는다

브라우저 자동화가 `location` 접근과 **긴 본문 텍스트 반환을 차단한다**
(`[BLOCKED: Cookie/query string data]`). 그래서 경로 대신 화면 고유 문구의 **포함 여부만**
불리언으로 받아 착지를 확인했다. 문구를 잘못 고르면(`"정답"` 처럼 흔한 말) 헛나가니
화면에만 있는 말을 쓴다 — 시간 설정은 `일괄`, 최종 순위는 `최종 순위`.
