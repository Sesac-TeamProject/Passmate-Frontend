# API 레이어 연결 준비 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 백엔드가 열리면 `NEXT_PUBLIC_API_BASE_URL` 한 줄로 붙도록, A 구역(학생·선생님·회원) 화면이 목 데이터 import 대신 `lib/queries` 훅과 `session-store`를 통해 데이터를 받게 만든다.

**Architecture:** 3층(전송 `lib/api`·`lib/stomp` → 상태 `lib/queries`·`lib/stores` → UI `app`·`features`)을 유지한다. DTO는 KMP `shared`가 실제 직렬화하는 모양과 1:1(`lib/types/dto/*`), KMP에 없는 호출은 `tasks.md` 경로로 `@draft`. 화면(`*View`)의 props 타입은 바꾸지 않고 **feature 폴더의 `adapt.ts`(DTO → 기존 뷰 타입)** 로 흡수한다. 목 데이터는 `lib/mocks/*` 한 곳으로 옮기고 feature `mock.ts`는 삭제한다.

**Tech Stack:** Next.js 16 App Router, TypeScript strict, TanStack Query 5, Zustand 5, `@stomp/stompjs`(신규), Vitest, pnpm.

**Spec:** `docs/superpowers/specs/2026-08-31-api-layer-design.md` · 계약 인벤토리: 세션 스크래치패드 `kmp-contract-inventory.md`(§3 DTO·§4 이벤트를 이 플랜에 옮겨 적었다)

## Global Constraints

- 브랜치 `feature/api-layer`(base `origin/develop`). `design/design.pen`·`.gitignore`·`HANDOVER.md`·`DESIGN_AUDIT.md`·`DESIGN_GAPS.md`·`.claude/`·`.specify/`·`specs/`는 **커밋하지 않는다**. `git add`는 항상 경로 지정, `git add -A` 금지. 푸시 금지.
- 커밋 메시지 한국어, prefix `feat:`/`fix:`/`chore:`/`docs:`/`test:`. 커밋 전 `pnpm format && pnpm lint && pnpm test` 통과.
- 컴포넌트·page에서 `fetch`/STOMP 직접 호출 금지. `*View`는 props만. 서버 상태를 useState/Zustand에 복사 금지. HTTP 숫자가 아니라 서버 `code`로 문구 분기. `any`·`@ts-ignore` 금지.
- 색상 hex 금지(시맨틱 토큰만). 사용자 노출 문구 한국어.
- `/admin/*`·`features/admin`·`lib/api/admin.ts`·`lib/queries/use-admin-*`·`lib/mocks/admin.ts`는 팀원 B 소유 — **내용 수정 금지**(파일 이동·import 경로 갱신만 허용).
- DTO 필드명은 KMP와 동일. Kotlin 기본값이 있는 필드는 `?:`(서버 생략 가능), nullable은 `| null`. 계약에 없는 필드를 추가하지 않는다. 계약이 없는 호출/필드는 JSDoc `@draft — 계약 없음. tasks.md T0xx 경로. 계약 도착 시 수정` 으로 표시한다.
- 라우트를 추가하면 `src/config/routes.ts` → page → `pnpm build && pnpm check:routes`.
- 화면 컨테이너 규칙: 훅 상단 → 파생·핸들러 → `useEffect` → JSX. 조회 실패 `ScreenError`+재시도, 로딩 `ScreenLoading`, 뮤테이션 `isPending` 동안 버튼 비활성.

---

## File Structure

| 파일 | 책임 | Task |
| --- | --- | --- |
| `src/lib/types/app-error.ts` | `PaymentRequired` kind 추가 | 1 |
| `src/lib/types/error-codes.ts` | 서버 `code` 문자열 상수 | 1 |
| `src/lib/env.ts` | `WS_URL` 추가 | 1 |
| `src/lib/guest-token-storage.ts` | 게스트 `participantToken` sessionStorage 보관 | 1 |
| `src/lib/api/client.ts` | 401 정책(회원 토큰 있을 때만 refresh·`LOGIN_REQUIRED` 보존), refresh 미회전, 게스트 헤더, `requestMultipart` | 1 |
| `src/lib/api/client.test.ts` | 위 규칙 테스트 | 1 |
| `src/lib/types/dto/{auth,admin,rooms,session,question-sets,results,me,payments,ratings,common}.ts` | 계약 1:1 DTO (도메인 분할) | 2 |
| `src/lib/types/dto.ts` | re-export 허브 | 2 |
| `src/lib/types/events.ts` | STOMP 이벤트 유니온 | 2 |
| `src/lib/mocks/router.ts` | `METHOD /path/:param` 매칭 | 3 |
| `src/lib/mocks/fixtures.ts` | 공용 픽스처(회원 이한결·방 482913·세트) | 3 |
| `src/lib/mocks/{rooms,session,question-sets,results,me,payments}.ts` | 도메인 목 응답 | 3 |
| `src/lib/mocks/handlers.ts` | 라우트 표(기존 + 신규) | 3 |
| `src/lib/mocks/handlers.test.ts` | api 경로 ↔ 핸들러 커버리지 | 3·4 |
| `src/lib/api/{rooms,sessions,question-sets,results,me,payments,ratings}.ts` | 도메인 api 함수 | 4 |
| `src/lib/api/auth.ts` | `/users/me`·logout·Google 로그인 URL | 4 |
| `src/lib/queries/use-{rooms,session-control,question-sets,results,me,payments,ratings}.ts` | 쿼리·뮤테이션 훅 | 5 |
| `src/lib/stores/session-store.ts` + `session-reducer.ts` + `session-reducer.test.ts` | 실시간 상태·순수 리듀서 | 6 |
| `src/lib/stomp.ts` | STOMP 연결·구독·재연결 | 6 |
| `src/lib/queries/use-session-connection.ts` | 연결 → 스냅샷 → 스토어 교체 | 6 |
| `src/config/routes.ts` + `src/app/(bare)/auth/callback/page.tsx` | 로그인 콜백 | 7 |
| `src/features/**/types.ts` · `adapt.ts` | 뷰 타입(구 mock.ts에서 이동)·DTO→뷰 어댑터 | 8~12 |
| `src/app/**/page.tsx`(A 구역) | 훅 연결 | 8~12 |
| `src/features/**/mock.ts` 삭제, README·CLAUDE 갱신 | 정리 | 13 |

---

### Task 1: 전송 층 보강 — 402·`LOGIN_REQUIRED`·refresh 미회전·게스트 토큰·multipart·WS URL

**Files:**
- Modify: `src/lib/types/app-error.ts`
- Create: `src/lib/types/error-codes.ts`
- Modify: `src/lib/env.ts`
- Create: `src/lib/guest-token-storage.ts`
- Modify: `src/lib/api/client.ts`
- Modify: `src/lib/queries/query-client.ts` (NON_RETRYABLE에 `PaymentRequired`)
- Test: `src/lib/api/client.test.ts`
- Modify: `.env.example`

**Interfaces:**
- Produces: `AppErrorKind`에 `"PaymentRequired"`; `ERROR_CODES` 상수; `WS_URL`; `readGuestToken()/writeGuestToken()/clearGuestToken()`; `requestMultipart<T>(path, form: FormData)`; `request()`의 401 정책 변경.

- [ ] **Step 1: 실패하는 테스트 4개 추가** — `src/lib/api/client.test.ts`의 `describe` 안, 기존 5개 뒤에:

```ts
  it("402는 PaymentRequired가 된다", async () => {
    stubLocalStorage({});
    fetchMock.mockResolvedValueOnce(
      jsonResponse(402, { code: "PAYMENT_REQUIRED", message: "코인 부족" }),
    );
    const { request } = await loadClient();

    await expect(request("/rooms/1/entry-payments", { method: "POST", body: {} })).rejects.toMatchObject(
      { kind: "PaymentRequired", code: "PAYMENT_REQUIRED" },
    );
  });

  it("회원 토큰이 없으면 401에 refresh를 시도하지 않고 code를 보존한다 (게스트 유료 방)", async () => {
    stubLocalStorage({});
    fetchMock.mockResolvedValueOnce(
      jsonResponse(401, { code: "LOGIN_REQUIRED", message: "로그인 필요" }),
    );
    const { request } = await loadClient();

    await expect(request("/rooms/1/participants", { method: "POST", body: {} })).rejects.toMatchObject(
      { kind: "Unauthorized", code: "LOGIN_REQUIRED" },
    );
    expect(fetchMock).toHaveBeenCalledTimes(1);
  });

  it("refresh 응답에 refreshToken이 없으면 기존 refresh 토큰을 유지한다", async () => {
    const store = stubLocalStorage({ [REFRESH_KEY]: "old-refresh" });
    fetchMock
      .mockResolvedValueOnce(jsonResponse(401, { code: "TOKEN_EXPIRED", message: "만료" }))
      .mockResolvedValueOnce(jsonResponse(200, { accessToken: "new-access" }))
      .mockResolvedValueOnce(jsonResponse(200, { ok: true }));
    const { request, useAuthStore } = await loadClient();
    useAuthStore.getState().setAccessToken("old-access");

    await request("/users/me");

    expect(store.get(REFRESH_KEY)).toBe("old-refresh");
    expect(useAuthStore.getState().accessToken).toBe("new-access");
  });

  it("회원 토큰이 없고 게스트 토큰이 있으면 게스트 토큰을 Bearer로 보낸다", async () => {
    stubLocalStorage({});
    const { request } = await loadClient();
    const { writeGuestToken } = await import("@/lib/guest-token-storage");
    writeGuestToken("guest-1");
    fetchMock.mockResolvedValueOnce(jsonResponse(200, { ok: true }));

    await request("/rooms/1/session");

    const [, init] = fetchMock.mock.calls[0] as [string, RequestInit];
    expect((init.headers as Headers).get("Authorization")).toBe("Bearer guest-1");
  });
```

`stubLocalStorage`의 `vi.stubGlobal("window", {...})`에 `sessionStorage`도 넣는다(게스트 토큰용):

```ts
  const session = new Map<string, string>();
  vi.stubGlobal("window", {
    localStorage: { /* 기존 */ },
    sessionStorage: {
      getItem: (k: string) => session.get(k) ?? null,
      setItem: (k: string, v: string) => void session.set(k, v),
      removeItem: (k: string) => void session.delete(k),
    },
    location: { pathname: "/admin/users", search: "", assign: vi.fn() },
  });
```

- [ ] **Step 2: 실패 확인** — `pnpm test src/lib/api/client.test.ts` → 새 4개 FAIL(`PaymentRequired` 미정의·refresh 호출됨·`guest-token-storage` 없음).

- [ ] **Step 3: `app-error.ts`** — `AppErrorKind`에 `| "PaymentRequired"`, `USER_MESSAGE`에 `PaymentRequired: "코인이 부족합니다. 충전 후 다시 시도해 주세요."`, `KIND_BY_STATUS`에 `402: "PaymentRequired"`.

- [ ] **Step 4: `error-codes.ts` 생성**

```ts
/** 서버 오류 응답 `code` (계약 §공통 오류 코드 + KMP 관측값). 화면은 HTTP 숫자가 아니라 이 값으로 문구를 분기한다. */
export const ERROR_CODES = {
  LOGIN_REQUIRED: "LOGIN_REQUIRED",
  PAYMENT_REQUIRED: "PAYMENT_REQUIRED",
  HOST_LEVEL_REQUIRED: "HOST_LEVEL_REQUIRED",
  FREE_QUOTA_EXCEEDED: "FREE_QUOTA_EXCEEDED",
  AI_GENERATION_FAILED: "AI_GENERATION_FAILED",
  NICKNAME_TAKEN: "NICKNAME_TAKEN",
  ALREADY_RATED: "ALREADY_RATED",
  RECORD_PURGED: "RECORD_PURGED",
} as const;

export type ErrorCode = (typeof ERROR_CODES)[keyof typeof ERROR_CODES];
```

- [ ] **Step 5: `env.ts`** — 추가:

```ts
/** STOMP 엔드포인트. 비우면 API base에서 유도한다 (http://h:8080/api/v1 → ws://h:8080/ws). */
export const WS_URL = process.env.NEXT_PUBLIC_WS_URL ?? deriveWsUrl(API_BASE_URL);

function deriveWsUrl(apiBase: string): string {
  if (!apiBase) return "";
  return apiBase.replace(/^http/, "ws").replace(/\/api\/v\d+\/?$/, "") + "/ws";
}
```

`.env.example`에 `NEXT_PUBLIC_WS_URL=` 과 주석("비우면 API 주소에서 유도. 예: ws://localhost:8080/ws"), 그리고 `NEXT_PUBLIC_API_BASE_URL` 주석에 "예: http://localhost:8080/api/v1 (`/api/v1`까지)"을 적는다.

- [ ] **Step 6: `guest-token-storage.ts` 생성** — `token-storage.ts`와 같은 꼴, `window.sessionStorage`, 키 `"passmate.guestToken"`. `readGuestToken(): string | null`, `writeGuestToken(token: string)`, `clearGuestToken()`.

- [ ] **Step 7: `client.ts` 수정**
  - `send()`: `accessToken`이 없으면 `readGuestToken()`을 Bearer로 쓴다.
  - `sendWithRefresh()`: 401일 때 **`store.accessToken`이 있고** `path !== REFRESH_PATH`일 때만 refresh. 그 외 401은 본문을 읽어 `toAppError`로 던진다(code 보존). refresh 실패 시 기존처럼 `Unauthorized`.
  - `doRefresh()`: 응답 타입을 `TokenRefreshResponse = { accessToken: string; refreshToken?: string | null }`로 두고 `if (tokens.refreshToken) writeRefreshToken(tokens.refreshToken)`.
  - 추가:

```ts
/** multipart/form-data (PTT 클립·자료 업로드). Content-Type은 브라우저가 boundary와 함께 붙인다. */
export async function requestMultipart<T>(path: string, form: FormData): Promise<T> {
  const url = buildUrl(path);

  if (IS_MOCK) return resolveMock<T>("POST", url, form);

  const response = await sendWithRefresh(url, path, { method: "POST", body: form });

  if (response.status === HTTP_NO_CONTENT) return undefined as T;

  return (await response.json()) as T;
}
```

  `send()`에서 `options.body instanceof FormData`면 `Content-Type`을 설정하지 않고 body를 그대로 넘긴다. `RequestOptions.body` 타입은 `unknown` 유지. `resolveMock`의 세 번째 인자(body)는 Task 3에서 시그니처를 맞춘다 — 지금은 `resolveMock<T>(method, url)`로 두고 Task 3에서 body를 추가한다.

- [ ] **Step 8: `query-client.ts`** — `NON_RETRYABLE`에 `"PaymentRequired"` 추가.

- [ ] **Step 9: 통과 확인** — `pnpm test src/lib/api/client.test.ts` → 9개 PASS. `pnpm exec tsc --noEmit` 0.

- [ ] **Step 10: 커밋**

```bash
pnpm format && pnpm lint
git add src/lib/types/app-error.ts src/lib/types/error-codes.ts src/lib/env.ts src/lib/guest-token-storage.ts src/lib/api/client.ts src/lib/api/client.test.ts src/lib/queries/query-client.ts .env.example
git commit -m "feat: API 클라이언트 계약 정합 — 402 PaymentRequired, 회원 토큰 있을 때만 401 refresh(LOGIN_REQUIRED 보존), refresh 미회전 허용, 게스트 토큰 Bearer, multipart 헬퍼, WS URL env"
```

---

### Task 2: DTO 도메인 분할 + KMP 계약 1:1 타입 + STOMP 이벤트 타입

**Files:**
- Create: `src/lib/types/dto/common.ts`, `auth.ts`, `rooms.ts`, `session.ts`, `question-sets.ts`, `results.ts`, `me.ts`, `payments.ts`, `ratings.ts`, `admin.ts`
- Modify: `src/lib/types/dto.ts` (내용을 re-export 허브로 교체)
- Create: `src/lib/types/events.ts`

**Interfaces:**
- Produces: 아래 모든 타입. 기존 import `@/lib/types/dto`는 그대로 동작해야 한다(허브가 전부 re-export).
- 검증: 타입 전용이라 테스트 없음. `pnpm exec tsc --noEmit` 0 + `pnpm test`(기존 9개) 통과가 완료 조건.

- [ ] **Step 1: `dto/admin.ts`** — 기존 `dto.ts`의 `/* ── A-01 …` 부터 파일 끝까지(관리자 DTO 전부)를 **그대로 옮긴다**. 상단에 `import type { UserRole } from "./auth";` 를 추가한다(`AdminUserSummary.role`이 `Exclude<UserRole,"ADMIN">`). 내용 수정 금지(팀원 B 소유).

- [ ] **Step 2: `dto/common.ts`**

```ts
/** 오류 응답 본문 (계약 §공통 오류 형식). KMP는 둘 다 생략 가능으로 파싱한다. */
export type ApiErrorBody = { code?: string | null; message?: string | null };

/** 커서 페이지네이션 공통 응답 (`?cursor=` 요청) */
export type CursorPage<T> = { items?: T[]; nextCursor?: string | null; hasNext?: boolean };

export type RoomStatus = "WAITING" | "RUNNING" | "FINISHED" | "ENDED";
export type QuestionType = "MULTIPLE_CHOICE" | "OX" | "ESSAY";
/** 호스트 등급 1~5 (새싹·성장·검증된 운영자·인기 운영자·마스터). 숫자로 전송 */
export type HostLevel = 1 | 2 | 3 | 4 | 5;
export type PaymentMethod = "KAKAO_PAY" | "NAVER_PAY" | "TOSS_PAY" | "CARD" | "TRANSFER";
```

- [ ] **Step 3: `dto/auth.ts`**

```ts
import type { HostLevel } from "./common";

/** @draft — 계약(`GET /users/me`)에는 role이 없다. 관리자 가드용으로 서버에 추가 요청(DESIGN_GAPS D-9). */
export type UserRole = "TEACHER" | "STUDENT" | "ADMIN";

/** POST /auth/refresh 요청 */
export type TokenRefreshRequest = { refreshToken: string };
/** POST /auth/refresh 응답 — refreshToken은 미회전 시 생략 */
export type TokenRefreshResponse = { accessToken: string; refreshToken?: string | null };
/** 로그인 콜백 쿼리(`/auth/callback?accessToken&refreshToken`)로 받는 쌍 */
export type TokenPair = { accessToken: string; refreshToken: string };

/** GET /users/me — 계약 UserProfileResponse 1:1 */
export type UserProfileResponse = {
  nickname?: string;
  email?: string | null;
  joinedAt?: string | null;
  avatarId?: number | null;
  level?: HostLevel | null;
  coins?: number | null;
  joinedRoomCount?: number | null;
  hostedRoomCount?: number | null;
};

/** auth-store가 들고 있는 내 프로필. userId·role은 @draft(D-9). */
export type MeResponse = UserProfileResponse & { userId?: number; role?: UserRole };

/** PUT /users/me — 닉네임·기본 캐릭터 부분 수정 */
export type UpdateProfileRequest = { nickname?: string | null; avatarId?: number | null };
```

기존 `RefreshTokenRequest`는 `TokenRefreshRequest`로 이름을 바꾸고 `client.ts` import를 갱신한다. `dto/admin.ts`·`mocks/auth.ts`가 쓰는 `MeResponse`는 새 모양에 맞춘다(`mocks/auth.ts`의 `MOCK_ME`는 Task 3에서 갱신).

- [ ] **Step 4: `dto/rooms.ts`**

```ts
import type { CursorPage, HostLevel, RoomStatus } from "./common";

/** POST /rooms — questionSetId는 CONFIRMED 세트만 */
export type CreateRoomRequest = {
  title: string;
  questionSetId?: number | null;
  topic?: string | null;
  maxParticipants?: number | null;
  /** ISO-8601 */
  scheduledAt?: string | null;
  isPaid?: boolean;
  /** 코인(1 C = ₩1) */
  entryFee?: number | null;
  isListed?: boolean;
};
export type CreateRoomResponse = { roomId?: number; pin?: string; qrUrl?: string | null };

/** GET /users/me/rooms/hosted 항목 */
export type HostedRoomDto = {
  roomId?: number;
  pin?: string;
  title?: string;
  status?: RoomStatus | null;
  participantCount?: number | null;
  scheduledAt?: string | null;
  endedAtLabel?: string | null;
  avgAccuracyPercent?: number | null;
};
export type HostedRoomsResponse = CursorPage<HostedRoomDto>;

/** POST /rooms/{roomId}/participants — 닉네임 1~12자, avatarId 없으면 서버 랜덤 */
export type JoinRoomRequest = { nickname: string; avatarId?: number | null };
/** participantToken은 게스트에게만 발급 */
export type JoinRoomResponse = { participantId: number; participantToken?: string | null; avatarId?: number | null };

export type ParticipantEntry = {
  participantId: number;
  nickname: string;
  avatarId?: number | null;
  isGuest?: boolean;
  isConnected?: boolean;
};
/** GET /rooms/{roomId}/participants — 초기 로딩·재접속 복구용 */
export type ParticipantsResponse = { participants?: ParticipantEntry[] };

export type RoomInfoHost = {
  userId?: number | null;
  nickname: string;
  level?: HostLevel | null;
  avgStars?: number | null;
  ratingCount?: number | null;
};
/** GET /rooms/pin/{pin} — 404 잘못된 PIN, 410 종료된 방 */
export type RoomInfoResponse = {
  roomId: number;
  pin: string;
  title: string;
  topic?: string | null;
  status?: RoomStatus | null;
  questionCount?: number | null;
  estimatedMinutes?: number | null;
  scheduledAt?: string | null;
  participantCount?: number | null;
  maxParticipants?: number | null;
  isPaid?: boolean;
  entryFee?: number | null;
  host?: RoomInfoHost | null;
};

/** GET /rooms/public 항목 */
export type PublicRoomDto = {
  roomId?: number;
  pin?: string;
  title?: string;
  topic?: string | null;
  hostId?: number | null;
  hostName?: string;
  hostLevel?: HostLevel | null;
  hostRating?: number | null;
  status?: RoomStatus | null;
  participantCount?: number | null;
  maxParticipants?: number | null;
  isPaid?: boolean;
  entryFee?: number | null;
  scheduledAt?: string | null;
};
export type PublicRoomPageResponse = CursorPage<PublicRoomDto>;
export type PublicRoomSort = "popular" | "upcoming";
export type PublicRoomType = "all" | "free" | "paid";
export type PublicRoomsQuery = { sort: PublicRoomSort; type: PublicRoomType; q?: string; cursor?: string };
```

- [ ] **Step 5: `dto/session.ts`**

```ts
import type { QuestionType, RoomStatus } from "./common";

/** 진행 문항 — 정답은 절대 포함하지 않는다 */
export type SnapshotQuestion = {
  questionId: number;
  questionNo: number;
  type?: QuestionType | null;
  body: string;
  choices?: string[] | null;
  points?: number;
  timeLimitSec?: number;
  /** ISO-8601. 남은 시간 = endsAt − 서버 ts */
  endsAt: string;
  isClosed?: boolean;
};
export type SnapshotAnswer = { questionId: number; correct?: boolean | null; earnedScore?: number | null; isProvisional?: boolean };
export type RankingEntry = { rank: number; participantId: number; nickname: string; avatarId?: number | null; total: number };

/** GET /rooms/{roomId}/session — 재접속 스냅샷. 404 = 세션 미시작(WAITING) */
export type SessionSnapshotResponse = {
  status?: RoomStatus | null;
  /** 서버 시각. 이 ts 이전 STOMP 프레임은 폐기 */
  ts: string;
  questionCount?: number | null;
  currentQuestion?: SnapshotQuestion | null;
  myAnswers?: SnapshotAnswer[];
  totalScore?: number | null;
  rank?: number | null;
  ranking?: RankingEntry[];
  isLocked?: boolean;
};

export type SubmissionChoice = { label?: string; count?: number };
export type SubmissionParticipant = { participantId?: number; nickname?: string; avatarId?: number | null; submitted?: boolean };
/** GET /rooms/{roomId}/session/current/submissions (호스트) */
export type SubmissionsResponse = {
  questionNo?: number;
  submittedCount?: number;
  totalCount?: number;
  accuracyPercent?: number | null;
  choices?: SubmissionChoice[] | null;
  participants?: SubmissionParticipant[];
};

/** POST /rooms/{roomId}/session/start — false면 이 세션 서술형 AI 분석 SKIPPED(FR-062) */
export type StartSessionResponse = { aiAnalysisEnabled?: boolean };
/** PUT /rooms/{roomId}/session/lock */
export type ScreenLockRequest = { locked: boolean };

/** 객관식: 보기 원문, OX: "O"|"X", 서술형: 자유 텍스트 */
export type SubmitAnswerRequest = { content: string };
export type SubmitAnswerResponse = {
  correct?: boolean | null;
  baseScore?: number;
  speedBonus?: number;
  earnedScore?: number;
  totalScore?: number;
  rank?: number | null;
  rankDelta?: number | null;
  isProvisional?: boolean;
};

export type VoiceHintEntry = { hintId: number; questionNo: number; clipUrl: string; durationMs?: number };
/** GET /rooms/{roomId}/session/hints — 다시 듣기·재접속 복구 */
export type VoiceHintsResponse = { hints?: VoiceHintEntry[] };
```

- [ ] **Step 6: `dto/question-sets.ts`**

```ts
import type { CursorPage, QuestionType } from "./common";

/** GET /question-sets 항목 — status는 "CONFIRMED"만 확정으로 해석 */
export type QuestionSetDto = {
  setId?: number;
  title?: string;
  status?: string | null;
  questionCount?: number;
  usedCount?: number | null;
  lastUsedAt?: string | null;
  createdAt?: string | null;
};
export type QuestionSetsResponse = CursorPage<QuestionSetDto>;
export type QuestionSetStatusFilter = "CONFIRMED" | "DRAFT";

/* ── 아래는 전부 @draft — 계약 없음(KMP는 목록만 호출). tasks.md T027·T028·T076·T087 경로. 계약 도착 시 수정 ── */

/** @draft POST /question-sets/generate 요청 (FR-009·016) */
export type GenerateQuestionSetRequest = {
  topic: string;
  counts: { type: QuestionType; count: number }[];
  difficulty: "EASY" | "MEDIUM" | "HARD";
  materialFileId?: number | null;
};
/** @draft 문항 — 에디터가 보여줄 최소 필드 */
export type QuestionDraft = {
  questionId: number;
  questionNo: number;
  type: QuestionType;
  body: string;
  choices?: string[] | null;
  answer?: string | null;
  explanation?: string | null;
  points: number;
  timeLimitSec: number;
  origin: "AI" | "MANUAL" | "MATERIAL";
};
/** @draft GET /question-sets/{id} · generate 응답 */
export type QuestionSetDetailResponse = {
  setId: number;
  title: string;
  status: "DRAFT" | "CONFIRMED";
  questions: QuestionDraft[];
};
/** @draft PATCH /question-sets/{id} */
export type UpdateQuestionSetRequest = { title?: string; questions?: QuestionDraft[] };
/** @draft GET /me/ai-usage (FR-061) */
export type AiUsageResponse = { generationLeft: number; generationLimit: number; analysisLeft: number; analysisLimit: number };
/** @draft POST /materials 응답 */
export type MaterialUploadResponse = { materialFileId: number; fileName: string; extractedChars: number };
```

- [ ] **Step 7: `dto/results.ts`**

```ts
import type { QuestionType, RoomStatus } from "./common";

export type AnswerVerdict = "CORRECT" | "WRONG" | "INCORRECT" | "AI_ANALYZED" | "ANALYZED" | "AI_PENDING" | "PENDING";
/** SKIPPED = 세션 시작 시 aiAnalysisEnabled=false */
export type AiFeedbackStatus = "PENDING" | "DONE" | "FAILED" | "SKIPPED";

export type AiFeedbackDto = {
  status?: AiFeedbackStatus | null;
  coveredConcepts?: string[];
  missingConcepts?: string[];
  weaknesses?: string | null;
  improvement?: string | null;
  suggestedScore?: number | null;
};
export type HostReviewDto = { comment?: string; improvement?: string | null; adjustedScore?: number | null };

export type ResultQuestionDto = {
  questionId: number;
  questionNo: number;
  title?: string;
  type?: QuestionType | null;
  verdict?: AnswerVerdict | null;
  myAnswer?: string | null;
  correctAnswer?: string | null;
  explanation?: string | null;
  earnedScore?: number;
  aiFeedback?: AiFeedbackDto | null;
  hostReview?: HostReviewDto | null;
};
/** GET /rooms/{roomId}/results/me */
export type SessionResultResponse = {
  roomTitle?: string;
  rank?: number | null;
  totalScore?: number;
  correctCount?: number;
  questionCount?: number;
  canRate?: boolean;
  isGuest?: boolean;
  questions?: ResultQuestionDto[];
};
/** GET /rooms/{roomId}/reports/me */
export type LearningReportResponse = { accuracyPercent?: number; weakTopics?: string[]; improvementPoints?: string[] };

export type RoomReportSummary = {
  avgAccuracyPercent?: number | null;
  studentCount?: number;
  questionCount?: number;
  aiAnalysisCount?: number;
  avgScore?: number | null;
  topScore?: number | null;
};
export type RoomReportQuestion = {
  questionId?: number;
  questionNo?: number;
  title?: string;
  type?: QuestionType | null;
  accuracyPercent?: number | null;
  aiFeedbackCount?: number | null;
};
export type RoomReportStudent = {
  participantId?: number;
  nickname?: string;
  rank?: number | null;
  totalScore?: number;
  correctCount?: number;
  isGuest?: boolean;
};
/** GET /rooms/{roomId}/results (호스트) */
export type RoomReportResponse = {
  roomTitle?: string;
  pin?: string;
  status?: RoomStatus | null;
  dateLabel?: string | null;
  summary?: RoomReportSummary;
  questions?: RoomReportQuestion[];
  students?: RoomReportStudent[];
};

/** @draft — 계약 없음. tasks.md T070. 서술형 답안 목록(W-07 분석 패널) */
export type EssayAnswerDto = {
  answerId: number;
  participantId: number;
  nickname: string;
  content: string;
  aiFeedback?: AiFeedbackDto | null;
  hostReview?: HostReviewDto | null;
};
/** @draft GET /rooms/{roomId}/questions/{questionId}/answers */
export type EssayAnswersResponse = { answers: EssayAnswerDto[] };
/** @draft POST /answers/{answerId}/review (FR-034) */
export type HostReviewRequest = { comment: string; improvement?: string | null; adjustedScore?: number | null };
```

- [ ] **Step 8: `dto/me.ts`**

```ts
import type { HostLevel } from "./common";
import type { PublicRoomDto } from "./rooms";

export type MyPageSummary = { participationCount?: number; accuracyPercent?: number; avgRank?: number | null; trendText?: string | null; weakTopics?: string[] };
export type MyPageOngoing = { roomId: number; pin: string; title: string; hostNickname?: string | null; progressLabel?: string | null };
export type MyPageRoom = { roomId: number; title: string; dateLabel?: string; questionCount?: number; myScore?: number | null; myRank?: number | null; hasReport?: boolean };
/** GET /users/me/rooms/joined — 요약+진행 중+참여 방 (FR-032·033) */
export type MyPageResponse = { summary?: MyPageSummary; ongoing?: MyPageOngoing | null; rooms?: MyPageRoom[]; nextCursor?: string | null };

export type GradeStats = { participationCount?: number; avgAccuracyPercent?: number | null; roomCount?: number; totalStudents?: number; avgStars?: number | null; ratingCount?: number };
export type GradeCriterion = { label?: string; current?: number; target?: number; met?: boolean };
export type GradeNext = { level?: HostLevel; progressPercent?: number; criteria?: GradeCriterion[] };
/** GET /users/me/grade */
export type GradeResponse = { level?: HostLevel; achievedAt?: string | null; stats?: GradeStats; next?: GradeNext | null };

export type BadgeType = "FIRST_ROOM" | "ROOMS_10" | "STUDENTS_100" | "RATING_45" | "RATINGS_50" | "STREAK_30" | "FIRST_PAID_ROOM" | "AI_SETS_50";
export type BadgeDto = { type?: BadgeType; earned?: boolean; earnedAt?: string | null; progressCurrent?: number | null; progressTarget?: number | null };
/** GET /users/me/badges */
export type BadgesResponse = { items?: BadgeDto[] };

/** GET/PUT /users/me/notification-settings — 3종뿐(마케팅 없음, DESIGN_GAPS C-5) */
export type NotificationSettingsDto = { sessionStart?: boolean; ratingRequest?: boolean; settlementDone?: boolean };

/** GET /users/{userId}/profile — 호스트 공개 프로필 */
export type HostProfileResponse = {
  userId?: number;
  nickname?: string;
  intro?: string | null;
  level?: HostLevel | null;
  avgStars?: number | null;
  ratingCount?: number;
  roomCount?: number;
  totalStudents?: number;
  badges?: BadgeType[];
  rooms?: PublicRoomDto[];
};

export type ReportReason = "NICKNAME" | "QUESTION_ERROR" | "PAID_ROOM" | "OPERATION" | "SPAM" | "DIFFICULTY";
/** POST /reports — 게스트 익명 신고 가능 */
export type ReportRequest = { targetType: "USER" | "ROOM" | "QUESTION"; targetId: number; reason: ReportReason; detail?: string | null };
/** POST /guest-records/claim — 가입 후 7일 내, 경과 시 410 RECORD_PURGED */
export type ClaimGuestRecordRequest = { participantId: number };
```

- [ ] **Step 9: `dto/payments.ts`**

```ts
import type { CursorPage, PaymentMethod } from "./common";

export type CoinTransactionType = "CHARGE" | "DEDUCT" | "REFUND";
/** amount는 부호 포함(충전·환급 +, 차감 −) */
export type CoinTransactionDto = {
  id?: number;
  type?: CoinTransactionType | null;
  amount?: number;
  balanceAfter?: number;
  method?: PaymentMethod | null;
  roomTitle?: string | null;
  paymentNo?: string | null;
  createdAt?: string | null;
};
/** GET /users/me/coins */
export type CoinBalanceResponse = { balance?: number; defaultMethod?: PaymentMethod | null; recent?: CoinTransactionDto | null };
/** GET /users/me/coins/transactions */
export type CoinTransactionPageResponse = CursorPage<CoinTransactionDto>;

/** POST /coins/charges — roomId 있으면 충전 후 바로 차감할 방 */
export type CreateChargeRequest = { amount: number; method: PaymentMethod; roomId?: number | null };
/** 포트원 V2 결제창 파라미터 — PortOne.requestPayment에 그대로(amount→totalAmount) */
export type ChargeCheckoutResponse = {
  chargeId?: string;
  storeId?: string;
  channelKey?: string;
  paymentId?: string;
  orderName?: string;
  amount?: number;
  currency?: string;
  payMethod?: string;
};
/** POST /coins/charges/{chargeId}/confirm */
export type ConfirmChargeRequest = { paymentId: string; roomId?: number | null };
export type ConfirmChargeResponse = { balance?: number; entryPayment?: EntryPaymentResponse | null };

/** POST /rooms/{roomId}/entry-payments — 참가비 코인 차감. 402 잔액 부족 */
export type CreateEntryPaymentRequest = { nickname: string; avatarId?: number | null };
export type EntryPaymentResponse = { paymentNo?: string; balance?: number };

export type SettlementStatus = "SCHEDULED" | "PAID" | "HELD";
export type SettlementItemDto = {
  settlementId?: number;
  dateLabel?: string;
  roomTitle?: string;
  participantCount?: number;
  entryFeeTotal?: number;
  feeAmount?: number;
  payoutAmount?: number;
  status?: SettlementStatus | null;
};
export type EarningsNextPayout = { dateLabel?: string; amount?: number };
export type EarningsAccount = { bankName?: string; maskedNumber?: string; payoutNote?: string | null };
/** GET /users/me/earnings — 수익·정산 요약+내역 */
export type EarningsResponse = CursorPage<SettlementItemDto> & {
  monthlyTotal?: number;
  hostSharePercent?: number;
  nextPayout?: EarningsNextPayout | null;
  paidRoomCount?: number;
  studentCount?: number;
  account?: EarningsAccount | null;
};

/** GET/PUT /users/me/settlement-account — 미등록이면 GET 404 */
export type SettlementAccountDto = { bankName?: string; accountNumber?: string; holderName?: string };
/** PUT /users/me/payment-method */
export type PaymentMethodRequest = { method: PaymentMethod };
```

- [ ] **Step 10: `dto/ratings.ts`**

```ts
export type RatingTag = "CLEAR_EXPLANATION" | "GOOD_DIFFICULTY" | "GOOD_PACING" | "HELPFUL_HINTS" | "GOOD_QUALITY";
/** POST /rooms/{roomId}/ratings — 세션당 1회. 409 ALREADY_RATED, 410 평가 기간(24h) 경과 */
export type SubmitRatingRequest = { stars: 1 | 2 | 3 | 4 | 5; tags: RatingTag[]; comment?: string | null };
```

- [ ] **Step 11: `dto.ts`를 허브로 교체**

```ts
/**
 * REST 계약과 1:1인 DTO. 도메인별 파일은 `./dto/*`, 여기는 re-export 허브다.
 * 계약 원천: KMP `shared` DTO(2026-08-28 백엔드 명세 정합) — `@draft` 표시는 계약이 아직 없는 초안.
 */
export * from "./dto/common";
export * from "./dto/auth";
export * from "./dto/rooms";
export * from "./dto/session";
export * from "./dto/question-sets";
export * from "./dto/results";
export * from "./dto/me";
export * from "./dto/payments";
export * from "./dto/ratings";
export * from "./dto/admin";
```

주의: `dto/admin.ts`에 `RoomKind`·`RoomStatus`(관리자용 `"WAITING"|"RUNNING"|"FINISHED"`)·`QuestionFormat`·`SettlementStatus`(`"CONFIRMED"|"ACCOUNT_MISSING"`)·`PaymentStatus`가 있어 **이름이 충돌**한다. 관리자 파일은 수정 금지이므로, 허브에서 `export * from "./dto/admin"` 대신 명시 re-export로 충돌 이름을 `Admin` 접두어로 별칭한다:

```ts
export type {
  AdminDashboardResponse, AdminDashboardKpis, DailySessionCount, AdminActivityType, AdminActivity, UserComposition, PopularTopic, SystemHealth, SystemComponentStatus,
  AdminUserFilter, AdminUserStatus, AdminUserSummary, AdminUsersResponse,
  AdminRoomsResponse, AdminRoomSummaryCounts, RoomKind, AdminRoomSummary, AdminReviewQueueResponse, QuestionFormat, QuestionDifficulty, QuestionReviewStatus, AdminReviewQuestion,
  AdminReportsResponse, AdminReportKpis, ReportTargetKind, ReportType, ReportStatus, AdminReport, AdminSanctionsResponse, SanctionType, SanctionStatus, AdminSanction,
  AdminPaymentsResponse, AdminPaymentKpis, PaymentStatus, AdminPayment, AdminSettlementsResponse, AdminSettlement,
  AdminAdCampaignsResponse, AdminAdKpis, AdPlacement, AdCampaignStatus, AdCampaign, AdminBrandedQuizzesResponse, BrandedQuizPurpose, BrandedQuizStatus, BrandedQuiz,
} from "./dto/admin";
export type { RoomStatus as AdminRoomStatus, SettlementStatus as AdminSettlementStatus } from "./dto/admin";
```

그 다음 `features/admin`·`lib/mocks/admin.ts`·`lib/api/admin.ts`·`lib/queries/use-admin-*`에서 `RoomStatus`/`SettlementStatus`를 import하는 곳은 **import 경로만** `@/lib/types/dto/admin`으로 바꾼다(타입 내용 불변 — `grep -rn "RoomStatus\|SettlementStatus" src/features/admin src/lib/mocks/admin.ts`로 찾는다).

- [ ] **Step 12: `events.ts`**

```ts
import type { QuestionType, RankingEntry } from "./dto";

/** STOMP 프레임 envelope. type·ts 없으면 폐기 */
export type ServerEventFrame = { type: string; ts: string; data?: unknown };

/** 계약 이벤트 19종 — type 문자열은 KMP 코드 기준(SESSION_STARTED/SESSION_ENDED/HINT_PUBLISHED) */
export type ServerEvent =
  | { type: "PARTICIPANT_JOINED"; ts: string; data: { participantId: number; nickname: string; isGuest: boolean; avatarId?: number | null; count: number } }
  | { type: "PARTICIPANT_LEFT"; ts: string; data: { participantId: number; count: number; reason?: "LEFT" | "KICKED" | null } }
  | { type: "SESSION_STARTED"; ts: string; data: { sessionId: number; questionCount: number } }
  | { type: "QUESTION_STARTED"; ts: string; data: { questionId: number; questionNo: number; type: QuestionType; body: string; choices?: string[] | null; points: number; timeLimitSec: number; endsAt: string } }
  | { type: "ANSWER_SUBMITTED"; ts: string; data: { questionNo: number; submittedCount: number; totalCount: number } }
  | { type: "QUESTION_ENDED"; ts: string; data: { questionNo: number; answerReveal: { answer?: string | null; explanation?: string | null }; correctCount: number } }
  | { type: "SCORE_UPDATED"; ts: string; data: { questionNo: number; scores: { participantId: number; delta: number; total: number }[] } }
  | { type: "RANKING_UPDATED"; ts: string; data: { ranking: RankingEntry[] } }
  | { type: "SCREEN_LOCKED"; ts: string; data: { locked: boolean } }
  | { type: "HINT_PUBLISHED"; ts: string; data: { hintId: number; questionNo: number; clipUrl: string; durationMs: number } }
  | { type: "SESSION_ENDED"; ts: string; data: { sessionId: number; finalRanking: RankingEntry[]; reportReady?: boolean } }
  | { type: "REPORT_READY"; ts: string; data: { sessionId: number } }
  | { type: "ROOM_CANCELLED"; ts: string; data: { reason?: string | null } }
  | { type: "FEEDBACK_READY"; ts: string; data: { answerId: number; questionNo: number } }
  | { type: "FEEDBACK_FAILED"; ts: string; data: { answerId: number; questionNo: number } }
  | { type: "REVIEW_RECEIVED"; ts: string; data: { answerId: number; questionNo: number } }
  | { type: "SUBMISSION_UPDATED"; ts: string; data: { questionNo: number; submittedCount: number; totalCount: number } }
  | { type: "PROJECTOR_CONNECTED"; ts: string; data: Record<string, never> }
  | { type: "PROJECTOR_DISCONNECTED"; ts: string; data: Record<string, never> };

export type ServerEventType = ServerEvent["type"];

export const SERVER_EVENT_TYPES: readonly ServerEventType[] = [
  "PARTICIPANT_JOINED", "PARTICIPANT_LEFT", "SESSION_STARTED", "QUESTION_STARTED", "ANSWER_SUBMITTED", "QUESTION_ENDED",
  "SCORE_UPDATED", "RANKING_UPDATED", "SCREEN_LOCKED", "HINT_PUBLISHED", "SESSION_ENDED", "REPORT_READY", "ROOM_CANCELLED",
  "FEEDBACK_READY", "FEEDBACK_FAILED", "REVIEW_RECEIVED", "SUBMISSION_UPDATED", "PROJECTOR_CONNECTED", "PROJECTOR_DISCONNECTED",
];

/** 알 수 없는 type·ts 누락은 null (폐기). data 없으면 {} */
export function parseServerEvent(raw: unknown): ServerEvent | null {
  if (typeof raw !== "object" || raw === null) return null;
  const frame = raw as Partial<ServerEventFrame>;
  if (typeof frame.type !== "string" || typeof frame.ts !== "string") return null;
  if (!SERVER_EVENT_TYPES.includes(frame.type as ServerEventType)) return null;
  return { type: frame.type, ts: frame.ts, data: frame.data ?? {} } as ServerEvent;
}
```

- [ ] **Step 13: 검증·커밋** — `pnpm exec tsc --noEmit` 0, `pnpm test` 9 PASS, `pnpm lint` 0.

```bash
pnpm format
git add src/lib/types/dto.ts src/lib/types/dto src/lib/types/events.ts src/lib/api/client.ts src/lib/mocks/auth.ts $(git diff --name-only src/features/admin src/lib/mocks/admin.ts src/lib/api/admin.ts src/lib/queries)
git commit -m "feat: DTO를 도메인별 lib/types/dto/*로 분할하고 KMP 계약 1:1 타입(rooms·session·question-sets·results·me·payments·ratings) + STOMP 이벤트 19종 유니온 추가 — 계약 없는 호출은 @draft 표시"
```

---

### Task 3: 목 레이어 — 경로 파라미터 라우터 · 공용 픽스처 · 도메인 목 응답

**Files:**
- Create: `src/lib/mocks/router.ts`, `src/lib/mocks/fixtures.ts`, `src/lib/mocks/rooms.ts`, `src/lib/mocks/session.ts`, `src/lib/mocks/question-sets.ts`, `src/lib/mocks/results.ts`, `src/lib/mocks/me.ts`, `src/lib/mocks/payments.ts`
- Modify: `src/lib/mocks/handlers.ts`, `src/lib/mocks/auth.ts`, `src/lib/api/client.ts`(`resolveMock` 시그니처)
- Test: `src/lib/mocks/router.test.ts`, `src/lib/mocks/handlers.test.ts`

**Interfaces:**
- Produces: `resolveMock<T>(method: string, url: string, body?: unknown): Promise<T>`; `type MockHandler = (ctx: MockContext) => unknown`; `MockContext = { params: Record<string, string>; url: URL; body?: unknown }`; `MOCK_ROUTES: readonly string[]`(키 목록).
- 목 값의 출처: 화면이 지금 보여주는 값과 같아야 한다. `features/home/mock.ts`(인기 방)·`features/host/mock.ts`(세트·진행 문항·리포트)·`features/host/my-rooms/mock.ts`(내가 만든 방·등급)·`features/me/mock.ts`(프로필·코인·계좌)·`features/me/{coins,joined,notifications,payment-methods,settlement}/mock.ts`·`features/participant/pay/mock.ts`(유료 방·잔액)를 열어 DTO 모양으로 옮긴다.

- [ ] **Step 1: `router.test.ts` (실패 테스트)**

```ts
import { describe, expect, it } from "vitest";
import { matchMockRoute } from "./router";

describe("mocks/router", () => {
  const table = ["GET /rooms/pin/:pin", "POST /rooms/:roomId/participants", "GET /users/me"];

  it("경로 파라미터를 뽑는다", () => {
    expect(matchMockRoute(table, "POST", "/rooms/12/participants")).toEqual({
      key: "POST /rooms/:roomId/participants",
      params: { roomId: "12" },
    });
  });
  it("고정 경로는 정확히 일치해야 한다", () => {
    expect(matchMockRoute(table, "GET", "/users/me")).toEqual({ key: "GET /users/me", params: {} });
    expect(matchMockRoute(table, "GET", "/users/me/extra")).toBeNull();
    expect(matchMockRoute(table, "DELETE", "/users/me")).toBeNull();
  });
});
```

- [ ] **Step 2: `router.ts`**

```ts
export type MockContext = { params: Record<string, string>; url: URL; body?: unknown };
export type MockHandler = (ctx: MockContext) => unknown;
export type MockMatch = { key: string; params: Record<string, string> };

/** "METHOD /a/:b/c" 표에서 첫 일치를 찾는다. 세그먼트 수가 같고 `:name`은 아무 값. */
export function matchMockRoute(keys: readonly string[], method: string, pathname: string): MockMatch | null {
  const target = pathname.split("/").filter(Boolean);

  for (const key of keys) {
    const [keyMethod, keyPath] = key.split(" ");
    if (keyMethod !== method) continue;

    const pattern = (keyPath ?? "").split("/").filter(Boolean);
    if (pattern.length !== target.length) continue;

    const params: Record<string, string> = {};
    const matched = pattern.every((seg, i) => {
      if (seg.startsWith(":")) {
        params[seg.slice(1)] = decodeURIComponent(target[i]);
        return true;
      }
      return seg === target[i];
    });
    if (matched) return { key, params };
  }
  return null;
}
```

- [ ] **Step 3: 통과 확인** — `pnpm test src/lib/mocks/router.test.ts` → 2 PASS.

- [ ] **Step 4: `fixtures.ts`** — 여러 도메인 목이 공유하는 값. 정확한 문구·숫자는 위 출처 파일에서 복사한다.

```ts
import type { HostedRoomDto, ParticipantEntry, PublicRoomDto, QuestionSetDto, RoomInfoResponse, SnapshotQuestion, UserProfileResponse } from "@/lib/types/dto";

/** 로그인 회원 = 이한결(여우, Lv.3). 시안 C-02 v3 */
export const ME_USER_ID = 1;
export const ME_PROFILE: UserProfileResponse = {
  nickname: "한결", email: "hangyeol@passmate.app", joinedAt: "2026-08-01", avatarId: 6,
  level: 3, coins: 1200, joinedRoomCount: 3, hostedRoomCount: 24,
};

/** 시연 방 — 웹 라우트 sample "DEMO01"은 이 PIN으로 바뀐다 */
export const DEMO_ROOM_ID = 1;
export const DEMO_PIN = "482913";
export const DEMO_ROOM: RoomInfoResponse = {
  roomId: DEMO_ROOM_ID, pin: DEMO_PIN, title: "Spring 실전 모의고사 4주차", topic: "백엔드", status: "WAITING",
  questionCount: 8, estimatedMinutes: 40, scheduledAt: "2026-08-28T20:00:00+09:00",
  participantCount: 24, maxParticipants: 40, isPaid: true, entryFee: 10000,
  host: { userId: 42, nickname: "김민지", level: 4, avgStars: 4.5, ratingCount: 312 },
};

/** 아바타 12종 ↔ avatarId 1..12 (components/common/student-avatar.tsx AVATAR_KEYS 순서) */
export const PARTICIPANTS: ParticipantEntry[] = [
  { participantId: 11, nickname: "영희", avatarId: 1, isGuest: true },
  /* features/host/mock.ts LiveRoom.students 를 그대로 옮긴다 (participantId 11부터 증가) */
];

export const HOSTED_ROOMS: HostedRoomDto[] = [/* features/host/my-rooms/mock.ts MY_ROOMS → roomId 100+, status live→RUNNING, ended→FINISHED, averageScore→avgAccuracyPercent, endedLabel→endedAtLabel */];
export const PUBLIC_ROOMS: PublicRoomDto[] = [/* features/home/mock.ts POPULAR_ROOMS → hostName=host, hostLevel=level, participantCount=participants, isPaid=type==="paid" */];
export const QUESTION_SETS: QuestionSetDto[] = [/* features/host/mock.ts QUESTION_SETS → setId, title, status:"CONFIRMED", questionCount, usedCount=usage.count, lastUsedAt=usage.lastUsed */];
export const LIVE_QUESTIONS: SnapshotQuestion[] = [/* features/host/mock.ts LiveQuestion → questionId=questionNo, type multiple→MULTIPLE_CHOICE·ox→OX·essay→ESSAY, body=prompt, choices=choices.map(c=>c.text), timeLimitSec=seconds, endsAt는 호출 시점 계산 */];
```

- [ ] **Step 5: 도메인 목 파일** — 각 함수는 DTO를 돌려준다. 상태가 필요한 것(입장 인원·잔액·세션 phase)은 **모듈 스코프 변수**로 유지해 한 세션 동안 일관되게 보인다.

`rooms.ts`
```ts
export function mockRoomByPin(pin: string): RoomInfoResponse            // pin !== DEMO_PIN이면 throw new AppError("NotFound")
export function mockCreateRoom(body: CreateRoomRequest): CreateRoomResponse   // isPaid && ME_PROFILE.level < 3 → throw new AppError("PermissionDenied",{code:"HOST_LEVEL_REQUIRED"}); 새 roomId 증가, pin 6자리 랜덤, HOSTED_ROOMS 앞에 push
export function mockHostedRooms(): HostedRoomsResponse
export function mockPublicRooms(url: URL): PublicRoomPageResponse       // type=free|paid 필터, q 부분 일치
export function mockJoinRoom(roomId: string, body: JoinRoomRequest): JoinRoomResponse   // 닉네임 중복 → AppError("Conflict",{code:"NICKNAME_TAKEN"}); 회원(access 토큰 있음)이면 participantToken 없음, 없으면 "mock-guest-token"
export function mockParticipants(): ParticipantsResponse
export function mockLeaveRoom(): undefined
```

`session.ts` — phase 상태 머신(WAITING→RUNNING→FINISHED), 현재 문항 인덱스, 제출 수. `mockSnapshot()`은 WAITING이면 `throw new AppError("NotFound")`(계약: 404=미시작). `mockStartSession()`→`{aiAnalysisEnabled:true}`, `mockNext()`(인덱스+1, endsAt=now+timeLimitSec), `mockEndCurrent()`, `mockEndSession()`, `mockLock(body)`, `mockSubmissions()`(features/host/mock.ts QuestionResult.distribution → choices), `mockSubmitAnswer(body)`(정답이면 earnedScore=points×1.25 아니면 0, 서술형은 isProvisional), `mockHints()`(features/participant/mock.ts VOICE_HINT), `mockUploadHint(form)`.

`question-sets.ts` — `mockQuestionSets(url)`, 그리고 `@draft` 4개: `mockGenerate(body)`(1.5초 지연 후 LIVE_QUESTIONS 기반 QuestionSetDetailResponse status DRAFT), `mockQuestionSetDetail(id)`, `mockUpdateQuestionSet`, `mockConfirmQuestionSet`, `mockCloneQuestionSet`, `mockAiUsage()`→`{generationLeft:3, generationLimit:5, analysisLeft:5, analysisLimit:5}`, `mockUploadMaterial`.

`results.ts` — `mockMyResult(roomId)`(features/host/mock.ts ReportQuestion·EssayAnswer 기반), `mockMyReport()`, `mockRoomReport(roomId)`(SessionReport → RoomReportResponse), `@draft` `mockEssayAnswers`, `mockPostReview`.

`me.ts` — `mockMe()`(ME_PROFILE + `role:"ADMIN"`·`userId:ME_USER_ID` — 관리자 화면 확인용으로 role은 유지), `mockUpdateProfile(body)`(ME_PROFILE 갱신), `mockDeleteMe()`, `mockMyPage()`(features/me/joined/mock.ts ACTIVE_SESSION → ongoing, features/home·me 참여 방 목록 → rooms), `mockGrade()`(features/host/my-rooms/mock.ts LEVEL_STATUS·PROMOTION → level 3, next level 4 progress 72, criteria = PROMOTION.rules), `mockBadges()`(8종, features/me/achievement-badge 기준 earned 4개), `mockNotificationSettings`/`mockPutNotificationSettings`, `mockHostProfile(userId)`, `mockReport()`, `mockClaim()`.

`payments.ts` — 잔액 변수 `balance = 1200`. `mockCoinBalance()`, `mockCoinTransactions()`(features/me/coins/mock.ts COIN_HISTORY → type은 amount>0?CHARGE:DEDUCT), `mockCreateCharge(body)`(chargeId "chg-<n>", storeId "store-mock", channelKey "channel-mock", paymentId "PM-<n>", orderName `패스메이트 코인 ${amount.toLocaleString("ko-KR")} C 충전`), `mockConfirmCharge(chargeId, body)`(balance += amount, roomId 있으면 entry 차감), `mockEntryPayment(roomId, body)`(balance < entryFee → `throw new AppError("PaymentRequired",{code:"PAYMENT_REQUIRED"})`), `mockEarnings()`(features/me/settlement/mock.ts SETTLEMENT_ROWS·SETTLEMENT_STATS → items status scheduled→SCHEDULED·paid→PAID), `mockSettlementAccount()`/`mockPutSettlementAccount`, `mockPutPaymentMethod`.

`auth.ts` 수정 — `MOCK_ME: MeResponse = { ...ME_PROFILE, userId: ME_USER_ID, role: "ADMIN" }`. `MOCK_TOKENS` 유지.

- [ ] **Step 6: `handlers.ts` 교체** — 표는 `Record<string, MockHandler>`; 기존 관리자 11개 키는 그대로 두고(handler 시그니처만 `(ctx) => mockAdminUsers(ctx.url.searchParams.get("filter"))`처럼 감쌈) 아래를 추가한다. `resolveMock`은 `matchMockRoute(Object.keys(HANDLERS), method, parsed.pathname)`으로 찾고 `MOCK_ROUTES = Object.keys(HANDLERS)`를 export.

```
POST /auth/logout · GET /users/me · PUT /users/me · DELETE /users/me
GET /rooms/pin/:pin · POST /rooms · GET /users/me/rooms/hosted · GET /rooms/public
POST /rooms/:roomId/participants · GET /rooms/:roomId/participants · DELETE /rooms/:roomId/participants/me
GET /rooms/:roomId/session · POST /rooms/:roomId/session/start · POST /rooms/:roomId/session/next
POST /rooms/:roomId/session/current/end · POST /rooms/:roomId/session/end · PUT /rooms/:roomId/session/lock
GET /rooms/:roomId/session/current/submissions · POST /rooms/:roomId/session/questions/:questionId/answers
GET /rooms/:roomId/session/hints · POST /rooms/:roomId/session/hints
GET /question-sets · POST /question-sets/generate · POST /question-sets · GET /question-sets/:id · PATCH /question-sets/:id
POST /question-sets/:id/confirm · POST /question-sets/:id/clone · GET /me/ai-usage · POST /materials
GET /rooms/:roomId/results/me · GET /rooms/:roomId/reports/me · GET /rooms/:roomId/results
GET /rooms/:roomId/questions/:questionId/answers · POST /answers/:answerId/review
POST /rooms/:roomId/ratings
GET /users/me/rooms/joined · GET /users/me/grade · GET /users/me/badges
GET /users/me/notification-settings · PUT /users/me/notification-settings
GET /users/:userId/profile · POST /reports · POST /guest-records/claim
GET /users/me/coins · GET /users/me/coins/transactions · POST /coins/charges · POST /coins/charges/:chargeId/confirm
POST /rooms/:roomId/entry-payments · GET /users/me/earnings
GET /users/me/settlement-account · PUT /users/me/settlement-account · PUT /users/me/payment-method
```

`client.ts`의 `request()`는 `resolveMock<T>(method, url, options.body)`로 body를 넘긴다(`requestMultipart`는 form).

- [ ] **Step 7: `handlers.test.ts`** — 위 표의 모든 키가 샘플 URL로 해석되는지(`MOCK_ROUTE_MISSING`이 아닌지) + 대표 응답 모양 3개.

```ts
import { describe, expect, it } from "vitest";
import { MOCK_ROUTES, resolveMock } from "./handlers";

const SAMPLE: Record<string, string> = { ":pin": "482913", ":roomId": "1", ":questionId": "1", ":id": "1", ":answerId": "1", ":userId": "42", ":chargeId": "chg-1" };

describe("mocks/handlers", () => {
  it("표의 모든 라우트가 샘플 URL로 해석된다", async () => {
    for (const key of MOCK_ROUTES) {
      const [method, path] = key.split(" ");
      const url = path.replace(/:[a-zA-Z]+/g, (m) => SAMPLE[m] ?? "1") + (path === "/rooms/public" ? "?sort=popular&type=all" : "");
      await expect(resolveMock(method, url, {})).resolves.not.toBeUndefined().catch(() => undefined);
      // 204 계열은 undefined를 돌려주므로 위 체인 대신: 예외 code가 MOCK_ROUTE_MISSING이 아니면 통과
    }
  });
  it("GET /rooms/pin/482913 은 시연 방을 돌려준다", async () => {
    await expect(resolveMock("GET", "/rooms/pin/482913")).resolves.toMatchObject({ roomId: 1, pin: "482913" });
  });
  it("모르는 PIN은 NotFound", async () => {
    await expect(resolveMock("GET", "/rooms/pin/000000")).rejects.toMatchObject({ kind: "NotFound" });
  });
  it("WAITING 상태의 스냅샷은 NotFound(404=미시작)", async () => {
    await expect(resolveMock("GET", "/rooms/1/session")).rejects.toMatchObject({ kind: "NotFound" });
  });
});
```

첫 테스트는 `try { await resolveMock(...) } catch (e) { expect((e as {code?:string}).code).not.toBe("MOCK_ROUTE_MISSING") }` 형태로 쓴다(응답이 `undefined`인 라우트가 있으므로).

- [ ] **Step 8: 검증·커밋** — `pnpm test` 전부 PASS, `tsc` 0.

```bash
pnpm format && pnpm lint
git add src/lib/mocks src/lib/api/client.ts
git commit -m "feat: 목 레이어 확장 — 경로 파라미터 라우터, 공용 픽스처(회원·시연 방 482913·세트), rooms·session·question-sets·results·me·payments 목 응답 55개 라우트"
```

---

### Task 4: 도메인 api 함수 (전송만 — DTO 반환·AppError 변환)

**Files:**
- Modify: `src/lib/api/auth.ts`
- Create: `src/lib/api/rooms.ts`, `src/lib/api/sessions.ts`, `src/lib/api/question-sets.ts`, `src/lib/api/results.ts`, `src/lib/api/me.ts`, `src/lib/api/payments.ts`, `src/lib/api/ratings.ts`
- Modify: `src/lib/queries/use-restore-session.ts`(`getMe` 경로 변경 반영 — 시그니처 동일이라 수정 없음, 확인만)

**Interfaces:**
- Produces(전부 `Promise<DTO>`): 아래 함수명·인자. Task 5의 훅이 그대로 부른다.

- [ ] **Step 1: `auth.ts`**

```ts
import { API_BASE_URL } from "@/lib/env";
import type { MeResponse, TokenRefreshRequest, TokenRefreshResponse } from "@/lib/types/dto";
import { request } from "./client";

/** POST /auth/refresh */
export function refreshTokens(refreshToken: string): Promise<TokenRefreshResponse> {
  const body: TokenRefreshRequest = { refreshToken };
  return request<TokenRefreshResponse>("/auth/refresh", { method: "POST", body });
}
/** GET /users/me — 내 프로필(userId·role은 @draft) */
export function getMe(): Promise<MeResponse> {
  return request<MeResponse>("/users/me");
}
/** POST /auth/logout — refresh 무효화. 실패해도 로컬 로그아웃은 진행한다 */
export function logout(): Promise<void> {
  return request<void>("/auth/logout", { method: "POST" });
}
/** @draft — 계약은 client=mobile 뿐. 웹은 client=web + /auth/callback 리다이렉트를 요청(DESIGN_GAPS D-1) */
export function googleLoginUrl(next: string): string {
  const callback = `${window.location.origin}/auth/callback?next=${encodeURIComponent(next)}`;
  return `${API_BASE_URL}/auth/oauth/google?client=web&redirect=${encodeURIComponent(callback)}`;
}
```

`use-restore-session.ts`의 `restoreSession()`은 `tokens.refreshToken`이 있을 때만 `writeRefreshToken` 하도록 고친다.

- [ ] **Step 2: `rooms.ts`**

```ts
export function getRoomByPin(pin: string): Promise<RoomInfoResponse>                      // GET /rooms/pin/{pin}
export function createRoom(body: CreateRoomRequest): Promise<CreateRoomResponse>          // POST /rooms
export function getHostedRooms(cursor?: string): Promise<HostedRoomsResponse>             // GET /users/me/rooms/hosted
export function getPublicRooms(query: PublicRoomsQuery): Promise<PublicRoomPageResponse>  // GET /rooms/public?sort&type&q&cursor (q 공백이면 생략)
export function joinRoom(roomId: number, body: JoinRoomRequest): Promise<JoinRoomResponse>   // POST /rooms/{roomId}/participants
export function getParticipants(roomId: number): Promise<ParticipantsResponse>           // GET /rooms/{roomId}/participants
export function leaveRoom(roomId: number): Promise<void>                                  // DELETE /rooms/{roomId}/participants/me
```

- [ ] **Step 3: `sessions.ts`**

```ts
export function getSessionSnapshot(roomId: number): Promise<SessionSnapshotResponse>      // GET /rooms/{roomId}/session (404=WAITING은 호출자가 처리)
export function startSession(roomId: number): Promise<StartSessionResponse>               // POST .../session/start
export function nextQuestion(roomId: number): Promise<void>                               // POST .../session/next
export function endCurrentQuestion(roomId: number): Promise<void>                         // POST .../session/current/end
export function endSession(roomId: number): Promise<void>                                 // POST .../session/end
export function lockScreen(roomId: number, locked: boolean): Promise<void>                // PUT .../session/lock {locked}
export function getSubmissions(roomId: number): Promise<SubmissionsResponse>              // GET .../session/current/submissions
export function submitAnswer(roomId: number, questionId: number, content: string): Promise<SubmitAnswerResponse>  // POST .../session/questions/{questionId}/answers
export function getVoiceHints(roomId: number): Promise<VoiceHintsResponse>                // GET .../session/hints
export function uploadVoiceHint(roomId: number, clip: Blob, durationMs: number, fileName = "hint.webm"): Promise<VoiceHintEntry>
// FormData: form.append("audio", clip, fileName); form.append("durationMs", String(durationMs)); requestMultipart(`/rooms/${roomId}/session/hints`, form)
```

- [ ] **Step 4: `question-sets.ts`** — 목록은 계약, 나머지는 `@draft`(tasks.md T027·T028·T076·T087)

```ts
export function getQuestionSets(status?: QuestionSetStatusFilter, cursor?: string): Promise<QuestionSetsResponse>   // GET /question-sets
export function generateQuestionSet(body: GenerateQuestionSetRequest): Promise<QuestionSetDetailResponse>           // @draft POST /question-sets/generate
export function getQuestionSet(setId: number): Promise<QuestionSetDetailResponse>                                    // @draft GET /question-sets/{id}
export function updateQuestionSet(setId: number, body: UpdateQuestionSetRequest): Promise<QuestionSetDetailResponse> // @draft PATCH
export function confirmQuestionSet(setId: number): Promise<QuestionSetDetailResponse>                                // @draft POST /{id}/confirm
export function cloneQuestionSet(setId: number): Promise<QuestionSetDetailResponse>                                  // @draft POST /{id}/clone
export function getAiUsage(): Promise<AiUsageResponse>                                                                // @draft GET /me/ai-usage
export function uploadMaterial(file: File): Promise<MaterialUploadResponse>                                           // @draft POST /materials (multipart part "file")
```

- [ ] **Step 5: `results.ts`**

```ts
export function getMyResult(roomId: number): Promise<SessionResultResponse>       // GET /rooms/{roomId}/results/me
export function getMyReport(roomId: number): Promise<LearningReportResponse>      // GET /rooms/{roomId}/reports/me
export function getRoomReport(roomId: number): Promise<RoomReportResponse>        // GET /rooms/{roomId}/results
export function getEssayAnswers(roomId: number, questionId: number): Promise<EssayAnswersResponse>   // @draft
export function postHostReview(answerId: number, body: HostReviewRequest): Promise<void>            // @draft POST /answers/{id}/review
```

- [ ] **Step 6: `me.ts`**

```ts
export function updateProfile(body: UpdateProfileRequest): Promise<void>          // PUT /users/me
export function deleteMe(): Promise<void>                                          // DELETE /users/me
export function getMyPage(cursor?: string): Promise<MyPageResponse>                // GET /users/me/rooms/joined
export function getGrade(): Promise<GradeResponse>                                 // GET /users/me/grade
export function getBadges(): Promise<BadgesResponse>                               // GET /users/me/badges
export function getNotificationSettings(): Promise<NotificationSettingsDto>        // GET /users/me/notification-settings
export function putNotificationSettings(body: NotificationSettingsDto): Promise<void>
export function getHostProfile(userId: number): Promise<HostProfileResponse>       // GET /users/{userId}/profile
export function postReport(body: ReportRequest): Promise<void>                     // POST /reports
export function claimGuestRecord(participantId: number): Promise<void>            // POST /guest-records/claim
```

- [ ] **Step 7: `payments.ts`**

```ts
export function getCoinBalance(): Promise<CoinBalanceResponse>                                        // GET /users/me/coins
export function getCoinTransactions(cursor?: string): Promise<CoinTransactionPageResponse>            // GET /users/me/coins/transactions
export function createCharge(body: CreateChargeRequest): Promise<ChargeCheckoutResponse>              // POST /coins/charges
export function confirmCharge(chargeId: string, body: ConfirmChargeRequest): Promise<ConfirmChargeResponse>  // POST /coins/charges/{chargeId}/confirm
export function createEntryPayment(roomId: number, body: CreateEntryPaymentRequest): Promise<EntryPaymentResponse>  // POST /rooms/{roomId}/entry-payments
export function getEarnings(cursor?: string): Promise<EarningsResponse>                               // GET /users/me/earnings
export function getSettlementAccount(): Promise<SettlementAccountDto>                                 // GET /users/me/settlement-account (404=미등록)
export function putSettlementAccount(body: SettlementAccountDto): Promise<void>
export function putPaymentMethod(method: PaymentMethod): Promise<void>                                // PUT /users/me/payment-method {method}
```

- [ ] **Step 8: `ratings.ts`**

```ts
export function submitRating(roomId: number, body: SubmitRatingRequest): Promise<void>   // POST /rooms/{roomId}/ratings
```

- [ ] **Step 9: `handlers.test.ts`에 커버리지 테스트 추가** — api 모듈이 부르는 경로가 모두 목 표에 있는지: 각 api 함수를 `IS_MOCK`(env 비움) 상태에서 실제로 호출해 `MOCK_ROUTE_MISSING`이 안 나는지 확인한다.

```ts
it("모든 api 함수가 목 라우트를 갖는다", async () => {
  const [rooms, sessions, qs, results, me, payments, ratings, auth] = await Promise.all([
    import("@/lib/api/rooms"), import("@/lib/api/sessions"), import("@/lib/api/question-sets"), import("@/lib/api/results"),
    import("@/lib/api/me"), import("@/lib/api/payments"), import("@/lib/api/ratings"), import("@/lib/api/auth"),
  ]);
  const calls: (() => Promise<unknown>)[] = [
    () => rooms.getRoomByPin("482913"), () => rooms.createRoom({ title: "t" }), () => rooms.getHostedRooms(), () => rooms.getPublicRooms({ sort: "popular", type: "all" }),
    () => rooms.joinRoom(1, { nickname: "n" }), () => rooms.getParticipants(1), () => rooms.leaveRoom(1),
    () => sessions.startSession(1), () => sessions.getSessionSnapshot(1), () => sessions.nextQuestion(1), () => sessions.endCurrentQuestion(1), () => sessions.endSession(1),
    () => sessions.lockScreen(1, true), () => sessions.getSubmissions(1), () => sessions.submitAnswer(1, 1, "A"), () => sessions.getVoiceHints(1),
    () => qs.getQuestionSets(), () => qs.getQuestionSet(1), () => qs.confirmQuestionSet(1), () => qs.cloneQuestionSet(1), () => qs.getAiUsage(),
    () => results.getMyResult(1), () => results.getMyReport(1), () => results.getRoomReport(1), () => results.getEssayAnswers(1, 1), () => results.postHostReview(1, { comment: "c" }),
    () => me.getMyPage(), () => me.getGrade(), () => me.getBadges(), () => me.getNotificationSettings(), () => me.getHostProfile(42), () => me.claimGuestRecord(11),
    () => payments.getCoinBalance(), () => payments.getCoinTransactions(), () => payments.createCharge({ amount: 10000, method: "KAKAO_PAY" }),
    () => payments.confirmCharge("chg-1", { paymentId: "p" }), () => payments.getEarnings(), () => payments.getSettlementAccount(), () => payments.putPaymentMethod("CARD"),
    () => ratings.submitRating(1, { stars: 5, tags: [] }), () => auth.getMe(), () => auth.logout(),
  ];
  for (const call of calls) {
    try { await call(); } catch (e) { expect((e as { code?: string | null }).code).not.toBe("MOCK_ROUTE_MISSING"); }
  }
});
```

- [ ] **Step 10: 검증·커밋** — `pnpm test` PASS, `tsc` 0.

```bash
pnpm format && pnpm lint
git add src/lib/api src/lib/queries/use-restore-session.ts src/lib/mocks/handlers.test.ts
git commit -m "feat: 도메인 api 함수 — auth(/users/me·logout·Google 로그인 URL)·rooms·sessions(제어·제출·PTT multipart)·question-sets(@draft)·results·me·payments·ratings"
```

---

### Task 5: 쿼리·뮤테이션 훅 (상태 층)

**Files:**
- Create: `src/lib/queries/keys.ts`, `use-rooms.ts`, `use-session-control.ts`, `use-question-sets.ts`, `use-results.ts`, `use-me.ts`, `use-payments.ts`, `use-ratings.ts`, `use-auth.ts`

**Interfaces:**
- Consumes: Task 4의 api 함수.
- Produces: 아래 훅. 조회는 `useQuery`(DTO 그대로 반환), 뮤테이션은 성공 시 관련 키 `invalidateQueries`. 키는 `keys.ts` 한 곳.

- [ ] **Step 1: `keys.ts`**

```ts
export const qk = {
  me: ["me"] as const,
  myPage: ["me", "joined"] as const,
  grade: ["me", "grade"] as const,
  badges: ["me", "badges"] as const,
  notifications: ["me", "notifications"] as const,
  coins: ["me", "coins"] as const,
  coinTransactions: ["me", "coins", "transactions"] as const,
  earnings: ["me", "earnings"] as const,
  settlementAccount: ["me", "settlement-account"] as const,
  aiUsage: ["me", "ai-usage"] as const,
  hostedRooms: ["rooms", "hosted"] as const,
  publicRooms: (q: PublicRoomsQuery) => ["rooms", "public", q] as const,
  roomByPin: (pin: string) => ["rooms", "pin", pin] as const,
  participants: (roomId: number) => ["rooms", roomId, "participants"] as const,
  snapshot: (roomId: number) => ["rooms", roomId, "session"] as const,
  submissions: (roomId: number) => ["rooms", roomId, "session", "submissions"] as const,
  hints: (roomId: number) => ["rooms", roomId, "session", "hints"] as const,
  questionSets: (status?: QuestionSetStatusFilter) => ["question-sets", status ?? "ALL"] as const,
  questionSet: (setId: number) => ["question-sets", setId] as const,
  myResult: (roomId: number) => ["rooms", roomId, "results", "me"] as const,
  myReport: (roomId: number) => ["rooms", roomId, "reports", "me"] as const,
  roomReport: (roomId: number) => ["rooms", roomId, "results"] as const,
  essayAnswers: (roomId: number, questionId: number) => ["rooms", roomId, "questions", questionId, "answers"] as const,
  hostProfile: (userId: number) => ["users", userId, "profile"] as const,
};
```

- [ ] **Step 2: `use-rooms.ts`**

```ts
export function useRoomByPin(pin: string | null)        // enabled: !!pin, queryFn getRoomByPin. 404/410은 화면이 error.kind로 분기
export function useHostedRooms()
export function usePublicRooms(query: PublicRoomsQuery) // placeholderData: keepPreviousData
export function useParticipants(roomId: number | null)
export function useCreateRoom()      // onSuccess: invalidate qk.hostedRooms
export function useJoinRoom(roomId: number | null)   // mutationFn (body: JoinRoomRequest) => joinRoom(roomId!, body); onSuccess: res.participantToken && writeGuestToken(res.participantToken); invalidate participants
export function useLeaveRoom(roomId: number | null)  // onSuccess: clearGuestToken(); invalidate participants
```

- [ ] **Step 3: `use-session-control.ts`** (호스트 제어 + 학생 제출)

```ts
export function useStartSession(roomId: number)       // mutationFn startSession; onSuccess: invalidate qk.snapshot(roomId)
export function useNextQuestion(roomId: number)
export function useEndCurrentQuestion(roomId: number)
export function useEndSession(roomId: number)
export function useLockScreen(roomId: number)         // mutationFn (locked: boolean)
export function useSubmissions(roomId: number | null, enabled: boolean)   // refetch는 SUBMISSION_UPDATED 수신 시 invalidate(Task 6 연결 훅이 함)
export function useSubmitAnswer(roomId: number)       // mutationFn ({questionId, content})
export function useVoiceHints(roomId: number | null)
export function useUploadVoiceHint(roomId: number)    // mutationFn ({clip, durationMs}); onSuccess invalidate hints
```

- [ ] **Step 4: `use-question-sets.ts`** — `useQuestionSets(status?)`, `useQuestionSet(setId|null)`, `useGenerateQuestionSet()`(onSuccess: invalidate questionSets·aiUsage), `useUpdateQuestionSet(setId)`, `useConfirmQuestionSet()`, `useCloneQuestionSet()`(onSuccess invalidate questionSets), `useAiUsage()`, `useUploadMaterial()`.

- [ ] **Step 5: `use-results.ts`** — `useMyResult(roomId|null)`, `useMyReport(roomId|null)`, `useRoomReport(roomId|null)`, `useEssayAnswers(roomId|null, questionId|null)`, `usePostHostReview(roomId, questionId)`(onSuccess invalidate essayAnswers·roomReport).

- [ ] **Step 6: `use-me.ts`** — `useMe()`(queryKey qk.me, queryFn getMe, `initialData: useAuthStore.getState().profile ?? undefined`), `useMyPage()`, `useGrade()`, `useBadges()`, `useNotificationSettings()`, `useUpdateNotificationSettings()`(낙관적 업데이트: onMutate에서 `setQueryData(qk.notifications, next)`, onError 롤백, onSettled invalidate), `useUpdateProfile()`(onSuccess: invalidate qk.me + `useAuthStore.getState().setProfile(...)` — auth-store에 `setProfile(patch: Partial<MeResponse>)` 액션 추가), `useDeleteMe()`, `useHostProfile(userId|null)`, `useReport()`, `useClaimGuestRecord()`.

- [ ] **Step 7: `use-payments.ts`** — `useCoinBalance()`, `useCoinTransactions()`, `useCreateCharge()`, `useConfirmCharge()`(onSuccess invalidate coins·coinTransactions), `useEntryPayment(roomId)`(onSuccess invalidate coins), `useEarnings()`, `useSettlementAccount()`(`retry:false`, 404는 "미등록"으로 화면이 처리), `useUpdateSettlementAccount()`(onSuccess invalidate settlementAccount·earnings), `useUpdatePaymentMethod()`(onSuccess invalidate coins).

- [ ] **Step 8: `use-ratings.ts`** — `useSubmitRating(roomId)`(onSuccess invalidate myResult).

- [ ] **Step 9: `use-auth.ts`** — `useLogout()`: mutationFn `logout().catch(() => undefined)`(실패 무시) → onSettled: `clearRefreshToken(); clearGuestToken(); useAuthStore.getState().clearSession(); queryClient.clear();`.

- [ ] **Step 10: 검증·커밋** — `tsc` 0, `pnpm lint` 0, 기존 테스트 PASS.

```bash
pnpm format
git add src/lib/queries src/lib/stores/auth-store.ts
git commit -m "feat: 도메인 쿼리·뮤테이션 훅 — 키 상수(keys.ts), rooms·session-control·question-sets·results·me·payments·ratings·auth(logout)"
```

---

### Task 6: 실시간 — `session-store` 순수 리듀서 + STOMP 클라이언트 + 연결 훅

**Files:**
- Create: `src/lib/stores/session-reducer.ts`, `src/lib/stores/session-reducer.test.ts`, `src/lib/stores/session-store.ts`, `src/lib/stomp.ts`, `src/lib/queries/use-session-connection.ts`
- Modify: `package.json`(`pnpm add @stomp/stompjs`)

**Interfaces:**
- Produces:
  - `SessionState`, `initialSessionState`, `reduceSessionEvent(state, event): SessionState`, `applySnapshot(state, snapshot): SessionState`, `isStaleFrame(frameTs, snapshotTs): boolean`
  - `useSessionStore` (Zustand): `{ ...SessionState, connection: "idle"|"connecting"|"connected"|"reconnecting", snapshotTs: string|null, dispatch(event), replaceWithSnapshot(snapshot), setConnection(c), reset() }`
  - `connectRoomStream({ roomId, isHost, onEvent, onStatus }): () => void`
  - `useSessionConnection(roomId: number | null, opts: { isHost: boolean })`

- [ ] **Step 1: 리듀서 실패 테스트** — `session-reducer.test.ts`

```ts
import { describe, expect, it } from "vitest";
import { applySnapshot, initialSessionState, isStaleFrame, reduceSessionEvent } from "./session-reducer";

const ts = "2026-08-31T10:00:00Z";

describe("session-reducer", () => {
  it("PARTICIPANT_JOINED / LEFT 는 참가자를 추가·제거한다", () => {
    let s = reduceSessionEvent(initialSessionState, { type: "PARTICIPANT_JOINED", ts, data: { participantId: 11, nickname: "영희", isGuest: true, avatarId: 1, count: 1 } });
    expect(s.participants).toHaveLength(1);
    s = reduceSessionEvent(s, { type: "PARTICIPANT_LEFT", ts, data: { participantId: 11, count: 0 } });
    expect(s.participants).toHaveLength(0);
  });
  it("SESSION_STARTED 는 phase RUNNING 으로 초기화한다", () => {
    const s = reduceSessionEvent(initialSessionState, { type: "SESSION_STARTED", ts, data: { sessionId: 7, questionCount: 8 } });
    expect(s.phase).toBe("RUNNING");
    expect(s.questionCount).toBe(8);
    expect(s.currentQuestion).toBeNull();
  });
  it("QUESTION_STARTED 는 현재 문항·endsAt 을 바꾸고 제출 현황·정답 공개를 리셋한다", () => {
    const started = reduceSessionEvent(initialSessionState, { type: "QUESTION_STARTED", ts, data: { questionId: 3, questionNo: 2, type: "OX", body: "b", points: 100, timeLimitSec: 20, endsAt: "2026-08-31T10:00:20Z" } });
    expect(started.currentQuestion?.questionNo).toBe(2);
    expect(started.submitted).toEqual({ submittedCount: 0, totalCount: started.participants.length });
    expect(started.reveal).toBeNull();
  });
  it("ANSWER_SUBMITTED / SUBMISSION_UPDATED 는 제출 수를 갱신한다", () => {
    const s = reduceSessionEvent(initialSessionState, { type: "SUBMISSION_UPDATED", ts, data: { questionNo: 1, submittedCount: 5, totalCount: 9 } });
    expect(s.submitted).toEqual({ submittedCount: 5, totalCount: 9 });
  });
  it("QUESTION_ENDED 는 정답 공개를 저장하고 문항을 닫는다", () => {
    const q = reduceSessionEvent(initialSessionState, { type: "QUESTION_STARTED", ts, data: { questionId: 3, questionNo: 2, type: "OX", body: "b", points: 100, timeLimitSec: 20, endsAt: ts } });
    const s = reduceSessionEvent(q, { type: "QUESTION_ENDED", ts, data: { questionNo: 2, answerReveal: { answer: "O", explanation: "e" }, correctCount: 4 } });
    expect(s.reveal).toEqual({ questionNo: 2, answer: "O", explanation: "e", correctCount: 4 });
    expect(s.currentQuestion?.isClosed).toBe(true);
  });
  it("RANKING_UPDATED 는 병합이 아니라 전체 교체다", () => {
    const a = reduceSessionEvent(initialSessionState, { type: "RANKING_UPDATED", ts, data: { ranking: [{ rank: 1, participantId: 1, nickname: "a", total: 10 }, { rank: 2, participantId: 2, nickname: "b", total: 5 }] } });
    const b = reduceSessionEvent(a, { type: "RANKING_UPDATED", ts, data: { ranking: [{ rank: 1, participantId: 2, nickname: "b", total: 20 }] } });
    expect(b.ranking).toEqual([{ rank: 1, participantId: 2, nickname: "b", total: 20 }]);
  });
  it("SESSION_ENDED 는 phase FINISHED + finalRanking", () => {
    const s = reduceSessionEvent(initialSessionState, { type: "SESSION_ENDED", ts, data: { sessionId: 7, finalRanking: [{ rank: 1, participantId: 2, nickname: "b", total: 20 }] } });
    expect(s.phase).toBe("FINISHED");
    expect(s.finalRanking).toHaveLength(1);
  });
  it("HINT_PUBLISHED 는 힌트 이력에 추가, SCREEN_LOCKED 는 isLocked", () => {
    let s = reduceSessionEvent(initialSessionState, { type: "HINT_PUBLISHED", ts, data: { hintId: 1, questionNo: 1, clipUrl: "u", durationMs: 3000 } });
    expect(s.hints).toHaveLength(1);
    s = reduceSessionEvent(s, { type: "SCREEN_LOCKED", ts, data: { locked: true } });
    expect(s.isLocked).toBe(true);
  });
  it("applySnapshot 은 상태를 통째 교체한다 (404 미시작은 호출자가 WAITING 으로)", () => {
    const s = applySnapshot(initialSessionState, { status: "RUNNING", ts, questionCount: 8, currentQuestion: { questionId: 3, questionNo: 2, body: "b", endsAt: ts }, ranking: [], isLocked: true });
    expect(s.phase).toBe("RUNNING");
    expect(s.currentQuestion?.questionNo).toBe(2);
    expect(s.isLocked).toBe(true);
  });
  it("isStaleFrame 은 스냅샷 ts 이전 프레임만 true", () => {
    expect(isStaleFrame("2026-08-31T09:59:59Z", ts)).toBe(true);
    expect(isStaleFrame(ts, ts)).toBe(false);
    expect(isStaleFrame("bad", ts)).toBe(false);
  });
});
```

- [ ] **Step 2: 실패 확인** — `pnpm test src/lib/stores/session-reducer.test.ts` → 모듈 없음 FAIL.

- [ ] **Step 3: `session-reducer.ts`**

```ts
import type { ParticipantEntry, RankingEntry, SessionSnapshotResponse, SnapshotQuestion, VoiceHintEntry } from "@/lib/types/dto";
import type { ServerEvent } from "@/lib/types/events";

export type SessionPhase = "WAITING" | "RUNNING" | "FINISHED";

export type SessionState = {
  phase: SessionPhase;
  questionCount: number | null;
  participants: ParticipantEntry[];
  currentQuestion: SnapshotQuestion | null;
  /** 서버 시각 기준 — 남은 시간은 endsAt − serverTs 로 렌더만 한다 */
  serverTs: string | null;
  submitted: { submittedCount: number; totalCount: number };
  reveal: { questionNo: number; answer: string | null; explanation: string | null; correctCount: number } | null;
  ranking: RankingEntry[];
  finalRanking: RankingEntry[];
  hints: VoiceHintEntry[];
  isLocked: boolean;
  aiAnalysisEnabled: boolean;
  cancelledReason: string | null;
};

export const initialSessionState: SessionState = {
  phase: "WAITING", questionCount: null, participants: [], currentQuestion: null, serverTs: null,
  submitted: { submittedCount: 0, totalCount: 0 }, reveal: null, ranking: [], finalRanking: [], hints: [],
  isLocked: false, aiAnalysisEnabled: true, cancelledReason: null,
};

export function reduceSessionEvent(state: SessionState, event: ServerEvent): SessionState {
  const base = { ...state, serverTs: event.ts };
  switch (event.type) {
    case "PARTICIPANT_JOINED": {
      const { participantId, nickname, isGuest, avatarId } = event.data;
      const rest = state.participants.filter((p) => p.participantId !== participantId);
      return { ...base, participants: [...rest, { participantId, nickname, isGuest, avatarId, isConnected: true }] };
    }
    case "PARTICIPANT_LEFT":
      return { ...base, participants: state.participants.filter((p) => p.participantId !== event.data.participantId) };
    case "SESSION_STARTED":
      return { ...base, phase: "RUNNING", questionCount: event.data.questionCount, currentQuestion: null, reveal: null, ranking: [], finalRanking: [] };
    case "QUESTION_STARTED": {
      const { questionId, questionNo, type, body, choices, points, timeLimitSec, endsAt } = event.data;
      return {
        ...base, phase: "RUNNING",
        currentQuestion: { questionId, questionNo, type, body, choices: choices ?? null, points, timeLimitSec, endsAt, isClosed: false },
        submitted: { submittedCount: 0, totalCount: state.participants.length }, reveal: null,
      };
    }
    case "ANSWER_SUBMITTED":
    case "SUBMISSION_UPDATED":
      return { ...base, submitted: { submittedCount: event.data.submittedCount, totalCount: event.data.totalCount } };
    case "QUESTION_ENDED":
      return {
        ...base,
        currentQuestion: state.currentQuestion ? { ...state.currentQuestion, isClosed: true } : null,
        reveal: { questionNo: event.data.questionNo, answer: event.data.answerReveal.answer ?? null, explanation: event.data.answerReveal.explanation ?? null, correctCount: event.data.correctCount },
      };
    case "SCORE_UPDATED":
      return base;
    case "RANKING_UPDATED":
      return { ...base, ranking: event.data.ranking };
    case "SCREEN_LOCKED":
      return { ...base, isLocked: event.data.locked };
    case "HINT_PUBLISHED":
      return { ...base, hints: [...state.hints, event.data] };
    case "SESSION_ENDED":
      return { ...base, phase: "FINISHED", finalRanking: event.data.finalRanking, ranking: event.data.finalRanking };
    case "ROOM_CANCELLED":
      return { ...base, cancelledReason: event.data.reason ?? "" };
    default:
      return base;
  }
}

export function applySnapshot(state: SessionState, snapshot: SessionSnapshotResponse): SessionState {
  const status = snapshot.status ?? "WAITING";
  return {
    ...state,
    phase: status === "RUNNING" ? "RUNNING" : status === "WAITING" ? "WAITING" : "FINISHED",
    questionCount: snapshot.questionCount ?? state.questionCount,
    currentQuestion: snapshot.currentQuestion ?? null,
    serverTs: snapshot.ts,
    ranking: snapshot.ranking ?? [],
    isLocked: snapshot.isLocked ?? false,
    reveal: null,
  };
}

/** 스냅샷보다 오래된 프레임은 폐기. 파싱 실패는 폐기하지 않는다(KMP SnapshotPolicy) */
export function isStaleFrame(frameTs: string, snapshotTs: string): boolean {
  const f = Date.parse(frameTs);
  const s = Date.parse(snapshotTs);
  if (Number.isNaN(f) || Number.isNaN(s)) return false;
  return f < s;
}
```

- [ ] **Step 4: 통과 확인** — 10 PASS.

- [ ] **Step 5: `session-store.ts`**

```ts
import { create } from "zustand";
import type { SessionSnapshotResponse } from "@/lib/types/dto";
import type { ServerEvent } from "@/lib/types/events";
import { applySnapshot, initialSessionState, isStaleFrame, reduceSessionEvent, type SessionState } from "./session-reducer";

export type ConnectionStatus = "idle" | "connecting" | "connected" | "reconnecting";

type SessionStore = SessionState & {
  connection: ConnectionStatus;
  snapshotTs: string | null;
  dispatch: (event: ServerEvent) => void;
  replaceWithSnapshot: (snapshot: SessionSnapshotResponse | null) => void;   // null = 404 미시작 → WAITING 유지, snapshotTs=now
  setParticipants: (participants: SessionState["participants"]) => void;    // GET /participants 초기 로딩
  setConnection: (c: ConnectionStatus) => void;
  setAiAnalysisEnabled: (v: boolean) => void;
  reset: () => void;
};

export const useSessionStore = create<SessionStore>()((set, get) => ({
  ...initialSessionState, connection: "idle", snapshotTs: null,
  dispatch: (event) => {
    const { snapshotTs } = get();
    if (snapshotTs && isStaleFrame(event.ts, snapshotTs)) return;
    set((s) => reduceSessionEvent(s, event));
  },
  replaceWithSnapshot: (snapshot) => set((s) => snapshot
    ? { ...applySnapshot(s, snapshot), snapshotTs: snapshot.ts }
    : { ...s, phase: "WAITING", snapshotTs: new Date().toISOString() }),
  setParticipants: (participants) => set({ participants }),
  setConnection: (connection) => set({ connection }),
  setAiAnalysisEnabled: (aiAnalysisEnabled) => set({ aiAnalysisEnabled }),
  reset: () => set({ ...initialSessionState, connection: "idle", snapshotTs: null }),
}));
```

- [ ] **Step 6: `pnpm add @stomp/stompjs`** 후 `stomp.ts`

```ts
import { Client, type IMessage } from "@stomp/stompjs";
import { IS_MOCK, WS_URL } from "@/lib/env";
import { readGuestToken } from "@/lib/guest-token-storage";
import { useAuthStore } from "@/lib/stores/auth-store";
import { parseServerEvent, type ServerEvent } from "@/lib/types/events";

type Status = "connecting" | "connected" | "reconnecting";
type Options = { roomId: number; isHost: boolean; onEvent: (e: ServerEvent) => void; onStatus: (s: Status) => void };

const MAX_BACKOFF_MS = 5_000;
const STEP_MS = 1_000;

/**
 * 방 이벤트 스트림(계약 websocket-events). 구독: /topic/rooms/{id} (+ /host), /user/queue/feedback, /user/queue/errors.
 * 재연결은 선형 백오프(1s×n, 최대 5s). 재연결 후 스냅샷 재조회는 use-session-connection이 onStatus("connected")로 한다.
 * 목 모드(WS_URL 비어 있음)는 연결 없이 즉시 connected — 이벤트는 오지 않는다.
 */
export function connectRoomStream({ roomId, isHost, onEvent, onStatus }: Options): () => void {
  if (IS_MOCK || !WS_URL) {
    const id = setTimeout(() => onStatus("connected"), 0);
    return () => clearTimeout(id);
  }

  let attempt = 0;
  const token = useAuthStore.getState().accessToken ?? readGuestToken();
  const client = new Client({
    brokerURL: WS_URL,
    connectHeaders: token ? { Authorization: `Bearer ${token}` } : {},
    reconnectDelay: STEP_MS,
    beforeConnect: () => {
      client.reconnectDelay = Math.min(STEP_MS * (attempt + 1), MAX_BACKOFF_MS);
      onStatus(attempt === 0 ? "connecting" : "reconnecting");
    },
    onConnect: () => {
      attempt = 0;
      const handle = (m: IMessage) => {
        const event = parseServerEvent(safeJson(m.body));
        if (event) onEvent(event);
      };
      client.subscribe(`/topic/rooms/${roomId}`, handle);
      if (isHost) client.subscribe(`/topic/rooms/${roomId}/host`, handle);
      client.subscribe("/user/queue/feedback", handle);
      client.subscribe("/user/queue/errors", handle);
      onStatus("connected");
    },
    onWebSocketClose: () => { attempt += 1; },
  });

  client.activate();
  return () => { void client.deactivate(); };
}

function safeJson(text: string): unknown {
  try { return JSON.parse(text); } catch { return null; }
}
```

- [ ] **Step 7: `use-session-connection.ts`**

```ts
"use client";
import { useQueryClient } from "@tanstack/react-query";
import { useEffect } from "react";
import { getParticipants } from "@/lib/api/rooms";
import { getSessionSnapshot } from "@/lib/api/sessions";
import { connectRoomStream } from "@/lib/stomp";
import { useSessionStore } from "@/lib/stores/session-store";
import { AppError } from "@/lib/types/app-error";
import { qk } from "./keys";

/**
 * 연결 → connected 마다 스냅샷(GET /rooms/{id}/session, 404=WAITING → 참가자 목록) → 스토어 통째 교체 → 이후 프레임 dispatch.
 * 컴포넌트는 이 훅만 부르고 스토어를 selector 로 읽는다.
 */
export function useSessionConnection(roomId: number | null, { isHost }: { isHost: boolean }) {
  const queryClient = useQueryClient();

  useEffect(() => {
    if (roomId === null) return;
    const store = useSessionStore.getState();
    store.reset();

    const restore = async () => {
      try {
        const snapshot = await getSessionSnapshot(roomId);
        store.replaceWithSnapshot(snapshot);
      } catch (e) {
        if (AppError.isAppError(e) && e.kind === "NotFound") {
          store.replaceWithSnapshot(null);
          const { participants } = await getParticipants(roomId);
          store.setParticipants(participants ?? []);
        } else {
          throw e;
        }
      }
    };

    const disconnect = connectRoomStream({
      roomId, isHost,
      onStatus: (s) => {
        store.setConnection(s);
        if (s === "connected") void restore().catch((e: unknown) => console.warn("세션 복구 실패", e));
      },
      onEvent: (event) => {
        store.dispatch(event);
        if (event.type === "SUBMISSION_UPDATED" || event.type === "ANSWER_SUBMITTED") void queryClient.invalidateQueries({ queryKey: qk.submissions(roomId) });
        if (event.type === "FEEDBACK_READY" || event.type === "REVIEW_RECEIVED") void queryClient.invalidateQueries({ queryKey: qk.myResult(roomId) });
        if (event.type === "REPORT_READY") void queryClient.invalidateQueries({ queryKey: qk.roomReport(roomId) });
      },
    });

    return () => { disconnect(); store.reset(); };
  }, [roomId, isHost, queryClient]);
}
```

목 모드에서 화면을 움직여 볼 수 있도록 **목 세션 제어가 이벤트를 흘려보내는 통로**를 하나 둔다: `lib/mocks/session.ts`에 `mockSessionEvents = new EventTarget()`을 export하고, `mockStartSession/mockNext/mockEndCurrent/mockEndSession/mockLock/mockJoinRoom`이 해당 `ServerEvent`를 `dispatchEvent(new CustomEvent("event", { detail }))`로 발행한다. `connectRoomStream`의 목 분기에서 이 EventTarget을 구독해 `onEvent`로 넘긴다(구독 해제는 반환 함수에서). 이렇게 하면 목 모드에서도 "세션 시작 → SESSION_STARTED → QUESTION_STARTED" 흐름이 실제와 같은 경로(REST → 이벤트 → 스토어)로 화면을 바꾼다.

- [ ] **Step 8: 검증·커밋** — `pnpm test` PASS(리듀서 10 포함), `tsc` 0, `lint` 0.

```bash
pnpm format
git add package.json pnpm-lock.yaml src/lib/stores/session-reducer.ts src/lib/stores/session-reducer.test.ts src/lib/stores/session-store.ts src/lib/stomp.ts src/lib/queries/use-session-connection.ts src/lib/mocks/session.ts
git commit -m "feat: 실시간 세션 — 순수 리듀서(이벤트 19종·스냅샷 교체·stale 프레임 폐기) + Zustand session-store + @stomp/stompjs 래퍼(4 구독·선형 백오프) + 연결 훅(connected마다 스냅샷 복구), 목 모드 이벤트 버스"
```

---

## 화면 연결 공통 규칙 (Task 7~12)

- **뷰 타입 이동**: 각 feature의 `mock.ts`에 있던 `export type …`은 같은 폴더 `types.ts`로 옮기고, 데이터 상수(`export const …`)는 Task 3의 `lib/mocks/*`로 이미 옮겨졌으므로 삭제한다. 뷰(`*-view.tsx`·카드·행 컴포넌트)의 import만 `./mock` → `./types`로 바꾼다. **뷰 props는 바꾸지 않는다.**
- **어댑터**: `features/<x>/adapt.ts`에 `toXxx(dto): ViewType` 순수 함수. 아바타는 `avatarId → AvatarKey` 변환에 `components/common/student-avatar.tsx`에 추가하는 `avatarKeyFromId(id: number | null | undefined): AvatarKey`(= `AVATAR_KEYS[(id ?? 1) - 1] ?? "cat"`)와 `avatarIdFromKey(key): number`(= index+1)를 쓴다.
- **컨테이너(page.tsx)**: 로딩 `ScreenLoading`, 오류 `ScreenError({message: error.message, onRetry})`. 서버 `code` 분기는 `ERROR_CODES` 상수. 뮤테이션 `isPending` 동안 버튼 disabled(뷰가 이미 `disabled`/`pending` prop을 받는 곳은 그것을 쓴다).
- **PIN → roomId**: `[code]` 파라미터는 PIN. `const room = useRoomByPin(pin)` → `room.data?.roomId ?? null`을 하위 훅에 넘긴다. `routes.ts`의 sample `DEMO01`은 전부 `482913`으로 바꾼다.
- 각 Task 끝에 `pnpm build && pnpm check:routes`가 통과해야 한다(`/dev` 포함 41개, Task 7에서 콜백 라우트가 늘어 42개).

---

### Task 7: 인증 — Google 로그인 진입·`/auth/callback`·로그아웃·사이드바 프로필

**Files:**
- Modify: `src/config/routes.ts`(`/auth/callback` 추가, `area:"public"`, title "로그인 처리", description "소셜 로그인 콜백 — 토큰 수신 후 원래 가려던 화면으로")
- Create: `src/app/(bare)/auth/callback/page.tsx`
- Modify: `src/app/(bare)/login/page.tsx`, `src/features/auth/login-page.tsx`(Google 버튼 `onGoogleClick` prop)
- Modify: `src/app/(member)/me/page.tsx`(로그아웃 → `useLogout`)
- Create: `src/components/layout/sidebar-account.tsx`(auth-store 프로필 → `SidebarUser`)
- Modify: `src/app/(member)/layout.tsx`, `src/app/host/(nav)/layout.tsx`(`ACCOUNT` 목 대신 `<SidebarAccount nav="member|host" />`)

- [ ] **Step 1: `routes.ts`에 라우트 추가** (공통 그룹, `/login` 다음).
- [ ] **Step 2: 콜백 페이지**

```tsx
"use client";
import { useRouter, useSearchParams } from "next/navigation";
import { useEffect, useState } from "react";
import { ScreenError } from "@/components/common/screen-error";
import { ScreenLoading } from "@/components/common/screen-loading";
import { getMe } from "@/lib/api/auth";
import { useAuthStore } from "@/lib/stores/auth-store";
import { writeRefreshToken } from "@/lib/token-storage";

const HOME_PATH = "/home";

/** /auth/callback?accessToken&refreshToken&next — 토큰 저장 → GET /users/me → next 로 이동. (DESIGN_GAPS A-6: 시안 없음) */
export default function Page() {
  const router = useRouter();
  const params = useSearchParams();
  const [failed, setFailed] = useState(false);

  useEffect(() => {
    const accessToken = params.get("accessToken");
    const refreshToken = params.get("refreshToken");
    const next = params.get("next") || HOME_PATH;
    if (!accessToken || !refreshToken) { setFailed(true); return; }

    useAuthStore.getState().setAccessToken(accessToken);
    writeRefreshToken(refreshToken);
    getMe()
      .then((profile) => { useAuthStore.getState().setSession(accessToken, profile); router.replace(next); })
      .catch(() => setFailed(true));
  }, [params, router]);

  if (failed) return <ScreenError message="로그인에 실패했어요. 다시 시도해 주세요." onRetry={() => router.replace("/login")} />;
  return <ScreenLoading />;
}
```

- [ ] **Step 3: 로그인 페이지** — `login-page.tsx`의 Google 버튼에 `onGoogleClick?: () => void` prop을 추가하고 컨테이너 `app/(bare)/login/page.tsx`에서 `window.location.assign(googleLoginUrl(next))` (`next`는 `useSearchParams().get("next") ?? "/home"`). 이메일 폼은 계약이 없으므로 그대로 두고 `TODO(API)` 주석에 "DESIGN_GAPS C-1 결정 대기"를 덧붙인다. 이미 로그인 상태(`useAuthStore(s=>s.status)==="authenticated"`)면 `/home`으로 replace.

- [ ] **Step 4: `sidebar-account.tsx`**

```tsx
"use client";
import { RoleSidebar, type SidebarUser } from "@/components/layout/role-sidebar";
import { useAuthStore } from "@/lib/stores/auth-store";

const FALLBACK: SidebarUser = { name: "회원", initial: "회", roleLabel: "회원", tone: "peach" };

export function SidebarAccount({ nav }: { nav: "member" | "host" }) {
  const profile = useAuthStore((s) => s.profile);
  const user: SidebarUser = profile?.nickname
    ? { name: profile.nickname, initial: profile.nickname.slice(0, 1), roleLabel: profile.level ? `Lv.${profile.level}` : "회원", tone: "peach" }
    : FALLBACK;
  return <RoleSidebar nav={nav} user={user} />;
}
```

두 레이아웃에서 `import { ACCOUNT } from "@/features/me/mock"`를 제거하고 `<SidebarAccount nav="…" />`로 교체.

- [ ] **Step 5: `/me` 로그아웃** — `useLogout()`; `onSuccess/onSettled`에서 `router.replace("/login")`.
- [ ] **Step 6: 검증** — `pnpm build && pnpm check:routes`(42), 목 모드에서 `/login` → Google 클릭 시 `${API_BASE_URL}/auth/oauth/google?client=web…`로 이동하는지(목에서는 base가 비어 `/auth/oauth/google…` 404 — 정상, 주석으로 명시), `/auth/callback?accessToken=a&refreshToken=b&next=/home` 이 `/home`으로 가는지.
- [ ] **Step 7: 커밋** `feat: 인증 연결 — Google 로그인 진입(@draft client=web), /auth/callback 토큰 수신 페이지, 로그아웃 뮤테이션, 사이드바 프로필을 auth-store에서`

---

### Task 8: 참여 흐름 — 게스트 입장 · 홈 PIN 입장 · 인기 방 · 학생 풀이

**Files:**
- Modify: `src/app/(bare)/join/page.tsx`, `src/features/participant/join/join-page.tsx`(오류 문구 prop `errorMessage`)
- Modify: `src/app/(member)/home/page.tsx`, `src/features/home/home-page.tsx`·`popular-rooms.tsx`·`pin-entry-card.tsx`
- Create: `src/features/home/types.ts`, `src/features/home/adapt.ts`, `src/features/participant/types.ts`, `src/features/participant/play/adapt.ts`
- Modify: `src/app/(bare)/play/[code]/page.tsx`, `src/features/participant/play/play-page.tsx`·`play-card.tsx`·`hint-banner.tsx`
- Delete: `src/features/home/mock.ts`, `src/features/participant/mock.ts`

- [ ] **Step 1: 입장 컨테이너(`/join`)** — 폼 submit `{pin, nickname, avatar}` →
  1. `getRoomByPin(pin)`은 `useMutation`으로 감싼 `useResolvePin()`(`lib/queries/use-rooms.ts`에 추가: mutationFn `getRoomByPin`) 또는 `queryClient.fetchQuery(qk.roomByPin(pin))`로 조회.
  2. `room.isPaid`면 회원 전용 → 게스트는 `/login?next=/pay/{pin}`로 안내(문구 "유료 방은 로그인 후 결제하고 입장할 수 있어요"), 회원은 `/pay/{pin}`.
  3. 무료면 `useJoinRoom(room.roomId).mutate({nickname, avatarId: avatarIdFromKey(avatar)})` → onSuccess `router.push(`/play/${pin}`)`.
  4. 오류 분기: `NotFound`→"없는 PIN이에요", `Gone`→"이미 종료된 방이에요", `code===NICKNAME_TAKEN`→"같은 닉네임이 이미 있어요", 그 외 `error.message`. `JoinPage`에 `errorMessage`·`pending` prop 추가.
- [ ] **Step 2: 홈** — `usePublicRooms({sort:"popular", type:"all"})` → `adapt.toPopularRooms(items)`(`PublicRoomDto`→`PopularRoom`: `code=pin, topic, type=isPaid?"paid":"free", title, host=hostName, level=hostLevel??1, participants=participantCount??0`). 내가 만든 방·참여 방 요약은 `useHostedRooms()`·`useMyPage()`에서 개수만. PIN 입장은 Step 1과 같은 로직(회원 닉네임 기본값 = 프로필 nickname, 아바타 = 프로필 avatarId).
- [ ] **Step 3: 풀이(`/play/[code]`)** — `useRoomByPin(pin)` → `useSessionConnection(roomId, {isHost:false})` → 스토어 selector: `phase`, `currentQuestion`, `serverTs`, `hints`, `isLocked`, `submitted`. `PlayPage` props를 `question: LiveQuestion`에서 만들려면 `adapt.toLiveQuestion(currentQuestion, questionCount, serverTs)`(`SnapshotQuestion`→`LiveQuestion`: `index=questionNo, total=questionCount, type=MULTIPLE_CHOICE→"multiple"|OX→"ox"|ESSAY→"essay", prompt=body, choices=choices.map((text,i)=>({key:"ABCD"[i], text})), seconds=timeLimitSec, remaining=max(0, round((Date.parse(endsAt)-Date.parse(serverTs))/1000)), submitted=submitted.submittedCount`). 제출은 `useSubmitAnswer(roomId).mutate({questionId, content})`(객관식은 선택 보기 원문, OX는 "O"/"X"). `phase==="WAITING"`이면 임시 대기 문구(시안 없음 — DESIGN_GAPS A-2) `"선생님이 시작하면 자동으로 넘어가요"` + 참가자 수. `phase==="FINISHED"`면 `/result/{roomId}`로 replace. `isLocked`면 카드 위에 "선생님이 화면을 잠갔어요" 오버레이(공용 컴포넌트 없음 — 단순 div, 토큰 색). 힌트는 `hints[hints.length-1]`을 `HintBanner`에 `clipUrl`로 넘겨 `<audio autoPlay>`(자동 재생 실패 시 수동 재생 버튼 — 기존 배너의 재생 버튼 재사용).
- [ ] **Step 4: 검증·커밋** — 목 모드: `/join`에 PIN 482913·닉네임 입력 → 유료 방이라 로그인 안내 확인; `/home` 인기 방 3장 렌더; `pnpm build && pnpm check:routes`. `feat: 참여 흐름 연결 — /join·홈 PIN 입장(방 조회→입장→게스트 토큰), 인기 방(GET /rooms/public), 학생 풀이를 session-store에 연결(문항·타이머·제출·힌트·잠금)`

---

### Task 9: 호스트 진행 — 방 만들기 → 대기실 → 진행 → 문항 결과

**Files:**
- Modify: `src/app/host/(flow)/rooms/new/page.tsx`, `src/features/host/room-flow/new-room-form.tsx`·`new-room-dialog.tsx`(submit 콜백 prop으로 `CreateRoomRequest` 전달, `pending`·`errorMessage` prop)
- Modify: `src/app/host/(flow)/rooms/[code]/lobby/page.tsx`, `live/page.tsx`, `result/page.tsx`
- Modify: `src/features/host/live/lobby-page.tsx`·`live-page.tsx`·`result-page.tsx`(props로 데이터·콜백 받기 — 이미 `frozen`·`paused` 등 prop이 있으므로 같은 방식으로 `room`, `students`, `question`, `onStart`, `onNext`, `onEndCurrent`, `onEnd`, `onLock`, `onHint(clip, durationMs)` 추가)
- Create: `src/features/host/types.ts`(구 `features/host/mock.ts`의 타입: `QuestionSet`·`Student`·`LiveRoom`·`LiveQuestion`·`QuestionResult`·`ReportQuestion`·`EssayAnswer`·`SessionReport`·`QuestionType`…), `src/features/host/live/adapt.ts`
- Delete: `src/features/host/mock.ts`(데이터 부분은 Task 3에서 이미 `lib/mocks`로 이동)

- [ ] **Step 1: 방 만들기** — 폼 값 → `CreateRoomRequest{ title, questionSetId, isPaid, entryFee, isListed:true }` → `useCreateRoom().mutate(body, { onSuccess: r => router.push(`/host/rooms/${r.pin}/lobby`) })`. 세트 목록은 `useQuestionSets("CONFIRMED")`. 오류: `code===HOST_LEVEL_REQUIRED` → "유료 방은 Lv.3부터 열 수 있어요", `ValidationFailed` → `error.serverMessage ?? error.message`. 등급은 `useGrade().data?.level`로 `paidLocked` 계산(기존 `setup.reputation.level` 자리).
- [ ] **Step 2: 대기실** — `useRoomByPin(pin)`→roomId, `useSessionConnection(roomId,{isHost:true})`, `participants`는 스토어 selector. `adapt.toStudents(participants)`(`ParticipantEntry`→`Student{id:String(participantId), name:nickname, avatar:avatarKeyFromId(avatarId)}`). QR은 `room.qrUrl`이 없으므로 `${origin}/join?pin=${pin}` 텍스트를 `<img src={`https://api.qrserver.com/…`}>` 대신 **외부 요청 없이** `qrcode` 패키지 없이 그리기 어렵다 → `pnpm add qrcode.react` 후 `<QRCodeSVG value=… size={116} />`. "세션 시작" → `useStartSession(roomId).mutate(undefined,{onSuccess:r=>store.setAiAnalysisEnabled(r.aiAnalysisEnabled ?? true)})`; 409 → "문제 세트를 먼저 확정해 주세요". `useEffect`: `phase==="RUNNING"` → `router.replace(`/host/rooms/${pin}/live`)`.
- [ ] **Step 3: 진행** — 같은 연결 훅(뒤로 가기·새로고침에도 스냅샷으로 복구됨). `question=adapt.toLiveQuestion(currentQuestion, questionCount, serverTs, submitted)`. 버튼: 다음 문항 `useNextQuestion`, 문항 마감 `useEndCurrentQuestion`, 세션 종료 `useEndSession`, 잠금 `useLockScreen`. PTT: `live-page`의 Mic 버튼을 `PttButton`(새 컴포넌트 `src/components/common/ptt-button.tsx` — `MediaRecorder` 홀드 녹음(`pointerdown`~`pointerup`), 릴리즈 시 `onRecorded(blob, durationMs)`; 권한 거부 시 `onError`)으로 교체하고 컨테이너가 `useUploadVoiceHint(roomId).mutate({clip, durationMs})`. `reveal`이 생기면(`QUESTION_ENDED`) `router.replace(`/host/rooms/${pin}/result`)`, `phase==="FINISHED"`면 `/host/sessions/${roomId}/review`. 연결 상태 `connection!=="connected"`이면 상단에 "연결 다시 맞추는 중…" 배지(시안 없음, DESIGN_GAPS W-05).
- [ ] **Step 4: 문항 결과** — `reveal` + `useSubmissions(roomId, phase==="RUNNING")`. `adapt.toQuestionResult(reveal, submissions, ranking)`(`QuestionResult{ correct: 보기 label→"ABCD" 키, distribution: choices.map((c,i)=>({key, text:c.label, count:c.count})), accuracy: accuracyPercent, accuracyDelta: 0, ranking: ranking.map(r=>({rank, studentId:String(participantId), score:total, change:0})) }`). "다음 문항" → `useNextQuestion` → `QUESTION_STARTED` 수신 시 `/live`로 replace. 마지막 문항(`currentQuestion.questionNo === questionCount`)이면 버튼 "세션 종료".
- [ ] **Step 5: 검증·커밋** — 목 모드로 `/host/rooms/new` → 만들기 → 대기실(목 이벤트 버스가 `PARTICIPANT_JOINED`를 흘리도록 `mockStartSession` 전에 픽스처 참가자 입장 이벤트 3개 발행) → 시작 → `/live` → 다음 → `/result` → 세션 종료 → `/review` 까지 화면이 스스로 전환되는지. `pnpm build && pnpm check:routes`. `feat: 호스트 진행 연결 — 방 만들기(POST /rooms, 등급 게이트), 대기실·진행·문항 결과를 session-store로 구동(시작/다음/마감/종료/잠금 REST → 이벤트 → phase 전환), PTT 홀드 녹음 업로드, QR`

---

### Task 10: 호스트 목록 — 내가 만든 방 · 문제 세트 · 방 리포트 · 에디터(@draft)

**Files:**
- Modify: `src/app/host/(nav)/rooms/page.tsx`, `src/features/host/my-rooms/my-rooms-page.tsx`(props화: `rooms`, `stats`, `level`, `promotion`), Create `src/features/host/my-rooms/types.ts`·`adapt.ts`, Delete `my-rooms/mock.ts`
- Modify: `src/app/host/(nav)/sets/page.tsx`, `src/features/host/sets/sets-page.tsx`(props `sets`, `onClone`)
- Modify: `src/app/host/(nav)/sessions/[sessionId]/review/page.tsx`(`[sessionId]`는 **roomId**로 해석), `src/features/host/review/review-page.tsx`(props `report`, `essayAnswers`), `analysis-panel.tsx`
- Modify: `src/app/host/(flow)/editor/page.tsx`, `src/features/host/editor/editor-page.tsx`·`generate-panel.tsx`(props `usage`, `onGenerate`, `generating`, `errorMessage`)

- [ ] **Step 1: 내가 만든 방** — `useHostedRooms()`+`useGrade()`+`useBadges()`. `adapt.toMyRooms(items)`(`HostedRoomDto`→`MyRoom{code:pin, title, status: RUNNING|WAITING→"live" | FINISHED|ENDED→"ended", students:participantCount, questionCount:0(계약 없음—리스트 카드가 0을 숨기도록 `questionCount?`로 완화), pin, startsLabel:scheduledAt, endedLabel:endedAtLabel, averageScore:avgAccuracyPercent, reportId:String(roomId)}`), `adapt.toLevelStatus(grade)`(`level, title=LEVEL_TITLE[level], achievedLabel=achievedAt+" 달성"…, next{level:next.level, title, progress:next.progressPercent}, perks=고정표`), `adapt.toPromotion(grade)`(`rules=next.criteria.map(c=>({label:c.label, value:`${c.current}/${c.target}`, met:c.met}))`). 등급 명칭 표 `LEVEL_TITLE = {1:"새싹",2:"성장",3:"검증된 운영자",4:"인기 운영자",5:"마스터"}`는 `features/host/my-rooms/types.ts`에.
- [ ] **Step 2: 문제 세트** — `useQuestionSets()` → `adapt.toQuestionSets(items)`(`QuestionSetDto`→`QuestionSet{id:String(setId), title, summary:"", questionCount, tile:{label:title.slice(0,1), tone:"mint"}, composition:[], totalPoints:0, minutes:0, usage: usedCount?{count:usedCount, lastUsed:lastUsedAt}:undefined, preview:[]}` — 계약에 없는 `composition·totalPoints·minutes·preview`는 상세(`useQuestionSet(setId)` @draft)가 열릴 때 채운다). 복제 → `useCloneQuestionSet().mutate(setId)` 성공 시 `/host/editor?set={newId}`.
- [ ] **Step 3: 방 리포트** — `useRoomReport(roomId)` → `adapt.toSessionReport(report)`(`RoomReportResponse`→`SessionReport{id, title:roomTitle, dateLabel, stats:{accuracy:summary.avgAccuracyPercent, students:studentCount, questions:questionCount, aiAnalyses:aiAnalysisCount}, questions: questions.map(q=>({id:String(questionId), index:questionNo, title, type, accuracy:accuracyPercent, aiCount:aiFeedbackCount}))}`). 분석 패널은 선택 문항의 `useEssayAnswers(roomId, questionId)`(@draft) → `EssayAnswer{studentId, text:content, findings:[covered→good, missing→lack, improvement→tip]}`. 내보내기 버튼은 `downloadFile(`/sessions/${roomId}/stats/export`, …)` @draft(tasks T060) — 목 모드에서는 토스트 "백엔드 연동 후 제공".
- [ ] **Step 4: 에디터** — `useAiUsage()`로 "무료 생성 n/5회 남음" 표시(`generate-panel`에 `usage` prop; 시안 없음 — 작은 라벨), 생성 → `useGenerateQuestionSet().mutate(body)` → 성공 시 `question-list`에 `QuestionDraft[]`를 `adapt.toEditorQuestions`로 넘김. 오류: `code===FREE_QUOTA_EXCEEDED` → "무료 생성 5회를 모두 썼어요. 추후 코인 결제 예정 — 직접 작성하거나 세트를 복제해 쓸 수 있어요", `code===AI_GENERATION_FAILED` → "생성에 실패했어요. 다시 요청해 주세요"(재요청 버튼). 확정 → `useConfirmQuestionSet().mutate(setId)` → `/host/rooms/new?set={id}`.
- [ ] **Step 5: 검증·커밋** — `pnpm build && pnpm check:routes`. `feat: 호스트 목록 연결 — 내가 만든 방(hosted+등급+뱃지), 문제 세트(목록·복제), 방 리포트(전체 결과+서술 답안 @draft), 에디터(AI 생성·잔여 횟수·확정 @draft, 한도·실패 코드 분기)`

---

### Task 11: 회원 — 마이페이지 · 계정 · 캐릭터 · 정산 계좌 · 정산 · 코인 · 충전 · 결제 수단 · 알림 · 참여한 방 · 탈퇴

**Files:**
- Modify: `src/app/(member)/me/page.tsx`, `me/account`, `me/character`, `me/password`(연결 없음 — 주석만), `me/settlement-account`, `me/settlement`, `me/coins`, `me/coins/charge`, `me/coins/charge/complete`, `me/payment-methods`, `me/notifications`, `me/joined`, `me/withdraw` 의 `page.tsx`
- Create: `src/features/me/types.ts`·`adapt.ts`(구 `features/me/mock.ts`·서브 mock 타입 이동)
- Delete: `src/features/me/mock.ts`, `me/coins/mock.ts`, `me/joined/mock.ts`, `me/notifications/mock.ts`, `me/payment-methods/mock.ts`, `me/settlement/mock.ts`

- [ ] **Step 1: 어댑터** — `toProfile(me: MeResponse, grade?: GradeResponse): Profile`(`name=nickname, nickname, email, joinedLabel=joinedAt→"YYYY-MM 가입", avatar=avatarKeyFromId(avatarId), level, levelTitle=LEVEL_TITLE[level], levelPerk=level>=3?"유료 방 개설 가능":"", nextLevel:{level:grade.next.level, roomsLeft:criteria "방 운영" target-current, studentsLeft}, progress:grade.next.progressPercent`), `toCoinSummary(coins: CoinBalanceResponse): CoinSummary`(`balance, paymentMethodLabel=PAY_METHOD_LABEL[defaultMethod], lastTransaction:{dateLabel: createdAt→"M/D", title:roomTitle??type 라벨, amount}`), `toSettlementRows(items)`(`SCHEDULED→"scheduled", PAID|HELD→"paid"`… HELD는 상태 라벨 "보류"로 `SETTLEMENT_STATUS_LABEL`에 추가), `toSettlementSummary(earnings)`, `toCoinHistory(items)`(`type CHARGE→charge, DEDUCT|REFUND→use` 필터 규칙은 amount 부호 유지), `toActiveSession(ongoing)`, `toNotificationSettings(dto)`(3종 + 마케팅은 로컬 상태로 유지·`TODO(API) DESIGN_GAPS C-5`).
- [ ] **Step 2: 컨테이너 연결** — 각 page에서 목 import를 훅으로: `/me` `useMe`+`useGrade`+`useCoinBalance`+`useEarnings`(정산 예정) / `account` `useUpdateProfile({nickname})` / `character` `useUpdateProfile({avatarId})` / `settlement-account` `useSettlementAccount`(404→빈 폼)+`useUpdateSettlementAccount` / `settlement` `useEarnings` / `coins` `useCoinBalance`+`useCoinTransactions` / `charge` `useCreateCharge`→`requestPayment`(portone 목)→`useConfirmCharge(chargeId,{paymentId})`→`/me/coins/charge/complete?paymentId=…&amount=…` / `complete` `useCoinBalance` 잔액 + 쿼리스트링 / `payment-methods` `useCoinBalance().defaultMethod`+`useUpdatePaymentMethod`(카드 목록 UI는 로컬 유지 — `DESIGN_GAPS C-4`) / `notifications` `useNotificationSettings`+`useUpdateNotificationSettings` / `joined` `useMyPage` / `withdraw` `useDeleteMe`(409 → `error.serverMessage ?? "정산 예정 금액이나 진행 중인 방이 있어 지금은 탈퇴할 수 없어요"`) 후 `useLogout` 정리 → `/`.
- [ ] **Step 3: 검증·커밋** — `pnpm build && pnpm check:routes`; 목 모드로 `/me` 하위 11화면 오류 없이 렌더. `feat: 회원 화면 연결 — 프로필·등급·코인·정산·계좌·알림·참여한 방·탈퇴를 lib/queries 훅으로 (feature mock 제거, adapt.ts로 뷰 타입 유지)`

---

### Task 12: 유료 방 결제 `/pay/[code]`

**Files:**
- Modify: `src/app/(member)/pay/[code]/page.tsx`, `src/features/participant/pay/pay-page.tsx`(props 유지), Create `src/features/participant/pay/types.ts`·`adapt.ts`, Delete `pay/mock.ts`

- [ ] **Step 1** — `useRoomByPin(pin)` → `adapt.toPaidRoom(room)`(`PaidRoom{code:pin, title, topic, composition:`문항 ${questionCount}개`, host:{name:host.nickname, avatar:avatarKeyFromId(null), level:host.level, levelTitle}, rating:host.avgStars, students:host.ratingCount, schedule:scheduledAt→"M/D (요일) HH:mm · 약 N분", capacity:{current:participantCount, max:maxParticipants}, fee:entryFee}`). `useCoinBalance()`로 잔액. 결제 버튼: 잔액 ≥ fee → `useEntryPayment(roomId).mutate({nickname, avatarId})` → 성공 시 `useJoinRoom(roomId).mutate({nickname, avatarId})` → 완료 카드 → "대기실 입장" `/play/{pin}`. 잔액 부족 → `useCreateCharge({amount:부족분 올림 프리셋, method, roomId})` → `requestPayment` → `useConfirmCharge(chargeId,{paymentId, roomId})`(응답 `entryPayment`가 있으면 차감까지 끝) → `joinRoom`. 402 `PaymentRequired`·409 `NICKNAME_TAKEN`·410 `Gone` 문구 분기.
- [ ] **Step 2: 검증·커밋** — 목 모드로 482913 결제 완료까지. `feat: 유료 방 결제 연결 — 방 조회·코인 잔액·참가비 차감(402 시 충전→확인→차감)·입장까지 계약 흐름으로`

---

### Task 13: 정리 — 남은 mock 제거·문서·최종 검증

**Files:**
- Delete: 남아 있는 `src/features/**/mock.ts` 전부 (`find src/features -name mock.ts`가 비어야 한다)
- Modify: `src/features/host/README.md`, `src/features/participant/README.md`(목업 → `lib/mocks`·`adapt.ts` 설명), `CLAUDE.md`("`src/lib/mocks/`" 항목에 라우터·픽스처 언급, "아직 넣지 않은 것"에서 STOMP·session-store 제거), `README.md`(env 2개), `docs/superpowers/specs/2026-08-31-api-layer-design.md` §9 가정에 실행 결과 반영

- [ ] **Step 1** — `grep -rn "features/.*/mock\"" src` 0건, `grep -rn "TODO(API)" src` 목록을 다시 보고 계약이 생긴 것은 지우고 `@draft`로 남는 것만 유지.
- [ ] **Step 2** — `pnpm format && pnpm lint && pnpm test && pnpm build && pnpm check:routes` 전부 통과.
- [ ] **Step 3** — Chrome(목 모드)로 핵심 3흐름 확인: `/login`→`/home`, `/host/rooms/new`→`/lobby`→`/live`→`/result`→`/review`, `/join`→`/play/482913`. 콘솔 오류 0.
- [ ] **Step 4: 커밋** `chore: feature mock 제거·README/CLAUDE 갱신 — 목은 lib/mocks 한 곳, 화면은 훅+adapt.ts로만 데이터 수신`

---

## Self-Review

- **Spec coverage**: §2 포함 1~7 → Task 2(1)·4(2)·5(3)·3(4)·6(5)·7~12(6)·13(7). §3 결정(호스트 3라우트 유지·adapt.ts·목 통합·`@draft`)은 Task 3·8~12 규칙에 반영. §4-1 전송 차이 6항목 → Task 1·4. §7 오류 코드 → `error-codes.ts` + 각 화면 분기.
- **Placeholder scan**: Task 3 Step 4·5의 픽스처 값은 "출처 파일에서 복사"로 지시 — 값 자체는 이미 리포에 있으므로 재기술하지 않았다(중복 방지). 나머지 코드 블록은 완전한 코드.
- **Type consistency**: `RankingEntry`는 `dto/session.ts`에 단일 정의, `events.ts`가 import. `MeResponse`는 `UserProfileResponse & {userId?, role?}`로 auth-store·RequireAuth(`profile?.role`)와 호환. `resolveMock(method, url, body?)` 시그니처는 Task 1(예고)→Task 3(확정). `avatarKeyFromId/avatarIdFromKey`는 공통 규칙에 정의하고 Task 8·9·11·12가 사용.
